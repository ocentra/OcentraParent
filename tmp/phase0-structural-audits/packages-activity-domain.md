# packages/activity-domain
- target_kind: TypeScript domain package for activity capture, journal, and aggregation contracts
- owned_paths: `packages/activity-domain/package.json`; `packages/activity-domain/README.md`; `packages/activity-domain/src/*.ts`; `packages/activity-domain/tests/**/*.ts`
- declared_responsibility: shared activity and evidence contracts for child-device observations
- observed_responsibility: activity surface, capture, journal, query, family aggregation, and screen-VLM journal/read-model projection contracts
- should_own: canonical activity capture/query/journal schemas, family aggregation read models, and screen-VLM projection contracts that remain evidence-first and privacy-negative
- should_not_own: portal layout, policy authority, enforcement, or raw content custody claims
- allowed_dependencies: `@ocentra-parent/app-game-domain`; `@ocentra-parent/evidence-domain`; `@ocentra-parent/schema-domain`; `@ocentra-parent/screen-domain`
- suspicious_dependencies: `@ocentra-parent/app-game-domain` and `@ocentra-parent/screen-domain` are acceptable here but are a sign this package is acting as an aggregation bridge across multiple domains, so the seams need to stay narrow
- expected_dependents: activity UI/report consumers, journal/read-model code, screen-VLM proof harnesses, and family aggregation consumers
- shared_contract_schema_usage: strong use of shared evidence primitives, screen evidence state contracts, and app-game primitive/status contracts
- duplicate_or_near_duplicate_shapes: repeated `schemaVersion`, scope/request, custody/source-label, and source-state-summary fields appear across `activity-surface.ts`, `activity-family-aggregation.ts`, `journal.ts`, `query.ts`, and `screen-vlm-journal-read-model.ts`
- id_name_status_drift: moderate; "activity-domain" is accurate, but the package now carries screen VLM projection logic and family aggregation state, which broadens the meaning beyond a simple capture/journal package
- direct_import_vs_event_boundary: direct imports are mostly schema-only and acceptable; `screen-vlm-journal-read-model.ts` composes a cross-domain projection instead of crossing a runtime/event boundary
- event_bus_usage: no obvious bus ownership; the package is mostly schema/read-model composition rather than event emission
- logging_and_proof_chain_expectations: proof-chain shape is encoded through schemas and projection rules, not runtime logs; keep any future runtime hooks redaction-safe and evidence-driven
- boundary_violations: none severe, but the package does act as a bridge across app-game, screen, and activity concerns, so it should resist further expansion into runtime or policy ownership
- dry_common_core_candidates: `activity-surface`, `journal`, `query`, `activity-family-aggregation`, `screen-vlm-journal-read-model`
- dead_frontage_or_shims: none in `src`; this package is mostly real contract code
- proposed_fix_packets: keep the capture/journal/aggregation core compact, and if more screen or app-game projection logic appears, split it into a named projection submodule instead of widening the package root
- dry_score_0_to_100: 74
- score_breakdown:
  - owner_boundary_0_to_20: 17
  - dependency_direction_0_to_15: 12
  - shared_contract_schema_0_to_15: 12
  - duplicate_shape_control_0_to_15: 8
  - direct_import_vs_event_boundary_0_to_15: 12
  - decoupling_and_common_core_0_to_10: 7
  - proof_logging_alignment_0_to_10: 6
- first_fix_roi: separate any future screen-specific projection helpers from the main activity surface before the package gets another cross-domain branch
- severity: medium
- confidence: high
- evidence_paths:
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\activity-domain\package.json`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\activity-domain\README.md`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\activity-domain\src\package-info.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\activity-domain\src\activity-surface.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\activity-domain\src\activity-family-aggregation.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\activity-domain\src\journal.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\activity-domain\src\screen-vlm-journal-read-model.ts`

## Current Refresh Audit - 2026-06-19

- current responsibility: shared activity, journal, query, capture, and read-model contracts for child-device observations, plus family aggregation and the screen-VLM journal/read-model projection seam.
- dependencies: `@ocentra-parent/schema-domain/effect`, `@ocentra-parent/evidence-domain/*`, `@ocentra-parent/app-game-domain/*`, and `@ocentra-parent/screen-domain/*`; the package is still a cross-domain contract bridge rather than a narrow single-domain leaf.
- boundary violations: no severe runtime or policy ownership leak, but `activity-surface.ts` now owns browser, games, network, app-use, and screen read models in one surface file, so the package boundary is broader than the package name suggests.
- duplicated shapes: repeated `schemaVersion` + `request` + `state` + `generatedAt` + `summary` read-model headers; repeated custody/sourceLabel/rawChildEvidenceIncluded fields on report/history/source rows; repeated source-state bucket counting and device-id bucketing in family aggregation.
- barrel/reexport/shim debt: no TS/JS re-export barrels found in `src`; the package exports are explicit in `package.json`, but `capture.ts` and `journal-primitives.ts` are internal-only and should stay intentionally private or be surfaced deliberately later.
- schema/contract drift: README still reads like a compact activity-contract package, while the live source now includes screen VLM projection and a wide `ActivitySurfaceSchema` family of read models; `ActivityFamilyAggregationModelSchema` also hardcodes service/UI ownership strings, which is contract drift if that moves again.
- event bus/log/proof misuse: no event bus or runtime log ownership in this package; the proof usage is schema-only and stays evidence-negative, but the proof story is thin because the `proofs/` tree only has `.gitkeep` placeholders.
- test/proof structure issues: coverage is concentrated in Vitest unit/contract tests under `tests/unit` and `tests/contract`; there is no package-local executable proof harness yet for the screen-VLM journal/read-model seam, so the proof path is easy to regress without a dist-level check.
- current DRY score: 74/100.
- fix-pass recommendation: split `activity-surface.ts` into smaller surface/history/read-model modules, pull repeated custody/source-label/default/bucket logic into shared helpers, and add a concrete proof harness for `tests/contract/screen-vlm-journal-read-model.test.ts`.
- decouple-pass recommendation: if screen-VLM projection keeps growing, move it into a named projection submodule or a separate boundary so activity reporting does not continue to absorb screen-specific projection semantics.
- sequencing/blockers: no write blocker for this audit refresh; the next fix pass should target `packages/activity-domain/src/activity-surface.ts` and `packages/activity-domain/src/activity-family-aggregation.ts` first, then reassess `packages/activity-domain/src/screen-vlm-journal-read-model.ts` and `packages/activity-domain/tests/contract/screen-vlm-journal-read-model.test.ts`.
- exact likely file paths: `packages/activity-domain/package.json`, `packages/activity-domain/README.md`, `packages/activity-domain/src/activity-surface.ts`, `packages/activity-domain/src/activity-family-aggregation.ts`, `packages/activity-domain/src/screen-vlm-journal-read-model.ts`, `packages/activity-domain/src/journal.ts`, `packages/activity-domain/src/capture.ts`, `packages/activity-domain/src/journal-primitives.ts`, `packages/activity-domain/tests/unit/*.test.ts`, `packages/activity-domain/tests/contract/screen-vlm-journal-read-model.test.ts`, `packages/activity-domain/proofs/local/.gitkeep`, `packages/activity-domain/proofs/ci/.gitkeep`.

## Current Refresh Audit - 2026-06-19

- responsibility: shared activity contracts for capture, journal, query, family aggregation, and screen-VLM projection/read-model behavior; this is still a contract package, but it now spans multiple surface shapes instead of one narrow activity leaf.
- dependencies: `@ocentra-parent/schema-domain/effect`, `@ocentra-parent/evidence-domain/contracts`, `@ocentra-parent/evidence-domain/primitives`, `@ocentra-parent/app-game-domain/*`, and `@ocentra-parent/screen-domain/*`; the live boundary is a cross-domain contract bridge.
- violations: no severe runtime or policy ownership leak, but `activity-surface.ts` is carrying browser, app-use, games, network, and screen read models together, which is broader than the package name implies.
- duplicated shapes: repeated `schemaVersion`/`request`/`state`/`generatedAt`/`summary` read-model headers; repeated custody and source-label fields on report, history, and saved-report rows; repeated ready/offline/stale/unavailable/unreachable/error bucketing in family aggregation.
- barrel/re-export/shim debt: no TS/JS barrel or re-export shims are present in `src`; the root surface is explicit file-by-file, but the package still has internal helper split debt because `capture.ts` and `journal-primitives.ts` are private-support modules rather than a clearly separated subpackage.
- schema drift: `README.md` still describes a compact activity contract package while the code now includes screen-VLM projection logic and wide read models; `ActivityFamilyAggregationModelSchema` also hardcodes service/UI owner strings, so any owner rename will require contract churn.
- event/log/proof misuse: no event bus ownership or runtime log ownership is visible here; proof is schema-first and evidence-negative, but the proof story is thin because `proofs/local` and `proofs/ci` are only `.gitkeep` placeholders and the screen-VLM seam relies on one contract test for executable evidence.
- test/proof structure issues: tests are concentrated in `tests/unit` plus one `tests/contract/screen-vlm-journal-read-model.test.ts`; there is no package-local dist/proof harness for the screen-VLM projection seam, so regressions can slip past the current structure.
- dry score 0-100: 72/100.
- dry score reasons: strong contract typing and mostly schema-only dependencies keep the score healthy, but the package has grown into a multi-surface aggregation bridge, duplicates shared headers and bucket logic, and lacks a real proof harness for the newest projection seam.
- fix recommendation: split `activity-surface.ts` into smaller surface/history/read-model modules, extract the repeated custody/source-label and source-state bucketing helpers, and add a concrete executable proof path for the screen-VLM contract seam.
- decouple recommendation: if screen-VLM keeps expanding, move it into a named projection submodule or separate boundary so activity contracts stop absorbing screen-specific projection semantics.
- blockers: none for the audit refresh itself; the only practical risk is that future screen/report expansion will keep widening the boundary unless the next fix pass is intentionally split.
- exact likely paths: `packages/activity-domain/package.json`, `packages/activity-domain/README.md`, `packages/activity-domain/src/activity-surface.ts`, `packages/activity-domain/src/activity-family-aggregation.ts`, `packages/activity-domain/src/screen-vlm-journal-read-model.ts`, `packages/activity-domain/src/journal.ts`, `packages/activity-domain/src/capture.ts`, `packages/activity-domain/src/journal-primitives.ts`, `packages/activity-domain/tests/unit/activity-surface.test.ts`, `packages/activity-domain/tests/unit/activity-family-aggregation.test.ts`, `packages/activity-domain/tests/contract/screen-vlm-journal-read-model.test.ts`, `packages/activity-domain/proofs/local/.gitkeep`, `packages/activity-domain/proofs/ci/.gitkeep`.
