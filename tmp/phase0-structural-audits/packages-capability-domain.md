# packages/capability-domain
- target_kind: ts package
- owned_paths: packages/capability-domain/package.json; packages/capability-domain/src/package-info.ts; packages/capability-domain/src/capabilities.ts; packages/capability-domain/src/capability-data.ts; packages/capability-domain/tests/unit/package-info.test.ts; packages/capability-domain/tests/unit/capabilities.test.ts
- declared_responsibility: shared capability names, statuses, and capability catalog contracts
- observed_responsibility: a platform capability catalog for Windows, Linux, macOS, Android, and iOS, with schema-backed status/note entries and a canonical platform matrix
- should_own: capability names, capability status enums, platform capability catalog data, and package identity for capability readiness reporting
- should_not_own: family identity or household authorization semantics; device/session/recovery policy logic; runtime enforcement implementations; UI or service behavior beyond the catalog contract
- allowed_dependencies: `@ocentra-parent/schema-domain`; local capability data module
- suspicious_dependencies: `ParentControlPlatformCapabilityInputs` is raw data frontage that encodes readiness claims outside the schema file; the wildcard export map broadens surface area; several capability names overlap policy/runtimes that likely belong to other owners
- expected_dependents: docs/reporting layers, dashboards, and any product surfaces that need a typed capability catalog
- shared_contract_schema_usage: good Effect Schema usage for the catalog shape; raw data is parsed into schema-validated entries
- duplicate_or_near_duplicate_shapes: capability platform/status vocabulary overlaps with family-domain and entitlement-style gatekeeping, but the package’s matrix is more catalog-like than the family authorization logic
- id_name_status_drift: capability names are stable but the human descriptions blur implementation status with product claims; note text can drift from actual runtime proof
- direct_import_vs_event_boundary: direct import is fine for catalog data; no event boundary is present
- event_bus_usage: none visible
- logging_and_proof_chain_expectations: if the catalog is used to drive proof or release reporting, it should stay honest about manual-required vs implemented vs scaffold states; there is no logging chain here unless a runtime consumer adds one
- boundary_violations: the package carries a mixed responsibility of schema, status catalog, and release-readiness commentary in data notes; that can become a policy/reporting shim if not kept strictly descriptive
- dry_common_core_candidates: a shared capability taxonomy package for readiness tracking; a separate platform-proof evidence model if catalog notes start encoding proof outcomes
- dead_frontage_or_shims: `package-info.ts` is pure identity frontage; `capability-data.ts` is data frontage that should stay subordinate to the schema contract and not become a second source of truth
- proposed_fix_packets: keep the package strictly as the canonical capability catalog and move any proof-status narrative into a separate evidence/reporting layer if it starts driving product claims; avoid widening it into entitlement or family policy ownership
- severity: medium
- confidence: medium-high
- evidence_paths: packages/capability-domain/package.json; packages/capability-domain/src/package-info.ts; packages/capability-domain/src/capabilities.ts; packages/capability-domain/src/capability-data.ts; packages/capability-domain/tests/unit/package-info.test.ts; packages/capability-domain/tests/unit/capabilities.test.ts

## Current Refresh Audit - 2026-06-19
- responsibility: canonical capability catalog for platform/capability/status naming plus package identity frontage; it should not own family identity, enforcement policy, or runtime proof truth
- deps: only `@ocentra-parent/schema-domain` and the local capability-data module; dependency depth is shallow, which keeps the package small but also means any claim drift is entirely self-inflicted by the catalog
- violations: the package still mixes catalog data with readiness commentary, and the status notes read like product/proof claims instead of a pure contract surface
- duplicated shapes: platform/status/name vocabularies are duplicated across schema literals, runtime constants, and raw input data; package identity is also duplicated between `package.json` and `src/package-info.ts`
- barrel/reexport/shim debt: `package.json` exposes a wildcard `./*` export map, which acts like a broad shim over the dist surface; `src/package-info.ts` is a tiny identity frontage and should stay that way
- schema drift: `capability-data.ts` is the main drift risk because the note text and status strings can diverge from actual runtime proof, especially where the text asserts guardrails, support states, or platform behavior
- event/log/proof misuse: there is no event bus or logging chain here, but the notes embed proof-like statements that should not be treated as evidence or log output; they are descriptive catalog text only
- test/proof structure issues: tests cover a few representative platform splits and package identity, but they do not exhaustively verify every platform/capability pair, export surface stability, or literal-to-data parity
- score: 6/10 structural risk
- fix recommendation: keep the package as a narrow typed catalog, add parity/exhaustiveness checks between the schema literals and the data table, and tighten export surface expectations around the explicit entrypoints
- decouple recommendation: move proof/readiness narrative into a separate evidence or reporting package if the catalog must continue carrying status notes; the catalog should retain only stable capability taxonomy and contract-shape data
- blockers: none for the audit refresh itself
- exact likely paths: `packages/capability-domain/package.json`, `packages/capability-domain/src/package-info.ts`, `packages/capability-domain/src/capabilities.ts`, `packages/capability-domain/src/capability-data.ts`, `packages/capability-domain/tests/unit/package-info.test.ts`, `packages/capability-domain/tests/unit/capabilities.test.ts`

## Current Refresh Audit - 2026-06-19
- responsibility: this package is the canonical typed catalog for parent-control capability names, statuses, and platform matrices; it should stay a catalog boundary, not a policy or enforcement owner
- dependencies: the package only depends on `@ocentra-parent/schema-domain` plus local data, so the main risk is not dependency sprawl but claim drift inside the catalog itself
- violations: catalog notes still read like product/readiness assertions, which blurs descriptive data into claim-bearing language; that is a boundary smell for a shared contract package
- duplicated shapes: platform, status, and capability-name vocabularies are repeated across schema literals, constant objects, and raw catalog data; package identity is also duplicated between `package.json` and `src/package-info.ts`
- barrel/re-export/shim debt: `package.json` exposes a broad `./*` export surface, which behaves like a shim over the dist tree instead of a narrowly named API; that makes it easier for accidental surface growth to hide
- schema drift: the highest drift risk sits in `src/capability-data.ts`, where note text can diverge from actual runtime/proof state and where the raw catalog can outgrow the schema intent
- event/log/proof misuse: there is no live event or logging chain here, but the note fields contain proof-like language that should not be interpreted as evidence, telemetry, or execution truth
- test/proof structure issues: the tests cover identity and representative catalog shape, but they do not fully prove export-surface stability, exhaustive platform/capability parity, or note-to-schema consistency
- DRY score: 64/100; the package is compact and typed, but it repeats core vocabularies, keeps identity in two places, and lets descriptive notes carry too much claim weight
- fix recommendation: keep the package narrowly focused on the authoritative catalog, add parity/exhaustiveness checks for capability/platform entries, and make export expectations explicit instead of wildcard-driven
- decouple recommendation: move readiness/proof narrative into a separate evidence/reporting layer if those claims must be maintained; this package should retain only stable capability taxonomy and contract-shape data
- blockers: none for this audit refresh
- exact likely paths: `packages/capability-domain/package.json`, `packages/capability-domain/src/package-info.ts`, `packages/capability-domain/src/capabilities.ts`, `packages/capability-domain/src/capability-data.ts`, `packages/capability-domain/tests/unit/package-info.test.ts`, `packages/capability-domain/tests/unit/capabilities.test.ts`
