# packages/family-domain
- target_kind: ts package
- owned_paths: packages/family-domain/package.json; packages/family-domain/src/package-info.ts; packages/family-domain/src/reference-primitives.ts; packages/family-domain/src/references.ts; packages/family-domain/src/household-authority.ts; packages/family-domain/src/child-profile.ts; packages/family-domain/src/setup-lifecycle.ts; packages/family-domain/src/session-lifecycle.ts; packages/family-domain/tests/unit/package-info.test.ts; packages/family-domain/tests/unit/references.test.ts; packages/family-domain/tests/unit/household-authority.test.ts; packages/family-domain/tests/unit/setup-lifecycle.test.ts; packages/family-domain/tests/unit/session-lifecycle.test.ts; packages/family-domain/tests/unit/child-profile.test.ts; packages/family-domain/tests/unit/invite-recovery-lifecycle.test.ts; packages/family-domain/tests/unit/token-lifecycle.test.ts
- declared_responsibility: shared family, profile, device, actor, and evidence reference contracts
- observed_responsibility: a large canonical TypeScript family domain covering reference primitives, household/device authority, child profile bridging, setup invites and recovery, and session credential/token lifecycle
- should_own: canonical TS family contracts, branded reference primitives, household/device authority inputs/decisions, setup/recovery lifecycle schemas, and session lifecycle schemas
- should_not_own: a second divergent family model under different names; runtime behavior that belongs in app/service layers; broad product capability cataloging; Rust-specific protocol concerns
- allowed_dependencies: `@ocentra-parent/schema-domain`; no other package dependency is declared in the manifest snapshot
- suspicious_dependencies: heavy internal duplication with `crates/family-identity-core`; repeated literal/schema catalogs across multiple source files; `exports` wildcard-style frontage is broader than the named module list and can hide module ownership
- expected_dependents: portal/service/runtime code that needs typed family contracts, plus any Rust mirror work that consumes the same semantics later
- shared_contract_schema_usage: strong use of Effect Schema across the package; this is the correct shared-contract layer for TS
- duplicate_or_near_duplicate_shapes: household authority enums/decisions mirror the Rust crate; setup invite/recovery decisions mirror the Rust crate; session token authorization and issuance mirror the Rust crate; child profile reference logic overlaps with the Rust family identity story
- id_name_status_drift: several names are semantically close but not identical to the Rust crate (`HouseholdRole` vs `FamilyActorRole`, `DeviceAuthorityAction` vs `HouseholdAuthorityAction`, `membershipState` vs Rust membership enums), which is a drift risk if both remain active
- direct_import_vs_event_boundary: direct imports are appropriate for schema/contract reuse here; runtime behavior still happens in direct functions rather than evented boundaries
- event_bus_usage: none visible
- logging_and_proof_chain_expectations: as a domain package, it should remain pure-contract focused; if runtime behavior is added later it must follow repo logging/proof-chain rules, but the current package is mostly contract definitions and pure decision helpers
- boundary_violations: the package is carrying a very broad family domain that overlaps almost one-for-one with the Rust crate; `household-authority.ts`, `setup-lifecycle.ts`, and `session-lifecycle.ts` all bundle both schema and policy logic, which makes the package a likely common-core candidate rather than a thin boundary package
- dry_common_core_candidates: a single canonical family authority model shared by TS and Rust; a shared recovery/setup/session policy catalog; a split between raw reference primitives and decision engines if the package keeps growing
- dead_frontage_or_shims: `package-info.ts` is pure identity frontage; `package.json` exports are broad path frontage, not domain logic
- proposed_fix_packets: decide whether this package is the canonical owner for family contracts or just a mirror of the Rust core; if canonical, align names and shapes with the Rust owner or split out the truly shared primitives into a smaller common-core package; if mirror, reduce it to adapters and contract conversions
- severity: high
- confidence: high
- evidence_paths: packages/family-domain/package.json; packages/family-domain/src/package-info.ts; packages/family-domain/src/reference-primitives.ts; packages/family-domain/src/references.ts; packages/family-domain/src/household-authority.ts; packages/family-domain/src/child-profile.ts; packages/family-domain/src/setup-lifecycle.ts; packages/family-domain/src/session-lifecycle.ts; packages/family-domain/tests/unit/package-info.test.ts; packages/family-domain/tests/unit/references.test.ts; packages/family-domain/tests/unit/household-authority.test.ts; packages/family-domain/tests/unit/setup-lifecycle.test.ts; packages/family-domain/tests/unit/session-lifecycle.test.ts; packages/family-domain/tests/unit/child-profile.test.ts; packages/family-domain/tests/unit/invite-recovery-lifecycle.test.ts; packages/family-domain/tests/unit/token-lifecycle.test.ts

## Current Refresh Audit - 2026-06-19
- responsibility: canonical TypeScript family contracts for references, household/device authority, setup/recovery, and session/token lifecycle
- deps: `@ocentra-parent/schema-domain` only; no secondary package dependency is declared in the manifest snapshot
- violations: the package is still broad for one boundary; authority and lifecycle helpers mix schemas with policy decisions; several names still drift from the Rust family-identity-core counterparts
- duplicated shapes: household roles, membership states, device trust states, session states, and recovery states mirror `crates/family-identity-core`; child-profile and reference structures overlap with setup-domain consumer expectations
- barrel/reexport/shim debt: no TS or Rust re-export shims were found in the owned src/tests surface; `package.json` exports are broad frontage, and `src/package-info.ts` is identity-only frontage
- schema drift: `HouseholdRole` vs `FamilyActorRole`, `DeviceAuthorityAction` vs `HouseholdAuthorityAction`, and `membershipState` vs Rust membership enums remain drift risks if both sides stay active
- event/log/proof misuse: none visible in this package; it is still pure-contract and decision-helper code, but any future runtime behavior here would need the repo logging/proof-chain rules
- test/proof structure issues: coverage is entirely unit-level; parsing and decision assertions are strong, but there is no dedicated consumer-diff proof layer, no cross-package mirror check, and no proof artifact directory with substantive outputs
- score: 4/10
- fix recommendation: decide whether this package is the canonical family-contract owner or a mirror of the Rust core; if canonical, align names and shapes with the Rust owner and split shared primitives away from policy engines; if mirror, reduce it to adapter and contract-conversion surfaces
- decouple recommendation: extract a smaller common-core for primitive identity and reference brands, then keep authority, setup, and session decision catalogs in separate modules or move the source of truth to one side
- blockers: no immediate code blocker for the audit refresh; the main blocker is ownership clarity between TS and Rust mirroring
- exact likely paths: `packages/family-domain/package.json`; `packages/family-domain/src/package-info.ts`; `packages/family-domain/src/reference-primitives.ts`; `packages/family-domain/src/references.ts`; `packages/family-domain/src/household-authority.ts`; `packages/family-domain/src/child-profile.ts`; `packages/family-domain/src/setup-lifecycle.ts`; `packages/family-domain/src/session-lifecycle.ts`; `packages/family-domain/tests/unit/package-info.test.ts`; `packages/family-domain/tests/unit/references.test.ts`; `packages/family-domain/tests/unit/household-authority.test.ts`; `packages/family-domain/tests/unit/setup-lifecycle.test.ts`; `packages/family-domain/tests/unit/session-lifecycle.test.ts`; `packages/family-domain/tests/unit/child-profile.test.ts`; `packages/family-domain/tests/unit/invite-recovery-lifecycle.test.ts`; `packages/family-domain/tests/unit/token-lifecycle.test.ts`

## Current Refresh Audit - 2026-06-19
- responsibility: this package owns the canonical TypeScript family-contract surface for references, household/device authority, child profile bridging, setup/recovery lifecycle, and session/token lifecycle
- dependencies: `@ocentra-parent/schema-domain` only in the manifest snapshot; no other package dependency is declared, and there is no local README or rules file under `packages/family-domain`
- violations: the boundary is still too broad for one package, with policy helpers and schema definitions living together across multiple files, and the TS naming still diverges from the Rust family-identity-core vocabulary
- duplicated shapes: household roles, authority decisions, membership states, session states, and recovery states are mirrored across the TS package and `crates/family-identity-core`; reference primitives and child-profile shapes also overlap with adjacent consumer expectations
- barrel/re-export/shim debt: no source re-export shim was visible in the small owned surface I read, but `package.json` exposes broad per-entry frontage that can conceal ownership drift
- schema drift: `HouseholdRole` vs `FamilyActorRole`, `DeviceAuthorityAction` vs `HouseholdAuthorityAction`, and `membershipState` vs the Rust membership enums remain live drift risks if both implementations stay active
- event/log/proof misuse: none visible in the package itself; it remains contract-first and pure-decision oriented, but any runtime expansion here would need the repo logging and proof-chain rules
- test/proof structure issues: coverage is still unit-only in the package-local tests; there is no visible consumer-mirror proof layer, no cross-language drift check, and no dedicated artifact path for audit evidence
- dry score: 43/100; the package centralizes useful shared contracts, but it loses DRY value to broad ownership, mirrored TS/Rust shapes, and split naming that still needs normalization
- fix recommendation: choose one source of truth for family contracts, then either align TS names and shapes to the Rust owner or reduce this package to a narrower adapter/contract-conversion layer
- decouple recommendation: split primitive reference brands from policy engines, and isolate authority, setup, and session decisions into smaller modules so the package boundary becomes thinner and easier to mirror or retire
- blockers: ownership clarity between TS and Rust remains the real blocker; there is no immediate code blocker for the audit refresh itself
- exact likely paths: `packages/family-domain/package.json`; `packages/family-domain/src/package-info.ts`; `packages/family-domain/src/reference-primitives.ts`; `packages/family-domain/src/references.ts`; `packages/family-domain/src/household-authority.ts`; `packages/family-domain/src/child-profile.ts`; `packages/family-domain/src/setup-lifecycle.ts`; `packages/family-domain/src/session-lifecycle.ts`; `packages/family-domain/tests/unit/package-info.test.ts`; `packages/family-domain/tests/unit/references.test.ts`; `packages/family-domain/tests/unit/household-authority.test.ts`; `packages/family-domain/tests/unit/setup-lifecycle.test.ts`; `packages/family-domain/tests/unit/session-lifecycle.test.ts`; `packages/family-domain/tests/unit/child-profile.test.ts`; `packages/family-domain/tests/unit/invite-recovery-lifecycle.test.ts`; `packages/family-domain/tests/unit/token-lifecycle.test.ts`
