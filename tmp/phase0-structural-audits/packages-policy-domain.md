# packages/policy-domain
- target_kind: TypeScript package
- owned_paths: packages/policy-domain/package.json; packages/policy-domain/src/package-info.ts; packages/policy-domain/src/*.ts
- declared_responsibility: Shared parent policy decision, action, target, and reason contracts.
- observed_responsibility: A large policy contract package that mixes shared schemas, runtime validators, preview logic, event contracts, compiler support, and policy state helpers.
- should_own: Canonical policy literals/schemas, schedule and preview contracts, policy events, approval/authority contracts, and parser/validator entrypoints.
- should_not_own: Feature-specific orchestration, transport plumbing, or runtime execution that belongs in downstream feature layers.
- allowed_dependencies: @ocentra-parent/event-domain; @ocentra-parent/family-domain; @ocentra-parent/schema-domain.
- suspicious_dependencies: Heavy direct dependency on family-domain is expected, but the package is broad enough that small contract changes can cascade through many unrelated policy surfaces.
- expected_dependents: billing/setup/provisioning/runtime code that needs policy contracts, plus tests and proof files that consume policy schemas.
- shared_contract_schema_usage: Uses the same branded-string/literal/schema pattern as the Rust policy-control crate and carries the canonical policy event/preview/authority vocabulary.
- duplicate_or_near_duplicate_shapes: Strong overlap with crates/policy-control-core for IDs, statuses, scopes, reasons, compiler domains, and event shapes.
- id_name_status_drift: The package name suggests a shared policy contract surface, but the actual code also owns preview validation, schedule parsing, and event replay behavior.
- direct_import_vs_event_boundary: Direct imports dominate; there is no event bus layer here, only contracts and validators.
- event_bus_usage: None.
- logging_and_proof_chain_expectations: Policy-event redaction helpers and deterministic parser filters should remain stable because other proof files likely rely on them.
- boundary_violations: The package is more than a shared contract layer; it is also a runtime validation and replay core, which makes the boundary broader than the package description implies.
- dry_common_core_candidates: A smaller shared policy-primitive core between this package and the Rust policy-control crate.
- dead_frontage_or_shims: `src/package-info.ts` is a thin constant shim, but the rest of the package is active.
- proposed_fix_packets: Split canonical policy literals/schemas from runtime validators/replay helpers, and align the Rust/TS policy vocabularies to one owner for the shared core.
- severity: medium
- confidence: high
- evidence_paths: packages/policy-domain/package.json; packages/policy-domain/src/package-info.ts; packages/policy-domain/src/policy.ts; packages/policy-domain/src/policy-event.ts; packages/policy-domain/src/authority.ts; packages/policy-domain/src/policy-compiler.ts

## Current Refresh Audit - 2026-06-19
- responsibility: Shared parent policy decision, action, target, and reason contracts.
- dependencies: `@ocentra-parent/event-domain`, `@ocentra-parent/family-domain`, and `@ocentra-parent/schema-domain`.
- violations: The package is broader than its description because it mixes canonical schemas with runtime validators, preview logic, event contracts, compiler support, and policy-state helpers.
- duplicated_shapes: Strong overlap with `crates/policy-control-core` remains for IDs, statuses, scopes, reasons, compiler domains, and event shapes.
- barrel_reexport_shim_debt: Low compared with the screen and AI packages, but `src/package-info.ts` is still a thin shim and the package surface itself is overly wide.
- schema_drift: Moderate; policy decision vocabulary is consistent, but preview validation, schedule parsing, and replay behavior are co-located with the contract layer.
- event_log_proof_misuse: No bus misuse is present, but the package carries redaction helpers and deterministic parser filters that should stay stable for downstream proof files.
- test_proof_structure_issues: Runtime validators and replay helpers in the same package make it harder to isolate contract-only tests from behavior-oriented tests.
- dry_score_0_to_100: 58
- dry_score_reasons: The package still has a strong canonical policy vocabulary, but breadth across validation, replay, preview, and compiler behavior keeps the DRY and ownership scores below the screen/AI packages.
- fix_recommendation: Split canonical policy literals/schemas from runtime validators and replay helpers, then keep the shared contract owner narrow and explicit.
- decouple_recommendation: Extract a smaller policy-primitive core shared with the Rust policy-control crate so the contract layer stops carrying workflow logic.
- blockers: Audit-only refresh; the downstream consumer map and replay/validator implications were not re-traced in this pass.
- exact_likely_paths: `packages/policy-domain/package.json`; `packages/policy-domain/src/package-info.ts`; `packages/policy-domain/src/policy.ts`; `packages/policy-domain/src/policy-event.ts`; `packages/policy-domain/src/authority.ts`; `packages/policy-domain/src/policy-compiler.ts`; `packages/policy-domain/tests/unit/policy.test.ts`; `packages/policy-domain/tests/unit/policy-event.test.ts`
