# packages/endpoint-domain
- target_kind: ts package, endpoint and route boundary package
- owned_paths: packages/endpoint-domain/package.json; packages/endpoint-domain/README.md; packages/endpoint-domain/tsconfig.json; packages/endpoint-domain/src/package-info.ts; packages/endpoint-domain/src/types/brands.ts; packages/endpoint-domain/src/constants/http.ts; packages/endpoint-domain/src/constants/lan-pairing.ts; packages/endpoint-domain/src/constants/billing-account.ts; packages/endpoint-domain/src/constants/sync-export.ts; packages/endpoint-domain/src/constants/versions.ts; packages/endpoint-domain/tests/unit/brands.test.ts; packages/endpoint-domain/tests/unit/lan-pairing.test.ts; packages/endpoint-domain/tests/unit/billing-account.test.ts; packages/endpoint-domain/tests/unit/sync-export.test.ts; packages/endpoint-domain/tests/unit/package-info.test.ts
- declared_responsibility: endpoint, path, header, query, version, and route-boundary constants
- observed_responsibility: branded endpoint/path/header/query decoders plus grouped constant catalogs for HTTP, LAN pairing, billing-account, sync-export, and versioned routes
- should_own: path/header/query brands; endpoint IDs; contract-only route constants; versioned route prefixes; cross-runtime endpoint identifiers
- should_not_own: WebSocket payload contracts; portal route/nav semantics; product policy decisions; runtime behavior beyond constant catalogs and decoders
- allowed_dependencies: @ocentra-parent/schema-domain; no runtime service dependencies beyond the contract helper package
- suspicious_dependencies: the package is broad in surface area, but the breadth is mostly intentional constant catalog ownership rather than runtime coupling
- expected_dependents: portal/runtime callers; service callers; Rust service path mirrors; tests that assert contract names and brand decoding
- shared_contract_schema_usage: strong use of Effect Schema brands and decode helpers, which fits the package boundary
- duplicate_or_near_duplicate_shapes: `BillingAccount*` and `AccountDistribution*` blocks repeat the same endpoint/id/path/header/query/version pattern; `ParentOwnedSyncExport*` and `RemoteConnectorStatus*` repeat the same route-contract pattern; `LanPairing*` repeats the same path/id/header pattern
- id_name_status_drift: moderate; the package has many parallel catalogs that need synchronized naming across generated dist exports and consumer code
- direct_import_vs_event_boundary: direct imports are correct because this is a contract package, not an event boundary
- event_bus_usage: none; this package is a static contract surface
- logging_and_proof_chain_expectations: no runtime logging needed, but any future runtime consumers should emit proof-chain logs when these constants gate command/event behavior
- boundary_violations: none obvious; the package is contract-heavy but still within its declared remit
- dry_common_core_candidates: a shared route/catalog builder for repeated endpoint-id/path/header/query/version groups; a single helper for versioned API prefix construction; a domain helper for contract-only markers
- dead_frontage_or_shims: `src/package-info.ts` is just package-name frontage, which is acceptable; the bigger issue is repeated catalog shapes across constants modules
- proposed_fix_packets: factor repeated endpoint catalog patterns into a small internal builder module while keeping the external exports stable
- dry_score_0_to_100: 79
- score_breakdown:
  - owner_boundary_0_to_20: 17
  - dependency_direction_0_to_15: 14
  - shared_contract_schema_0_to_15: 14
  - duplicate_shape_control_0_to_15: 9
  - direct_import_vs_event_boundary_0_to_15: 14
  - decoupling_and_common_core_0_to_10: 6
  - proof_logging_alignment_0_to_10: 5
- first_fix_roi: medium; extract a reusable catalog pattern for the repeated endpoint groups
- severity: medium
- confidence: high
- evidence_paths: packages/endpoint-domain/package.json; packages/endpoint-domain/README.md; packages/endpoint-domain/src/package-info.ts; packages/endpoint-domain/src/types/brands.ts; packages/endpoint-domain/src/constants/http.ts; packages/endpoint-domain/src/constants/lan-pairing.ts; packages/endpoint-domain/src/constants/billing-account.ts; packages/endpoint-domain/src/constants/sync-export.ts; packages/endpoint-domain/src/constants/versions.ts

## Current Refresh Audit - 2026-06-19
- responsibility: shared endpoint, path, header, query, version, and route-boundary contracts; the package is a contract surface, not runtime behavior
- dependencies: `@ocentra-parent/schema-domain` only for Effect Schema brands and decode helpers; no runtime service dependency should be introduced here
- boundary_violations: none visible in the package itself; the main risk is consumer drift if runtime code starts owning route strings, ids, or headers that should stay here
- duplicated_shapes: `BillingAccount*` and `AccountDistribution*` repeat the same endpoint-id/path/header/query/version catalog pattern; `ParentOwnedSyncExport*` and `RemoteConnectorStatus*` repeat the same contract-table pattern; `LanPairing*` repeats path/id/header grouping with the same decode flow
- barrel_reexport_shim_debt: none found in this package; there are no local barrels or re-export shims, and `src/package-info.ts` is only package-name frontage
- schema_drift: moderate risk because endpoint ids, paths, headers, and version strings are duplicated across several catalogs and consumers; the likely drift vector is a rename or path change in one catalog without a matching test or downstream mirror update
- event_log_proof_misuse: none in this package, because it is static contract data; misuse would appear in downstream runtime consumers that gate command/event/read-model behavior on these constants without proof-chain logging
- test_proof_structure_issues: the unit tests are narrow contract checks, which is correct for this package, but the suite does not yet collapse repeated catalog shape coverage into a shared invariant; that leaves duplication risk under-tested
- dry_score_0_to_100: 76
- dry_score_reasons: strong ownership boundary and schema-brand usage raise the score; repeated catalog shapes, synchronized naming burden, and consumer-mirror drift lower it; the package is still mostly intentional contract repetition rather than accidental runtime coupling
- fix_recommendation: factor the repeated endpoint catalog pattern into a small internal builder/helper while keeping the exported constants stable and preserving the current contract names
- decouple_recommendation: separate truly shared primitives like API version prefix construction and brand-decode wrappers from the per-domain catalog tables so the package keeps one source of truth for prefix logic
- blockers: no local implementation blocker; the only practical blocker is that any refactor here must preserve external constant names because downstream callers likely depend on them
- exact_likely_paths: `packages/endpoint-domain/src/types/brands.ts`; `packages/endpoint-domain/src/constants/http.ts`; `packages/endpoint-domain/src/constants/versions.ts`; `packages/endpoint-domain/src/constants/lan-pairing.ts`; `packages/endpoint-domain/src/constants/billing-account.ts`; `packages/endpoint-domain/src/constants/sync-export.ts`; `packages/endpoint-domain/tests/unit/*.test.ts`; downstream mirrors in service/runtime callers that reference these constants
