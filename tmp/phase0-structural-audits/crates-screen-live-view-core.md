# crates/screen-live-view-core
- target_kind: Rust crate
- owned_paths: crates/screen-live-view-core/Cargo.toml; crates/screen-live-view-core/src/lib.rs; crates/screen-live-view-core/src/live_view_runtime.rs; crates/screen-live-view-core/src/live_view_worker.rs
- declared_responsibility: Live-view runtime readiness and worker startup gating for screen live-view behavior.
- observed_responsibility: Owns live-view observation intent helpers, runtime readiness decisions, and worker startup/execution readiness records.
- should_own: Live-view runtime gating, worker startup gating, and child-domain observation/event helpers for live-view flows.
- should_not_own: A broad facade that hides the real module boundaries or duplicate readiness logic that belongs in a shared screen core.
- allowed_dependencies: ocentra-parent-agent-protocol.
- suspicious_dependencies: None; the dependency set is narrow and appropriate.
- expected_dependents: Live-view service bootstrap code, screen-domain readiness proofs, and any caller that needs to decide if live-view can start.
- shared_contract_schema_usage: ChildDomainObservedEvent; ChildDomainObservedEventProfile; ChildDomainEvidenceRecordedEvent; ChildDomainAiAnalysisRequestedEvent; ChildDomainPolicyEvaluationRequestedEvent; ScreenLiveViewRuntimeDecision; ScreenLiveViewWorkerStartupDecision.
- duplicate_or_near_duplicate_shapes: screen_live_view_observed_profile mirrors screen_observed_profile from crates/screen-core; live-view runtime/worker readiness mirrors screen runtime decision/record layering.
- id_name_status_drift: Medium; startup/runtime/session/worker state names overlap conceptually and make live-view readiness harder to compare with screen-core at a glance.
- direct_import_vs_event_boundary: Direct imports are fine; no event bus. The boundary issue is facade duplication, not a missing event hop.
- event_bus_usage: None; helper functions construct protocol events locally.
- logging_and_proof_chain_expectations: No runtime logging here; readiness decisions should stay schema/proof-only and leave any redaction or diagnostics to callers.
- boundary_violations: Rust public re-export facade in lib.rs; near-duplicate observed-profile and worker/runtime decision layering relative to screen-core.
- dry_common_core_candidates: Shared observed-event/profile helper with crates/screen-core; possibly a shared readiness-state builder if the screen and live-view cores continue to converge.
- dead_frontage_or_shims: lib.rs is a pure re-export shim over runtime and worker modules.
- proposed_fix_packets: Remove the crate-level re-export shim; extract one shared observed-profile helper with screen-core; keep worker/runtime gating explicit and separate.
- dry_score_0_to_100: 36
- score_breakdown:
  - owner_boundary_0_to_20: 15
  - dependency_direction_0_to_15: 6
  - shared_contract_schema_0_to_15: 3
  - duplicate_shape_control_0_to_15: 5
  - direct_import_vs_event_boundary_0_to_15: 3
  - decoupling_and_common_core_0_to_10: 2
  - proof_logging_alignment_0_to_10: 2
- first_fix_roi: High: the public re-export shim is easy to remove and the shared decision shape is already obvious.
- severity: medium
- confidence: high
- evidence_paths: crates/screen-live-view-core/Cargo.toml; crates/screen-live-view-core/src/lib.rs; crates/screen-live-view-core/src/live_view_runtime.rs; crates/screen-live-view-core/src/live_view_worker.rs; crates/screen-core/src/lib.rs

## Current Refresh Audit - 2026-06-19
- responsibility: Live-view runtime readiness, worker startup gating, and child-domain observation/event helpers for live-view flows.
- dependencies: Narrow and appropriate on `ocentra-parent-agent-protocol`; no broad runtime dependency spread, and the current shape stays close to the protocol boundary.
- boundary_violations: `crates/screen-live-view-core/src/lib.rs` is still a pure re-export shim; the crate also overlaps with `crates/screen-core` on observed-profile and readiness layering instead of keeping a single shared owner.
- duplicated_shapes: `screen_live_view_observed_profile` mirrors `screen_observed_profile` from `crates/screen-core`; runtime and worker readiness both repeat the same decision-record pattern.
- barrel_reexport_shim_debt: High enough to matter because `lib.rs` hides the real module split and makes the crate appear broader than it is.
- schema_drift: Medium; startup/runtime/session/worker naming is close enough to drift conceptually even if the underlying schema contracts are currently aligned.
- event_log_proof_misuse: No direct misuse observed here; the file stays schema/proof-oriented and does not try to own runtime logging, which should remain with callers.
- test_proof_structure_issues: The live-view readiness shape is split across runtime and worker modules, so the proof surface is still small but slightly duplicated instead of having one obvious canonical readiness owner.
- dry_score_0_to_100: 38
- dry_score_reasons: Narrow protocol dependency and clear boundary ownership keep the score low, but the crate still carries a pure re-export shim plus a near-duplicate shared-profile/readiness shape with screen-core.
- fix_recommendation: Remove the crate-level shim, then isolate the shared observed-profile helper so live-view runtime and worker gates stay explicit and thin.
- decouple_recommendation: Keep live-view startup gating separate from shared screen observation/profile helpers so screen-core can own the common shape without live-view frontage.
- blockers: Audit-only refresh; downstream consumer impact was not re-traced in this pass, so any fix plan should re-check callers before moving the shim or changing names.
- exact_likely_paths: `crates/screen-live-view-core/Cargo.toml`; `crates/screen-live-view-core/src/lib.rs`; `crates/screen-live-view-core/src/live_view_runtime.rs`; `crates/screen-live-view-core/src/live_view_worker.rs`; `crates/screen-core/src/lib.rs`

## Current Refresh Audit - 2026-06-19
- responsibility: Live-view runtime readiness, worker startup gating, and child-domain observation/event helpers for live-view flows.
- dependencies: Narrow and appropriate on `ocentra-parent-agent-protocol`; the crate stays close to the protocol boundary and does not pull in broad runtime layers.
- violations: `crates/screen-live-view-core/src/lib.rs` is still a pure re-export shim, and the crate overlaps with `crates/screen-core` on observed-profile and readiness layering instead of leaving one shared owner.
- duplicated_shapes: `screen_live_view_observed_profile` mirrors `screen_observed_profile` from `crates/screen-core`; runtime and worker readiness both repeat the same decision-record pattern.
- barrel_reexport_shim_debt: High enough to matter because `lib.rs` hides the real module split and makes the crate appear broader than it is.
- schema_drift: Medium; startup/runtime/session/worker naming is close enough to drift conceptually even if the underlying schema contracts are currently aligned.
- event_log_proof_misuse: No direct misuse observed here; the file stays schema/proof-oriented and leaves runtime logging and redaction to callers.
- test_proof_structure_issues: The live-view readiness shape is split across runtime and worker modules, so the proof surface is small but still duplicated instead of having one canonical readiness owner.
- dry_score_0_to_100: 38
- dry_score_reasons: Narrow protocol dependency and clear boundary ownership keep the score low, but the crate still carries a pure re-export shim plus a near-duplicate shared-profile/readiness shape with screen-core.
- fix_recommendation: Remove the crate-level shim, then isolate the shared observed-profile helper so live-view runtime and worker gates stay explicit and thin.
- decouple_recommendation: Keep live-view startup gating separate from shared screen observation/profile helpers so screen-core can own the common shape without live-view frontage.
- blockers: Audit-only refresh; downstream consumer impact was not re-traced in this pass, so any fix plan should re-check callers before moving the shim or changing names.
- exact_likely_paths: `crates/screen-live-view-core/Cargo.toml`; `crates/screen-live-view-core/src/lib.rs`; `crates/screen-live-view-core/src/live_view_runtime.rs`; `crates/screen-live-view-core/src/live_view_worker.rs`; `crates/screen-core/src/lib.rs`
- test_proof_structure_issues: The crate boundary is still shaped more like a facade than a proof-friendly ownership boundary, so tests/proofs can end up duplicated around runtime and worker paths instead of converging on one shared helper.
- dry_score_0_to_100: 36
- dry_reasons: The score stays low because the crate still carries a re-export shim, mirrors `screen-core` naming and decision shapes, and keeps worker/runtime gating split across near-duplicate code paths instead of one shared core.
- fix_recommendation: Remove the crate-level re-export shim first, then extract the shared observed-profile/readiness helper where both screen cores can consume it without changing the public surface.
- decouple_recommendation: Keep live-view runtime gating and worker startup gating explicit and separate after the shared helper is extracted; do not collapse them into one hidden facade.
- blockers: No source blocker for the audit refresh itself. The only practical blocker to structural cleanup is the need to preserve existing protocol/public shape while moving shared logic out of the shim.
- exact_likely_paths: `crates/screen-live-view-core/src/lib.rs`; `crates/screen-live-view-core/src/live_view_runtime.rs`; `crates/screen-live-view-core/src/live_view_worker.rs`; `crates/screen-core/src/lib.rs`; likely a future shared helper path beside the screen-core observed-profile code.
