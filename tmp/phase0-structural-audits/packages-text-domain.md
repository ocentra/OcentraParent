# packages/text-domain
- target_kind: ts package, text token contract package
- owned_paths: packages/text-domain/package.json; packages/text-domain/README.md; packages/text-domain/tsconfig.json; packages/text-domain/src/package-info.ts; packages/text-domain/src/contracts.ts; packages/text-domain/src/browser-child-ux.ts; packages/text-domain/src/browser-parent-explanation.ts; packages/text-domain/src/app-game-child-ux-text.ts; packages/text-domain/src/portal-product-text.ts; packages/text-domain/src/portal-product-text-tokens.ts; packages/text-domain/src/portal-dev.ts; packages/text-domain/src/portal-dev-tracking-report-text.ts; packages/text-domain/src/portal-dev-tracking-unsupported-text.ts; packages/text-domain/src/social-dashboard-ux-text.ts; packages/text-domain/src/social-child-approval-block-ux-text.ts; packages/text-domain/tests/unit
- declared_responsibility: schema-backed display text tokens and shared copy values
- observed_responsibility: multiple token catalogs and text maps for browser, app-game, portal product, portal dev, and social surfaces, all backed by display-text decoding helpers
- should_own: reusable contract-visible copy tokens; text maps that need stable ids; portal/dev/browser/app-game surface copy that should not live as loose app strings
- should_not_own: arbitrary documentation prose; policy ids; route ids; event names; local ad hoc UI strings that do not need contract visibility
- allowed_dependencies: @ocentra-parent/schema-domain
- suspicious_dependencies: none visible in the manifest, but the package is a large umbrella for several unrelated text families and seems to absorb many portal-dev surface strings
- expected_dependents: portal UI, browser UI, app-game UI, dev tooling, and any runtime or test that needs stable display-text tokens
- shared_contract_schema_usage: strong use of `decodeTextTokenId` and `decodeDisplayText`, with token-to-text maps as the core contract pattern
- duplicate_or_near_duplicate_shapes: `BrowserParentExplanationText`, `BrowserChildUxText`, `AppGameChildUxText`, `PortalProductText`, `PortalDevTrackingReportText`, and `PortalDevTrackingUnsupportedText` all repeat the same token-map pattern; `PortalDevText` is a very large composition of those maps plus many extra tokens
- id_name_status_drift: high; the token namespaces mix browser, app-game, portal product, portal dev, tracking, and social families in one package, so the naming surface is broad and easy to drift
- direct_import_vs_event_boundary: direct imports are fine because this is a contract package, but the package is carrying too many unrelated text owners
- event_bus_usage: none; this is a static token package
- logging_and_proof_chain_expectations: no runtime logging needed for pure token maps, but any runtime consumer should emit proof-chain logs when text tokens are used to prove command/event boundaries
- boundary_violations: none obvious at the schema level, but the package is an umbrella over many unrelated copy domains and could hide ownership drift
- dry_common_core_candidates: a small internal token-map builder; shared resolve/fallback helper; subfamily modules for browser, app-game, portal product, portal dev, and social copy
- dead_frontage_or_shims: `PortalProductTextToken` and `PortalDevTextToken` are large frontage maps that mostly aggregate other token families; the package currently acts as a monolithic text shim
- proposed_fix_packets: split by text family and keep a tiny shared contract helper; keep portal-dev aggregation only if it remains the explicit owner of composed UI copy
- dry_score_0_to_100: 58
- score_breakdown:
  - owner_boundary_0_to_20: 11
  - dependency_direction_0_to_15: 14
  - shared_contract_schema_0_to_15: 11
  - duplicate_shape_control_0_to_15: 6
  - direct_import_vs_event_boundary_0_to_15: 13
  - decoupling_and_common_core_0_to_10: 2
  - proof_logging_alignment_0_to_10: 1
- first_fix_roi: medium; split the largest token families before adding more UI copy
- severity: medium
- confidence: high
- evidence_paths: packages/text-domain/package.json; packages/text-domain/README.md; packages/text-domain/src/contracts.ts; packages/text-domain/src/browser-child-ux.ts; packages/text-domain/src/browser-parent-explanation.ts; packages/text-domain/src/app-game-child-ux-text.ts; packages/text-domain/src/portal-product-text.ts; packages/text-domain/src/portal-product-text-tokens.ts; packages/text-domain/src/portal-dev.ts; packages/text-domain/src/portal-dev-tracking-report-text.ts; packages/text-domain/src/portal-dev-tracking-unsupported-text.ts; packages/text-domain/src/social-dashboard-ux-text.ts; packages/text-domain/src/social-child-approval-block-ux-text.ts

## Current Refresh Audit - 2026-06-19
- responsibility: schema-backed display text tokens and shared copy values for browser, app-game, portal product, portal dev, and social surfaces; the package also owns token-id decoding and text decoding helpers in `packages/text-domain/src/contracts.ts`
- dependencies: only `@ocentra-parent/schema-domain` is declared in `packages/text-domain/package.json`; runtime text maps depend on `decodeTextTokenId` and `decodeDisplayText` from `packages/text-domain/src/contracts.ts`
- boundary_violations: no obvious schema boundary break at the helper layer, but the package is still an umbrella for several unrelated text families, which weakens ownership clarity in `packages/text-domain/src/portal-dev.ts` and the family-specific modules
- duplicated_shapes: `packages/text-domain/src/browser-child-ux.ts`, `packages/text-domain/src/browser-parent-explanation.ts`, `packages/text-domain/src/app-game-child-ux-text.ts`, `packages/text-domain/src/social-dashboard-ux-text.ts`, and `packages/text-domain/src/social-child-approval-block-ux-text.ts` all repeat the same token-object plus `Record<token, DisplayText>` pattern; `packages/text-domain/src/portal-dev.ts` composes many of those patterns into one very large aggregate
- barrel_reexport_shim_debt: no forbidden TS/JS barrel or external re-export pattern was found in the package sources; the only export-site note is the local named export `export { PortalProductTextToken };` in `packages/text-domain/src/portal-product-text.ts`, which is not a hard-gate violation. The package does have shim-like aggregation debt because `packages/text-domain/src/portal-dev.ts` pulls in `PortalProductTextToken`, `PortalDevTrackingReportTextToken`, and `PortalDevTrackingUnsupportedTextToken` and then layers a very large surface on top
- schema_drift: high risk from naming breadth rather than runtime schema failure; token namespaces mix browser, app-game, portal product, portal dev, tracking, and social concepts in one package, and `packages/text-domain/src/portal-dev.ts` is the main drift sink
- event_log_proof_misuse: none observed in the package itself because it is a static token package, not a runtime event/log producer; the only proof-adjacent pressure is in token names like `TrackingReport...`, `TrackingUnsupported...`, and `TrackingRetention...`, which should stay as copy contracts and not be mistaken for proof emission
- test_proof_structure_issues: test coverage is unit-only and shape-focused in `packages/text-domain/tests/unit/*.test.ts`; the tests assert token decode behavior and some banned-fragment checks, but there are no package-level contract, integration, or boundary tests that would catch future ownership drift or accidental token-family merging
- dry_score_0_to_100: 56
- reasons: the package has a good typed contract core and no obvious dependency inversion problem, but it is still a broad umbrella with repeated token-map scaffolding, a very large `portal-dev` aggregation module, and only light structural test coverage for ownership boundaries
- fix_recommendation: split the largest token families out of `packages/text-domain/src/portal-dev.ts` into smaller text-family modules with a tiny shared contract helper, while keeping `packages/text-domain/src/contracts.ts` as the only common decoding surface
- decouple_recommendation: isolate portal-dev tracking/report/unsupported families first, then separate browser/app-game/social families from the portal product token set so each module reflects one owner and one UI surface
- blockers: none for refresh/audit work; the only constraint is that this package already contains multiple established families, so any follow-up fix pass will need deliberate migration ordering
- exact_likely_paths: `packages/text-domain/src/portal-dev.ts`; `packages/text-domain/src/portal-product-text.ts`; `packages/text-domain/src/portal-product-text-tokens.ts`; `packages/text-domain/src/portal-dev-tracking-report-text.ts`; `packages/text-domain/src/portal-dev-tracking-unsupported-text.ts`; `packages/text-domain/src/browser-child-ux.ts`; `packages/text-domain/src/browser-parent-explanation.ts`; `packages/text-domain/src/app-game-child-ux-text.ts`; `packages/text-domain/src/social-dashboard-ux-text.ts`; `packages/text-domain/src/social-child-approval-block-ux-text.ts`
- no_barrels_no_reexports_gate: currently not violated by the package sources I inspected; exact likely path if this changes would be `packages/text-domain/src/portal-product-text.ts`, but the present `export { PortalProductTextToken };` is local and allowed, not a forbidden re-export

## Current Refresh Audit - 2026-06-19
- responsibility: shared schema-backed display text tokens and decode helpers for browser, app-game, portal product, portal dev, and social surfaces; `packages/text-domain/src/contracts.ts` is the common decoding core, while the family modules own their own token and text maps
- dependencies: declared dependency surface is only `@ocentra-parent/schema-domain` in `packages/text-domain/package.json`; the real shared runtime dependency inside the package is the local `decodeTextTokenId` / `decodeDisplayText` helper pair from `packages/text-domain/src/contracts.ts`
- violations: no hard schema or dependency-direction violation is visible in the inspected files; the structural problem is ownership breadth, not a broken import cycle or runtime contract breach
- duplicated_shapes: the package repeats the same token-object plus `Record<token, DisplayText>` pattern in `packages/text-domain/src/browser-child-ux.ts`, `packages/text-domain/src/browser-parent-explanation.ts`, `packages/text-domain/src/app-game-child-ux-text.ts`, `packages/text-domain/src/social-dashboard-ux-text.ts`, and `packages/text-domain/src/social-child-approval-block-ux-text.ts`; `packages/text-domain/src/portal-dev.ts` stretches that same shape into a 946-line aggregate surface
- barrel_reexport_shim_debt: no forbidden TypeScript/JavaScript barrel or cross-file re-export was found in the inspected package sources; the debt is shim-like aggregation, especially `packages/text-domain/src/portal-dev.ts` importing and composing `PortalProductTextToken`, `PortalDevTrackingReportTextToken`, and `PortalDevTrackingUnsupportedTextToken` into one umbrella module
- schema_drift: high naming drift risk because one package owns browser, app-game, portal product, portal dev, tracking, and social token namespaces at once; the module names are still specific, but the owner boundary is broader than the package name suggests
- event_log_proof_misuse: none observed in-package because this is a static text-token package; the only proof-adjacent risk is semantic drift where copy names like `TrackingReport...`, `TrackingUnsupported...`, and `TrackingRetention...` can start to imply proof or runtime claims if they keep accumulating in `packages/text-domain/src/portal-dev.ts`
- test_proof_structure_issues: coverage is unit-only and shape-focused in `packages/text-domain/tests/unit/*.test.ts`; the tests assert decode behavior and a few banned-fragment checks, but they do not enforce ownership seams, family boundaries, or migration partitions between the text groups
- dry_score_0_to_100: 56
- score_reasons: good typed decode core and no obvious dependency inversion problem; medium duplication from repeated token-map scaffolding; high umbrella breadth in `packages/text-domain/src/portal-dev.ts`; only light structural test coverage for owner boundaries; no hard barrel violation, but enough shim-like aggregation to keep the package from being close to a clean shared-core split
- fix_recommendation: keep `packages/text-domain/src/contracts.ts` as the tiny common core, then split the biggest text families out of `packages/text-domain/src/portal-dev.ts` so each module reflects one owner and one surface
- decouple_recommendation: peel off `portal-dev` tracking/report/unsupported text first, then separate browser, app-game, and social families from portal product text so future work can move one family at a time without widening the package again
- blockers: none for the audit refresh itself; follow-up work will need migration ordering because the package already has many established consumers and the aggregate `portal-dev` surface is carrying several families at once
- exact_likely_paths: `packages/text-domain/src/contracts.ts`; `packages/text-domain/src/browser-child-ux.ts`; `packages/text-domain/src/browser-parent-explanation.ts`; `packages/text-domain/src/app-game-child-ux-text.ts`; `packages/text-domain/src/portal-product-text.ts`; `packages/text-domain/src/portal-product-text-tokens.ts`; `packages/text-domain/src/portal-dev.ts`; `packages/text-domain/src/portal-dev-tracking-report-text.ts`; `packages/text-domain/src/portal-dev-tracking-unsupported-text.ts`; `packages/text-domain/src/social-dashboard-ux-text.ts`; `packages/text-domain/src/social-child-approval-block-ux-text.ts`; `packages/text-domain/tests/unit/browser-child-ux.test.ts`; `packages/text-domain/tests/unit/browser-parent-explanation.test.ts`; `packages/text-domain/tests/unit/app-game-child-ux-text.test.ts`; `packages/text-domain/tests/unit/package-info.test.ts`; `packages/text-domain/tests/unit/portal-dev.test.ts`; `packages/text-domain/tests/unit/social-dashboard-ux-text.test.ts`; `packages/text-domain/tests/unit/social-child-approval-block-ux-text.test.ts`
