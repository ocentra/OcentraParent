# packages/lan-domain
- target_kind: TypeScript package
- owned_paths: `packages/lan-domain/**`
- declared_responsibility: Household LAN discovery, pairing, relay, and local device-spine contracts.
- observed_responsibility: Owns a very broad LAN contract surface, including pairing values, browser/runtime requests, proof read models, device spine data, discovery matrices, and several v0.9 proof surfaces.
- should_own: LAN-specific shared contracts, schema-backed read models, route/proof gate states, and the LAN-facing contract vocabulary used by consumers.
- should_not_own: A package-wide frontage barrel, duplicated proof-row shapes that belong in a smaller common core, or unrelated product-domain meaning.
- allowed_dependencies: `@ocentra-parent/capability-domain`, `@ocentra-parent/event-domain`, `@ocentra-parent/family-domain`, `@ocentra-parent/schema-domain`.
- suspicious_dependencies: The package depends on a large amount of `family-domain` reference material and `event-domain` only for a schema filter, while `src/lan-pairing.ts` aggregates the entire package into a single export surface.
- expected_dependents: Portal/runtime consumers, LAN proof/test code, and any future Rust bridge or protocol consumers that import these contracts.
- shared_contract_schema_usage: Heavy Effect Schema usage throughout, plus shared family references and an event-taxonomy filter against `EventingEventTypeSchema`.
- duplicate_or_near_duplicate_shapes: Many files repeat the same row shape families: `schemaVersion`, `proofState`, `runtimeOwner`, `evidenceLabel`, `requiredArtifactSummary`, `routeId`, `trustState`, and `discoveryState` appear in several proof/read-model files with only small variations.
- id_name_status_drift: `package.json` exports `./*` and `./package-info`, which is much wider than the package’s real ownership story; `tests/README.md` also states only `tests/unit` is real, so the package surface is broader than the currently proven test surface.
- direct_import_vs_event_boundary: Contract-to-contract imports are fine, but the barrel file erases ownership boundaries and makes consumers depend on aggregation instead of the underlying contract modules.
- event_bus_usage: Only light event-taxonomy validation is present; the package is mostly contract/schema rather than runtime event-bus code.
- logging_and_proof_chain_expectations: Most source files are pure schemas or pure contract helpers, so logger-ready expectations are low; the proof/read-model files are still contract surfaces and should stay explicit about claims rather than runtime logging.
- boundary_violations: `src/lan-pairing.ts` is a large re-export barrel, and `package.json` exposes a wildcard subpath surface that encourages frontage-style imports instead of narrower owners.
- dry_common_core_candidates: Split repeated proof-row bases into smaller shared schema helpers, factor repeated `manual-required` / `not-implemented` proof-state patterns, and consider separating v0.9 proof artifacts from the main LAN contract core.
- dead_frontage_or_shims: `src/lan-pairing.ts` is mostly a frontage shim; `package.json` wildcard exports reinforce that shim behavior.
- proposed_fix_packets: Replace the barrel with explicit subpath exports, extract common proof-row base schemas, and decide whether the v0.9 proof files belong in a separate proof-owner package or a clearly named submodule.
- severity: high
- confidence: high
- evidence_paths: `packages/lan-domain/package.json`, `packages/lan-domain/src/lan-pairing.ts`, `packages/lan-domain/src/lan-pairing-product-proof.ts`, `packages/lan-domain/src/lan-signed-discovery-relay-spine.ts`, `packages/lan-domain/src/lan-pairing-device.ts`, `packages/lan-domain/tests/README.md`

## Current Refresh Audit - 2026-06-19
- responsibility: Owns LAN discovery, pairing, relay, and local device-spine contracts. The current surface still stretches across browser/runtime-facing read models, proof summaries, discovery matrices, and v0.9 proof artifacts, so the package is broader than a narrow LAN core.
- deps: Uses `schema-domain` heavily, plus `family-domain`, `capability-domain`, and `event-domain`. The main risk is not dependency count alone but the amount of family reference plumbing and the event-domain filter being used only for schema classification.
- violations: `src/lan-pairing.ts` still acts as a package-wide frontage barrel, while `package.json` exports `./*` and `./package-info`. That combination keeps the package looking like a generic aggregation point instead of a bounded contract owner.
- duplicated shapes: Repeated row families still show up across `lan-pairing-product-proof.ts`, `lan-production-household-proof.ts`, `lan-signed-discovery-relay-spine.ts`, `lan-discovery-source-matrix.ts`, and `lan-pairing-device.ts`. The recurring fields are `schemaVersion`, `proofState`, `runtimeOwner`, `evidenceLabel`, `requiredArtifactSummary`, `routeId`, `trustState`, `discoveryState`, `claimsProved`, and `claimsNotProved`.
- barrel/reexport/shim debt: `src/lan-pairing.ts` remains the only obvious package frontage shim, and the wildcard subpath export in `package.json` keeps widening the same shim behavior instead of collapsing it.
- schema drift: The package export surface is wider than the currently proven test surface. `tests/README.md` still says only `tests/unit` is real coverage, while the package exposes every `src/*` leaf and keeps v0.9 proof surfaces beside current LAN contracts.
- event/log/proof misuse: This package is mostly contract/schema work, not runtime event handling. That is fine, but the proof helpers should stay explicit about claim boundaries rather than implying runtime logging, event delivery, or operational proof they do not own.
- test/proof structure issues: Coverage is still unit-only, with placeholder categories in `tests/**` not counting as real coverage. Proof and read-model shapes are spread across many files, so the package lacks a smaller shared core for repeated row bases and proof-state families.
- score: 4/10.
- fix recommendation: Replace the package-wide barrel with explicit subpath entrypoints, narrow `package.json` exports, extract shared row-base schemas for the repeated proof/read-model shapes, and separate the v0.9 proof artifacts from the core LAN contract surface if they remain active.
- decouple recommendation: Split the proof-heavy LAN surface into smaller owners for core pairing/discovery, production household proof, signed relay spine, and v0.9 history/proof staging so consumers stop depending on one aggregated frontage file.
- blockers: No immediate runtime blocker for the refresh note itself. The real blocker for a fix phase is deciding which proof artifacts still belong in `packages/lan-domain` versus a follow-on proof-owner package or submodule.
- exact likely paths: `packages/lan-domain/package.json`, `packages/lan-domain/src/lan-pairing.ts`, `packages/lan-domain/src/lan-pairing-product-proof.ts`, `packages/lan-domain/src/lan-production-household-proof.ts`, `packages/lan-domain/src/lan-signed-discovery-relay-spine.ts`, `packages/lan-domain/src/lan-discovery-source-matrix.ts`, `packages/lan-domain/src/lan-pairing-device.ts`, `packages/lan-domain/src/v0-9-production-discovery-household-proof.ts`, `packages/lan-domain/src/v0-9-household-lan-pairing-proof.ts`, `packages/lan-domain/tests/README.md`

## Current Refresh Audit - 2026-06-19
- responsibility: `packages/lan-domain` still owns the LAN discovery, pairing, relay, and local device-spine contract boundary. In practice the package now also carries browser/runtime read models, household proof summaries, source matrices, device role read models, and historical v0.9 proof artifacts, so the ownership story is broader than a narrow LAN contract core.
- dependencies: The package correctly leans on `schema-domain` for schema-backed contracts and on `family-domain` for shared reference primitives. `event-domain` is used only for the audit taxonomy filter in `lan-pairing-values.ts`, which is acceptable but shows this package is still mostly contract metadata rather than runtime behavior.
- violations: `packages/lan-domain/package.json` exposes `./*`, which turns the package into a broad subpath surface instead of a curated contract boundary. `packages/lan-domain/src/lan-pairing.ts` is still a package-wide frontage barrel that pulls together multiple ownership lines behind one import path.
- duplicated shapes: Repeated row and evidence families still appear across `lan-pairing-product-proof.ts`, `lan-production-household-proof.ts`, `lan-signed-discovery-relay-spine.ts`, `lan-discovery-source-matrix.ts`, `lan-pairing-device.ts`, `household-device-spine.ts`, and `device-roles.ts`. The recurring fields are `schemaVersion`, `generatedAt`, `checkedAt`, `proofState`, `runtimeOwner`, `evidenceLabel`, `requiredArtifactSummary`, `routeId`, `trustState`, `discoveryState`, `claimsProved`, and `claimsNotProved`.
- barrel/re-export/shim debt: `src/lan-pairing.ts` remains the clearest shim, and `package.json` widens it with a wildcard subpath export. Together they encourage consumers to import from the package frontage instead of the actual owner modules.
- schema drift: The declared export surface is wider than the proven coverage story. `tests/README.md` still frames only `tests/unit` as real coverage, while the package exports all `src/*` leaves and keeps v0.9 proof artifacts adjacent to active LAN contracts.
- event/log/proof misuse: The source is mostly schema and proof modeling, not event processing or runtime logging. The main misuse risk is conceptual: proof rows and read models can read like operational guarantees unless the files stay explicit that they are contract claims, not emitted runtime evidence.
- test/proof structure issues: The package relies on unit coverage and schema filters, but the proof/read-model surfaces are spread across many files with repeated base fields. That makes the package hard to verify as one coherent proof owner and suggests the need for a smaller shared core plus narrower feature-specific owners.
- dry score: 36/100. The score is held down by the wildcard export surface, the package-wide barrel, repeated proof-row scaffolding, and the fact that historical proof surfaces still live beside current contracts. It is not lower because the schemas are typed, explicit, and mostly consistent with the LAN ownership story.
- fix recommendation: Narrow `package.json` exports, remove or shrink `lan-pairing.ts` into explicit owner entrypoints, and extract shared row-base schemas for the repeated proof/read-model families before adding any new LAN contract rows.
- decouple recommendation: Split the LAN contract surface into core pairing/discovery, production household proof, signed relay spine, browser add-device/runtime support, and v0.9 history/proof staging so each area has a smaller, clearer owner.
- blockers: No blocker for the refresh note itself. A real fix pass is blocked only by ownership decisions on the v0.9 proof artifacts and whether the package should keep acting as an aggregation point or become a set of narrower submodules.
- exact likely paths: `packages/lan-domain/package.json`, `packages/lan-domain/src/lan-pairing.ts`, `packages/lan-domain/src/lan-pairing-values.ts`, `packages/lan-domain/src/lan-pairing-product-proof.ts`, `packages/lan-domain/src/lan-production-household-proof.ts`, `packages/lan-domain/src/lan-signed-discovery-relay-spine.ts`, `packages/lan-domain/src/lan-discovery-source-matrix.ts`, `packages/lan-domain/src/lan-pairing-device.ts`, `packages/lan-domain/src/lan-pairing-support.ts`, `packages/lan-domain/src/household-device-spine.ts`, `packages/lan-domain/src/device-roles.ts`, `packages/lan-domain/src/lan-device-parent-actions.ts`, `packages/lan-domain/src/package-info.ts`
