# packages/schema-domain
- target_kind: TypeScript shared Effect Schema helper package
- owned_paths: packages/schema-domain/package.json, packages/schema-domain/README.md, packages/schema-domain/src/**, packages/schema-domain/tests/**
- declared_responsibility: shared Effect Schema validation helpers, branded non-empty string helpers, and unknown-to-domain parsing
- observed_responsibility: a compact schema utility layer that centralizes parser wrappers, decode errors, safe parsing, and branded string creation
- should_own: generic schema helpers, branded decode helpers, safe parse wrappers, and decode error formatting
- should_not_own: product-specific policy, route, protocol, logging, or runtime behavior
- allowed_dependencies: `effect` only, plus the TypeScript toolchain
- suspicious_dependencies: none material beyond the intended Effect Schema dependency
- expected_dependents: every domain package that needs branded strings or schema parsing, especially event, logging, and other contract packages
- shared_contract_schema_usage: this is the correct shared schema boundary and it is already the common helper layer for parsing unknown input into domain types
- duplicate_or_near_duplicate_shapes: `SchemaDecodeError`, `safeParseUnknown`, `parseUnknown`, and `withParser` are the right small helper set, but they overlap conceptually with per-package decode helpers if those grow too large
- id_name_status_drift: none material; the package name matches its helper role well
- direct_import_vs_event_boundary: no event or runtime boundary issue is visible here, because the package stays at the schema-helper layer
- event_bus_usage: none
- logging_and_proof_chain_expectations: none beyond normal schema correctness, since this package should remain pure helper infrastructure
- boundary_violations: no clear boundary violation is visible in the current surface
- dry_common_core_candidates: schema decode helpers, branded non-empty strings, partial parsing, and shared decode error formatting
- dead_frontage_or_shims: `package-info.ts` is simple package-name frontage and does not look like a redundant shim
- proposed_fix_packets: keep the helper surface compact, and split by concern only if parser, branding, or error handling starts to grow into separate ownership areas
- severity: low
- confidence: high
- evidence_paths: packages/schema-domain/package.json, packages/schema-domain/README.md, packages/schema-domain/src/effect.ts, packages/schema-domain/src/package-info.ts

## Current Refresh Audit - 2026-06-19
- responsibility: shared Effect Schema helper boundary for branded non-empty strings, decode wrappers, and unknown-to-domain parsing
- deps: `effect` only; no material runtime, logging, or product-contract dependency pressure inside the package itself
- violations: no hard boundary break visible in-package; the main concern is helper creep around parser wrapping and error shaping rather than ownership misuse
- duplicated shapes: `SchemaDecodeError`, `SafeParseResult`, `safeParseUnknown`, `parseUnknown`, and `withParser` overlap conceptually with downstream decode helpers, so the boundary is compact but easy to duplicate elsewhere
- barrel/reexport/shim debt: `packages/schema-domain/src/effect.ts` re-exports `Schema` for convenience and `withParser` mutates schemas in place, which is a small shim layer rather than a pure utility module; `packages/schema-domain/src/package-info.ts` is only package-name frontage
- schema drift: the package is still aligned with its README, but `withParser` plus `SchemaDecodeError.format`/`flatten` create a small API surface that can drift from consumer expectations if error-path formatting or partial parsing semantics change
- event/log/proof misuse: none visible in this package; it correctly stays out of event, log, and proof responsibilities
- test/proof structure issues: coverage is only unit-level and checks happy decode, generic failure, parser attachment, and branded non-empty strings; there is no regression test for `SchemaDecodeError.format`, `flatten`, or partial-parser behavior
- score: 3/10, low severity but with concentrated shim and coverage debt
- fix recommendation: keep the package small, and add focused tests for decode-error formatting plus partial/parser semantics before expanding helper surface
- decouple recommendation: if downstream packages start needing custom decode behavior, move those package-specific helpers out of `schema-domain` and keep this package limited to generic parse/brand primitives
- blockers: none for the audit refresh itself; evidence was sufficient from `packages/schema-domain` plus direct consumers in `packages/endpoint-domain/src/types/brands.ts`, `packages/capability-domain/src/capabilities.ts`, and `packages/event-domain/src/primitives.ts`
- exact likely paths: `packages/schema-domain/src/effect.ts`, `packages/schema-domain/src/package-info.ts`, `packages/schema-domain/tests/unit/effect.test.ts`, `packages/schema-domain/tests/unit/package-info.test.ts`, `packages/schema-domain/README.md`, `packages/schema-domain/package.json`, `packages/endpoint-domain/src/types/brands.ts`, `packages/capability-domain/src/capabilities.ts`, `packages/event-domain/src/primitives.ts`

## Current Refresh Audit - 2026-06-19
- responsibility: shared Effect Schema helper boundary for branded non-empty strings, decode wrappers, and unknown-to-domain parsing
- dependencies: `effect` only; the package stays out of runtime, logging, event, and product-contract dependencies
- violations: no domain responsibility violation is visible, but `packages/schema-domain/src/effect.ts` does contain a convenience re-export of `Schema`, which is the one structural rule-risk in the surface
- duplicated shapes: `SafeParseResult`, `SchemaIssue`, `SchemaDecodeError`, `parseUnknown`, `safeParseUnknown`, and `withParser` form a compact helper cluster, but the same decode/format/partial concerns are repeated in a few different wrapper shapes
- barrel/re-export/shim debt: `export { Schema }` is a small re-export shim, and `withParser` mutates schema objects in place to attach methods instead of staying a pure helper function; `src/package-info.ts` is harmless package frontage, not a meaningful shim
- schema drift: the README still matches the code, but `SchemaDecodeError.format`, `flatten`, and `withParser` can drift from consumer expectations if downstream packages start depending on exact error-path or partial-parser semantics
- event/log/proof misuse: none visible; this package correctly avoids event, log, and proof ownership
- test/proof structure issues: coverage is unit-only and exercises decode success, generic failure, parser attachment, and branded strings, but there is no direct regression coverage for `SchemaDecodeError.format`, `flatten`, or `partial()` semantics
- dry score: 76/100; the package is focused and dependency-light, but helper overlap, the `Schema` re-export, and the mutable parser shim keep it from being cleaner
- fix recommendation: keep the package narrow, add focused tests for error formatting and partial-parser behavior, and avoid growing more wrapper variants unless they are clearly reusable across multiple domains
- decouple recommendation: if downstream packages need custom decode behavior, move that package-specific logic out of `schema-domain` and keep this package limited to generic schema primitives and branded helpers
- blockers: none for the audit refresh itself
- exact likely paths: `packages/schema-domain/src/effect.ts`, `packages/schema-domain/src/package-info.ts`, `packages/schema-domain/tests/unit/effect.test.ts`, `packages/schema-domain/tests/unit/package-info.test.ts`, `packages/schema-domain/README.md`, `packages/schema-domain/package.json`
