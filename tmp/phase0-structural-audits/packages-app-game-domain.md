# packages/app-game-domain
- target_kind: TypeScript contract and schema package for app/game-related domain shapes.
- owned_paths: `packages/app-game-domain/package.json`, `packages/app-game-domain/src/**/*.ts`, `packages/app-game-domain/tests/unit/**/*.test.ts`
- declared_responsibility: provide typed schemas, branded primitives, read models, policy/authority/category/risk contracts, and proof or handoff shapes for app-game surfaces.
- observed_responsibility: acts as a broad contract aggregator spanning app-game primitives, category/risk, authority, child-facing UX, install-store handoff, runtime evidence, budgets, and policy-preview/read-model shapes.
- observed_responsibility: acts as a broad contract aggregator spanning app-game primitives, category/risk, authority, child-facing UX, install-store handoff, runtime evidence, budgets, and policy-preview/read-model shapes.
- should_own: app-game-specific schemas, branded primitives, validation rules, evidence-backed handoff/read-model contracts, and other pure data contracts that remain domain-owned.
- should_not_own: runtime execution, UI rendering, actual enforcement, generic app-control or game-control engines outside app/game scope, or a barrel that obscures module ownership.
- allowed_dependencies: local sibling modules under `src`, `@ocentra-parent/schema-domain/effect`, `@ocentra-parent/evidence-domain/*`, `@ocentra-parent/family-domain/*`, `@ocentra-parent/enforcement-domain/*`, `@ocentra-parent/policy-domain/policy`, and the other domain packages declared in `package.json`.
- suspicious_dependencies: the package is intentionally broad, but the wildcard export in `package.json` and the `src/app-game.ts` barrel flatten ownership; `app-game-control-authority.ts` and related modules also behave like thin frontage over sibling modules.
- expected_dependents: app/game runtime packages, portal policy builders, proof/test suites, classification and authority consumers, installer/purchase workflows, and other domain packages that encode or decode these contracts.
- shared_contract_schema_usage: heavy use of `Schema.Struct`, `withParser`, branded string schemas, `ActivityEvidenceRefSchema`, family reference primitives, `ParentContractSchemaVersion`, `AppGameSchemaVersion`, and repeated evidence-backed schema filters across modules.
- duplicate_or_near_duplicate_shapes: `app-game-category-risk.ts` and `app-riskdetection.ts` model very similar candidate/source/risk tuples; `app-game-control-authority.ts` and `app-game-control-platform-authority.ts` split one authority concept into multiple layers; `app-game-child-facing-ux.ts`, `app-game-install-store-handoff.ts`, and `native-game-budget-policy.ts` repeat the same evidence-backed state + validation pattern; `app-control-catalog.ts` and `game-control-catalog.ts` are parallel catalog builders with near-identical traversal/validation logic in adjacent domains.
- id_name_status_drift: strong drift risk. The package name says `app-game-domain`, but the source surface spans `app-control-*`, `app-riskdetection`, `app-game-control-*`, `app-install-purchase-*`, `native-game-*`, and many `game-*` variants; the `app-game.ts` barrel hides which subdomain owns each shape.
- direct_import_vs_event_boundary: mostly direct schema imports, which is appropriate for a contract package, but the barrel export chain and wildcard package export flatten boundaries and make shims look like owners.
- event_bus_usage: none observed; this package is schema/contract-only, with evidence and handoff semantics encoded in fields and validation rules rather than runtime dispatch.
- logging_and_proof_chain_expectations: no runtime logging is expected here; proof-chain expectations are expressed through evidence refs, proof-pack refs, and schema filters. Callers that execute workflows should own the logger/proof milestones.
- boundary_violations: `src/app-game.ts` is a barrel built from `export *` re-exports and violates the repo no-reexports rule; `src/app-game-category-risk.ts`, `src/app-game-control-authority.ts`, and `src/app-game-install-store-handoff.ts` also expose frontage-like re-export behavior; the wildcard `./*` export in `package.json` amplifies the boundary blur.
- dry_common_core_candidates: consolidate mirrored risk/category shapes, shared authority/approval matrix shapes, and repeated evidence-backed handoff schema patterns into smaller canonical modules with one owner each.
- dead_frontage_or_shims: `src/package-info.ts` is a pure identity shim; `src/app-game.ts` is a facade barrel; `src/app-game-category-risk.ts` and `src/app-game-control-authority.ts` are partly facade-like because they mostly forward sibling modules.
- proposed_fix_packets: split the facade barrel into explicit entry modules, remove `export *` chains, narrow the wildcard package export, and extract shared contract builders for the repeated evidence-backed schema families so each subdomain owns one canonical shape.
- severity: high
- confidence: high
- evidence_paths: `packages/app-game-domain/package.json`, `packages/app-game-domain/src/package-info.ts`, `packages/app-game-domain/src/app-game.ts`, `packages/app-game-domain/src/app-game-category-risk.ts`, `packages/app-game-domain/src/app-riskdetection.ts`, `packages/app-game-domain/src/app-game-control-authority.ts`, `packages/app-game-domain/src/app-game-control-platform-authority.ts`, `packages/app-game-domain/src/app-game-child-facing-ux.ts`, `packages/app-game-domain/src/app-game-install-store-handoff.ts`, `packages/app-game-domain/src/app-control-catalog.ts`, `packages/app-game-domain/src/game-control-catalog.ts`

## Current Refresh Audit - 2026-06-19

- responsibility: own app-game contract schemas, branded primitives, read models, and evidence-backed handoff/proof shapes for app/game surfaces; avoid runtime execution, UI rendering, and generic control engines.
- dependencies: `@ocentra-parent/schema-domain/effect`, `@ocentra-parent/evidence-domain/*`, `@ocentra-parent/family-domain/*`, `@ocentra-parent/enforcement-domain/*`, `@ocentra-parent/policy-domain/policy`, plus local sibling modules under `src`.
- violations: `packages/app-game-domain/package.json` uses a wildcard `./*` export that flattens module ownership; `packages/app-game-domain/src/app-game.ts` is a repo-banned `export *` barrel; `packages/app-game-domain/src/package-info.ts` is a pure identity shim that adds no boundary value.
- duplicated shapes: `app-game-category-risk.ts` and `app-riskdetection.ts` carry similar candidate/source/risk tuples; `app-game-control-authority.ts` and `app-game-control-platform-authority.ts` split one authority concept across adjacent layers; `app-control-catalog.ts` and `game-control-catalog.ts` mirror catalog-building traversal and validation logic; the `app-install-purchase-*` and `app-game-child-facing-ux*` families repeat evidence-backed handoff patterns.
- barrel/re-export/shim debt: `src/app-game.ts` centralizes many `export *` re-exports and hides ownership; the wildcard package export in `package.json` mirrors that blur; `src/package-info.ts` is a shim that only names the package; `app-game-category-risk.ts` and `app-game-control-authority.ts` act like frontage modules over nearby contracts instead of distinct canonical owners.
- schema drift: package naming says `app-game-domain`, but the source surface now spans `app-control-*`, `app-riskdetection`, `app-game-control-*`, `app-install-purchase-*`, `native-game-*`, and `game-*` namespaces; authority and catalog semantics are parallelized instead of normalized, so drift risk is high even where validation still passes.
- event/log/proof misuse: no runtime event or log misuse is evident in this contract-only slice; the issue is semantic overloading, where proof-like module names and evidence fields are used as if they were the owning proof artifact, while the actual proof responsibility belongs in the caller or test harness.
- test/proof structure issues: `tests/unit` is dominated by one-file-per-shape assertions and proof-style filenames, which makes it hard to separate canonical contract validation from path-specific proof coverage; `package-info.test.ts`, `app-game.test.ts`, `app-control-policy-catalog.test.ts`, and `game-control-policy-catalog.test.ts` are especially boundary/identity oriented rather than DRY-normalized.
- dry score 0-100: 24/100. The package is structurally broad, the barrel and wildcard export flatten ownership, several modules are thin frontage layers, and there are at least two obvious mirrored catalog families plus repeated evidence-backed handoff patterns. Some shared primitives exist, but they are not yet normalized into a smaller canonical surface.
- fix recommendation: split the barrel into explicit entry modules, narrow the wildcard export map, keep one canonical owner per schema family, and factor shared candidate/authority/catalog builders into smaller internal modules with named ownership.
- decouple recommendation: separate app-game core contracts from cross-cutting app-control and game-control catalog builders, then isolate install-purchase and child-facing UX handoff contracts so each concern has one stable module boundary.
- blockers: the repo-wide no-reexports rule blocks further barrel growth; the current wildcard export pattern makes ownership unclear for dependents; the catalog/authority duplication is wide enough that a safe refactor likely needs staged extraction rather than a single rename.
- exact likely paths: `packages/app-game-domain/package.json`, `packages/app-game-domain/src/app-game.ts`, `packages/app-game-domain/src/package-info.ts`, `packages/app-game-domain/src/app-game-category-risk.ts`, `packages/app-game-domain/src/app-riskdetection.ts`, `packages/app-game-domain/src/app-game-control-authority.ts`, `packages/app-game-domain/src/app-game-control-platform-authority.ts`, `packages/app-game-domain/src/app-game-install-store-handoff.ts`, `packages/app-game-domain/src/app-control-catalog.ts`, `packages/app-game-domain/src/game-control-catalog.ts`, `packages/app-game-domain/tests/unit/app-game.test.ts`, `packages/app-game-domain/tests/unit/app-control-policy-catalog.test.ts`, `packages/app-game-domain/tests/unit/game-control-policy-catalog.test.ts`, `packages/app-game-domain/tests/unit/package-info.test.ts`
