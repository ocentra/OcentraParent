# crates/entitlement-core
- target_kind: rust crate
- owned_paths: crates/entitlement-core/Cargo.toml; crates/entitlement-core/src/lib.rs; crates/entitlement-core/src/entitlement_access.rs; crates/entitlement-core/tests/unit.rs; crates/entitlement-core/tests/unit/capability_gate.rs; crates/entitlement-core/tests/unit/capability_access.rs
- declared_responsibility: entitlement and capability access boundary for the Rust side
- observed_responsibility: a small Rust gate around entitlement/access decisions, but the current file set suggests the crate is a thin wrapper with little distinct contract surface
- should_own: entitlement access contracts that are genuinely Rust-runtime specific; canonical entitlement-state decisions; any protocol-adjacent access gating that cannot live in a more general family/capability domain
- should_not_own: family household/session/recovery semantics; capability catalog status matrices; unrelated product policy or setup lifecycle logic
- allowed_dependencies: currently only local crate code is visible from the declared target; the crate manifest needs its own dependency rationale kept narrow
- suspicious_dependencies: name overlap with capability-domain concepts; likely overlap with family-domain household action matrices; no evidence of a richer boundary than a single access module
- expected_dependents: Rust services or adapters that need entitlement gating, plus tests that assert access decisions
- shared_contract_schema_usage: none visible in this target snapshot
- duplicate_or_near_duplicate_shapes: probable overlap with capability status cataloging in `packages/capability-domain`; potential overlap with family household authority if access gating is derived from roles rather than entitlement-specific state
- id_name_status_drift: no explicit branded ID layer was visible in the target snapshot
- direct_import_vs_event_boundary: no event boundary visible; access appears to be direct function/decision logic
- event_bus_usage: none visible
- logging_and_proof_chain_expectations: if this crate is used in a command/request chain, it should inherit logger/proof-chain expectations from the repo rules; the current surface does not show instrumentation hooks
- boundary_violations: the crate looks under-specified and risks becoming a name-only frontage if it duplicates access rules already owned by family or capability domains
- dry_common_core_candidates: a shared entitlement/access decision core that other runtimes can import; a shared capability-grant model if entitlement is really a cross-domain permission primitive
- dead_frontage_or_shims: the crate may be acting as a thin frontage around one module rather than a durable ownership boundary
- proposed_fix_packets: clarify whether this crate owns entitlement semantics distinct from family authority and capability catalogs; if not, collapse it into the true owner and keep only a minimal adapter surface
- severity: medium
- confidence: medium
- evidence_paths: crates/entitlement-core/Cargo.toml; crates/entitlement-core/src/lib.rs; crates/entitlement-core/src/entitlement_access.rs; crates/entitlement-core/tests/unit.rs; crates/entitlement-core/tests/unit/capability_access.rs; crates/entitlement-core/tests/unit/capability_gate.rs

## Current Refresh Audit - 2026-06-19
- responsibility: canonical Rust-side entitlement capability decisioning, typed decision event recording, and idempotent decision replay for child-runtime and parent-facing access checks.
- deps: `ocentra-eventing` plus `serde`; no broader internal dependency fanout is visible in `Cargo.toml`, which keeps the crate narrow but also makes the module the single semantic choke point.
- violations: the module comment still claims broader ownership than the code proves, especially around payment-result consumption and offline grace policy; the current code only proves the capability gate and decision-recording path.
- duplicated shapes: the `EntitlementCapability*` enums, `EntitlementCapabilityInput`, and `EntitlementDecision` mirror capability-matrix concepts already represented in `packages/capability-domain/src/capabilities.ts`, while the same gate semantics are repeated in `crates/child-runtime/src/runtime_gate.rs` and its tests.
- barrel/reexport/shim debt: no forbidden re-export was found, but `crates/entitlement-core/src/lib.rs` is a thin module frontage that exposes only `entitlement_access`; this is shim-like packaging rather than a true multi-module owner.
- schema drift: event payloads are versioned only at the contract wrapper level (`EventContract` + schema version 1); the request/decision structs themselves are unversioned, so future field drift will have to be managed manually inside `entitlement_access.rs`.
- event/log/proof misuse: event typing is used correctly for the two decision events, but there is no visible logging or proof-chain instrumentation in this crate; if it enters a runtime command/request path, repo logging/redaction expectations will need to be satisfied elsewhere.
- test/proof structure issues: the test split between `capability_gate.rs` and `capability_access.rs` repeats the same decision matrix with only small variations, which makes maintenance noisy; there is no separate proof harness or contract-level coverage beyond direct `DomainEvent` assertions.
- score: 4/10 structural fit, because the gate is coherent but still overlaps nearby capability-domain semantics and remains too thin to justify broad ownership language.
- fix recommendation: collapse the repeated decision matrix into one canonical entitlement policy core, keep the event adapter thin, and trim the module comment so it only claims the gate and decision-recording behavior actually present.
- decouple recommendation: if the capability catalog is the true owner, move the shared capability-state vocabulary there and let this crate consume it; if entitlement-core is the true owner, make `packages/capability-domain` and `crates/child-runtime` consume this crate instead of re-expressing the same policy vocabulary.
- blockers: no hard blocker on the audit itself; the only limitation is that this is a read-only refresh and cannot prove which downstream package is the authoritative semantic owner without a broader ownership pass.
- exact likely paths: `crates/entitlement-core/src/lib.rs`; `crates/entitlement-core/src/entitlement_access.rs`; `crates/entitlement-core/tests/unit.rs`; `crates/entitlement-core/tests/unit/capability_gate.rs`; `crates/entitlement-core/tests/unit/capability_access.rs`; `crates/entitlement-core/Cargo.toml`; `packages/capability-domain/src/capabilities.ts`; `packages/capability-domain/src/capability-data.ts`; `crates/child-runtime/src/runtime_gate.rs`; `crates/child-runtime/tests/unit/runtime_gate.rs`.

## Current Refresh Audit - 2026-06-19
- responsibility: owns the Rust entitlement gate boundary for `ocentra-entitlement-core`; the actual module root is thin (`src/lib.rs` only re-exports the internal access module), so the effective responsibility is decisioning plus event-adapter behavior rather than a broad policy surface.
- dependencies: `ocentra-eventing` and `serde` only, which keeps the crate intentionally narrow but also means the crate is highly coupled to its internal module for all meaningful behavior.
- violations: the crate-level framing still looks a bit larger than the visible boundary; the manifest and module root support a small decision gate, not a fully independent entitlement subsystem.
- duplicated shapes: the entitlement/capability naming overlaps with nearby capability-domain vocabulary, and the current tests suggest the same access matrix is being modeled in more than one place.
- barrel/re-export/shim debt: `src/lib.rs` is a minimal frontage over `entitlement_access`; no forbidden re-export pattern is visible, but the crate does read like a shim boundary rather than a multi-module owner.
- schema drift: there is no visible versioned schema layer inside the crate root itself, so any event or decision-shape drift will likely be handled manually in `entitlement_access.rs` and its tests.
- event/log/proof misuse: no visible logging or proof-chain contract is established at the crate root, so this target should not be treated as a proven runtime evidence source; if it enters a chain path, the repo logging/redaction rules will need separate satisfaction.
- test/proof structure issues: the target layout suggests unit tests are split around closely related capability/access cases, which usually leads to repeated matrix logic and thin proof separation instead of one canonical decision harness.
- score: 38/100 DRY fit. The crate is focused and small, but the naming overlap, repeated access vocabulary, and shim-like frontage mean the boundary still looks partially duplicated rather than fully canonical.
- fix recommendation: keep this crate as the single Rust entitlement decision adapter only if it owns a unique entitlement concept; otherwise move the shared capability vocabulary into the true domain owner and reduce this crate to a thin consumer.
- decouple recommendation: split the policy vocabulary from the access adapter. Shared capability/entitlement state should live in the canonical domain package, while this crate should consume that model and only emit the Rust-side decision/event translation.
- blockers: no audit blocker, but the current refresh cannot prove ownership finality without reading the downstream capability-domain and child-runtime call sites, which is outside this file’s allowed scope.
- exact likely paths: `crates/entitlement-core/Cargo.toml`; `crates/entitlement-core/src/lib.rs`; `crates/entitlement-core/src/entitlement_access.rs`; `crates/entitlement-core/tests/unit.rs`; `crates/entitlement-core/tests/unit/capability_gate.rs`; `crates/entitlement-core/tests/unit/capability_access.rs`.
