# packages/browser-domain
- target_kind: TypeScript package of browser and adjacent domain contracts
- owned_paths: `packages/browser-domain/package.json`; `packages/browser-domain/src/browser.ts`; `packages/browser-domain/src/browser-values.ts`; `packages/browser-domain/src/browser-schemas.ts`; `packages/browser-domain/src/browser-target-schemas.ts`; `packages/browser-domain/src/package-info.ts`
- declared_responsibility: managed browser, browser game, URL intelligence, and browser intervention contracts
- observed_responsibility: the package is a wide contract mega-surface spanning browser, unmanaged-process, inventory, extension, intervention, URL, game, social, AI, dashboard, and alert-report schemas; `src/browser.ts` acts as an export hub for many of them
- should_own: browser-domain contracts and narrow browser-adjacent schemas
- should_not_own: runtime behavior, notification delivery, policy execution, or sibling feature families that deserve their own contract packages if they keep growing
- allowed_dependencies: `@ocentra-parent/schema-domain/effect`; `@ocentra-parent/evidence-domain/contracts`; `@ocentra-parent/evidence-domain/primitives`; `@ocentra-parent/evidence-domain/kinds`; `@ocentra-parent/family-domain/*`; `@ocentra-parent/capability-domain/*`; `@ocentra-parent/notification-domain/*`; `@ocentra-parent/policy-domain/*`
- suspicious_dependencies: notification and policy imports in the social alert/report files imply the package is carrying cross-domain workflow contracts, not just browser shape definitions
- expected_dependents: browser plan tests, portal consumers, other contract packages, and runtime adapters that need browser schemas
- shared_contract_schema_usage: extensive Effect Schema and brand usage; `browser.ts` aggregates `browser-values`, `browser-schemas`, `browser-url-metadata-schemas`, `browser-url-intelligence-schemas`, `browser-inventory-schemas`, `browser-intervention`, and related contract files
- duplicate_or_near_duplicate_shapes: repeated evidence-id/timestamp schema pairs across many `browser-*` files; browser/game/social subfamilies repeat nearly identical schema scaffolding and could be grouped by subdomain
- id_name_status_drift: the package name still says browser-domain, but the observed surface mixes browser, browser-game, social-report, AI, and alert-report families; `src/browser.ts` behaves like a whole-package prelude rather than a narrow domain entrypoint
- direct_import_vs_event_boundary: direct imports are contract-only and mostly acceptable; no event bus usage observed; the risk is over-centralized re-exporting, not bus leakage
- event_bus_usage: none observed
- logging_and_proof_chain_expectations: this should stay schema-only and log-free; proof-chain logging belongs in runtime consumers, not in these contract files
- boundary_violations: `package.json` exports `./*`, and `src/browser.ts` uses wildcard and named re-exports at lines 51-168, which violates the repo no-barrels rule and creates a fake single entrypoint
- dry_common_core_candidates: split the package into browser-core contracts, browser-game contracts, and social/report contracts; keep only small shared primitives in a narrow root surface
- dead_frontage_or_shims: `src/browser.ts` is dead frontage in the sense that it mostly forwards other modules instead of owning one contract boundary
- proposed_fix_packets: remove the wildcard package export, replace `browser.ts` with explicit submodule exports, carve out subdomain packages or nested entrypoints, and centralize shared evidence/timestamp primitives in a smaller base module
- severity: high
- confidence: high
- evidence_paths: `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\browser-domain\package.json`; `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\browser-domain\src\browser.ts`; `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\browser-domain\src\browser-values.ts`; `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\browser-domain\src\browser-schemas.ts`; `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\browser-domain\src\browser-target-schemas.ts`; `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\browser-domain\src\browser-social-ai-analysis-schemas.ts`; `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\browser-domain\src\social-alert-report-intent.ts`

## Current Refresh Audit - 2026-06-19

- Responsibility: `packages/browser-domain` still reads as a broad browser-adjacent contract bucket rather than a narrow browser-only package. The visible surface spans browser core, unmanaged process, inventory, extension, intervention, URL intelligence, browser game, social, AI, dashboard, and alert-report contracts.
- Dependencies: the package depends on `schema-domain`, `evidence-domain`, `family-domain`, `capability-domain`, `notification-domain`, and `policy-domain`. The cross-domain workflow pressure is most visible in the notification and policy dependencies.
- Boundary violations: the repo no-barrels/no-re-exports hard gate is still violated. `packages/browser-domain/package.json` exposes `./*`, and `packages/browser-domain/src/browser.ts` acts as a central export hub for many subdomains.
- Duplicated shapes: repeated evidence-id, timestamp, state, and schema-wrapper patterns appear across the `browser-*`, `browser-game-*`, `browser-social-*`, and `social-*` files. The package is carrying many near-parallel contract families that should be decomposed.
- Barrel/re-export/shim debt: high. `browser.ts` re-exports across multiple subdomains, and the wildcard package export in `package.json` turns the package into a barrel-backed namespace. Exact likely paths: `packages/browser-domain/package.json`, `packages/browser-domain/src/browser.ts`, and the broader `packages/browser-domain/src/browser-*.ts` and `packages/browser-domain/src/social-*.ts` families that feed the hub.
- Schema drift: the declared responsibility says browser, browser game, URL intelligence, and browser intervention contracts, but the observed surface has expanded into social alert/report, AI analysis, dashboard, and parent/policy-adjacent proof contracts. That is contract drift, not just naming drift.
- Event/log/proof misuse: no runtime event bus or logging misuse was verified in the inspected files. Proof-named contract files such as `social-alert-report-provider-preflight-proof.ts`, `social-alert-report-provider-status-handoff-proof.ts`, and `social-alert-report-intent.ts` are acceptable only if they remain schema-only; they would be misuse if they started carrying runtime proof signaling or log emission here.
- Test/proof structure issues: the package does not show a compact proof/test spine proportional to its breadth. Validation appears distributed across many adjacent contract families instead of being anchored by a small browser-domain contract core.
- DRY score: 18/100. Reasons: one package owns too many adjacent domains; the root entrypoint is a barrel; export patterns are repeated across many subfamilies; and the contract surface overlaps heavily in schema scaffolding instead of being split into smaller ownership units.
- Fix recommendation: remove the package wildcard export, stop using `browser.ts` as the package-wide hub, and split the package into smaller contract packages or explicit submodule entrypoints with narrow ownership.
- Decouple recommendation: separate browser-core, browser-inventory/extension/intervention, browser-game, and social/report contracts. Keep only the smallest shared primitives in the root package surface.
- Blockers: no functional runtime blocker was verified in this refresh. The blocker is structural debt plus the hard-gate violation, which should be treated as package boundary cleanup before more surface area is added.
- Exact likely paths: `packages/browser-domain/package.json` for the wildcard export; `packages/browser-domain/src/browser.ts` for the barrel hub; `packages/browser-domain/src/browser-values.ts`, `packages/browser-domain/src/browser-schemas.ts`, `packages/browser-domain/src/browser-inventory-schemas.ts`, `packages/browser-domain/src/browser-intervention.ts`, `packages/browser-domain/src/browser-game-*.ts`, `packages/browser-domain/src/browser-social-*.ts`, and `packages/browser-domain/src/social-*.ts` for the oversized contract families that should be split or narrowed.
- No-barrels/no-re-exports gate: yes, this package currently violates it. The confirmed paths are `packages/browser-domain/package.json` and `packages/browser-domain/src/browser.ts`. The rest of the `browser-*.ts` and `social-*.ts` files are likely feed paths rather than individually confirmed barrel violations in this refresh.

## Current Refresh Audit - 2026-06-19

- Responsibility: this package owns browser-domain contracts and values, but the current surface extends beyond browser core into browser game, AI analysis, intervention, inventory, platform routing, and social-report adjacent contracts. The package boundary is therefore broader than the package name suggests.
- Dependencies: `@ocentra-parent/schema-domain/effect`, `@ocentra-parent/evidence-domain`, `@ocentra-parent/family-domain`, `@ocentra-parent/policy-domain`, `@ocentra-parent/notification-domain`, and `@ocentra-parent/capability-domain`. The local source surface also depends on sibling contract files such as `browser-schemas.ts`, `browser-values.ts`, `browser-unmanaged-process-schemas.ts`, `browser-target-schemas.ts`, and `browser-intervention.ts`.
- Violations: `packages/browser-domain/package.json` exposes `./*` to `dist/*.js` and `dist/*.d.ts`, which turns the package into a wildcard barrel. `packages/browser-domain/src/browser.ts` then re-exports from many submodules and acts as a second barrel hub. That conflicts with the repo no-barrels/no-re-exports gate.
- Duplicated shapes: the package repeats the same literal-and-brand pattern across schema/value pairs and across many sibling contract families. `browser-values.ts` mirrors `browser-schemas.ts`; the AI, game, and intervention families repeat the same enum/value scaffolding; and redaction-safe branded string helpers are repeated in more than one module.
- Barrel/re-export/shim debt: high. The package root export map is wildcard-based, and `browser.ts` centralizes cross-family exports instead of forcing narrow entrypoints. The package also relies on dist-path indirection for consumers rather than explicit source-boundary modules.
- Schema drift: the declared package responsibility is browser, browser game, URL intelligence, and browser intervention contracts, but the source tree also carries AI analysis, platform inventory, policy-questionnaire, social-alert/report, and other adjacent contract families. That is contract drift at the package boundary.
- Event/log/proof misuse: I did not verify runtime event or log emission in the inspected source files. The structural concern is that proof- and observability-themed directories exist beside the contract package without a clearly narrower proof owner, so proof semantics are easy to blur into the contract surface.
- Test/proof structure issues: the package exposes a very broad semantic test taxonomy (`unit`, `integration`, `contract`, `observability`, `security`, `migration`, `release`, `ai-safety`, `chaos`, and more). In the sampled files, the actual tests are concentrated in `tests/unit/browser.test.ts` and `tests/unit/package-info.test.ts`, which suggests the surrounding structure is broader than the verified browser-domain core.
- DRY score: 17/100. Reasons: one package owns too many adjacent domains; the package exports are barrel-like; schema/value duplication is repeated across many families; and the test/proof layout is much larger than the narrow package boundary that the name implies.
- Fix recommendation: remove the wildcard export from `package.json`, stop using `browser.ts` as a package-wide hub, and move toward explicit, narrow submodule entrypoints owned by the smallest contract slices.
- Decouple recommendation: split the current surface into browser-core, browser-inventory/extension/intervention, browser-game, AI-analysis, and social/report contract packages or at least explicit entrypoints. Keep only the smallest shared primitives at the root.
- Blockers: no runtime failure was verified in this refresh. The blocker is structural debt plus the no-barrels/no-re-exports gate, which should be treated as cleanup before the package grows further.
- Exact likely paths: `packages/browser-domain/package.json`, `packages/browser-domain/src/browser.ts`, `packages/browser-domain/src/browser-values.ts`, `packages/browser-domain/src/browser-schemas.ts`, `packages/browser-domain/src/browser-unmanaged-process-schemas.ts`, `packages/browser-domain/src/browser-target-schemas.ts`, `packages/browser-domain/src/browser-intervention.ts`, `packages/browser-domain/src/browser-inventory-schemas.ts`, `packages/browser-domain/src/browser-game-*.ts`, `packages/browser-domain/src/browser-social-*.ts`, `packages/browser-domain/tests/unit/browser.test.ts`, and `packages/browser-domain/tests/unit/package-info.test.ts`.
