# crates/ocentra-evidence
- target_kind: Rust crate
- owned_paths: `crates/ocentra-evidence/**`
- declared_responsibility: Evidence custody and evidence-reference evaluation.
- observed_responsibility: Implements a small evidence-reference decision model with custody, retention, payload, and review-state outcomes.
- should_own: Rust-side evidence-reference decisions and any crate-local mapping needed for evidence custody policy.
- should_not_own: LAN-specific discovery logic, child-domain event construction, or TS package semantics that should live in `packages/evidence-domain`.
- allowed_dependencies: `serde`.
- suspicious_dependencies: None obvious in the dependency list; the main risk is semantic drift, not a third-party dependency.
- expected_dependents: Rust consumers that need custody/reference decisions and any adapters that bridge to shared evidence contracts.
- shared_contract_schema_usage: No TS schema usage here; the crate is pure Rust enums/structs and does not expose a shared schema layer.
- duplicate_or_near_duplicate_shapes: `EvidenceCustodyScope`, `EvidenceReferenceState`, and the decision struct mirror the TS evidence package conceptually but not semantically.
- id_name_status_drift: The Rust crate’s custody scopes are `child-device-local`, `parent-device-local`, and `parent-owned-remote`, while the TS evidence package uses `local-only`, `family-shared`, and `exportable`; that is a real cross-owner drift if these are supposed to describe the same boundary.
- direct_import_vs_event_boundary: No event boundary is present; this is a pure decision crate.
- event_bus_usage: None.
- logging_and_proof_chain_expectations: No runtime/proof chain logging is expected from this small pure decision crate unless it becomes part of a larger runtime flow.
- boundary_violations: The crate is not obviously violating its own scope, but it does not line up cleanly with the TS evidence package’s naming and scope model.
- dry_common_core_candidates: A single canonical custody vocabulary, a shared evidence-reference decision owner, and a small cross-language mapping layer if Rust and TS are intentionally different.
- dead_frontage_or_shims: None.
- proposed_fix_packets: Decide whether this crate should be the canonical evidence contract owner or a Rust-specific adapter, then either align the vocabulary with `packages/evidence-domain` or isolate the translation behind an explicit mapping module.
- severity: medium-high
- confidence: medium-high
- evidence_paths: `crates/ocentra-evidence/src/lib.rs`, `crates/ocentra-evidence/tests/unit/evidence_reference.rs`, `packages/evidence-domain/src/custody.ts`, `packages/evidence-domain/src/primitives.ts`

## Current Refresh Audit - 2026-06-19
- responsibility: Rust-side evidence custody and reference evaluation. The crate is a small pure decision model with no runtime, event, or proof-chain ownership.
- deps: only `serde`; dependency risk is low. The real risk is semantic drift, not external coupling.
- violations: no direct scope violation inside the crate, but the Rust custody vocabulary does not line up with the TS evidence package vocabulary and looks like a split ownership model without an explicit adapter contract.
- duplicated shapes: `EvidenceCustodyScope`, `EvidenceReferenceState`, and the decision/input structs mirror the TS package conceptually, while the Rust enums add `PrivatePayloadState`, `RetentionState`, `RuntimeBoundaryState`, and `ManualReviewState` in a way that duplicates policy logic instead of sharing a single owner.
- barrel/reexport/shim debt: none visible in `src/lib.rs`; the crate exports concrete types and functions directly with no re-export shim.
- schema drift: Rust serializes `child-device-local` / `parent-device-local` / `parent-owned-remote`, while the TS package uses `local-only` / `family-shared` / `exportable`; that is a real cross-language contract drift if both are meant to describe the same evidence boundary.
- event/log/proof misuse: none in the crate itself, which is correct for a pure decision module. The evidence/proof directories are present but empty, so there is no local proof artifact yet to pin the contract to.
- test/proof structure issues: only one unit test file with three cases. Coverage hits happy-path cross-boundary and two rejection cases, but there is no round-trip serialization test, no table over custody states, and no contract/proof harness using the `proof/` or `proofs/` trees.
- score: 68/100. Small and understandable, but the contract drift and missing proof scaffolding keep it from being cleanly owned.
- fix recommendation: choose one canonical evidence vocabulary, then add an explicit Rust-to-TS mapping layer or align the enum names and serialized literals across the two owners.
- decouple recommendation: if Rust must stay canonical, isolate translation in a dedicated adapter module and keep the TS package as a consumer; otherwise move the shared custody contract to a single owner and delete the duplicated shape.
- blockers: cannot fully close the audit without confirming whether `packages/evidence-domain` is the canonical evidence contract owner and whether any downstream consumer depends on the Rust literals as public wire format.
- exact likely paths: `crates/ocentra-evidence/src/lib.rs`, `crates/ocentra-evidence/tests/unit/evidence_reference.rs`, `crates/ocentra-evidence/proof/`, `crates/ocentra-evidence/proofs/ci/`, `crates/ocentra-evidence/proofs/local/`, `packages/evidence-domain/src/custody.ts`, `packages/evidence-domain/src/primitives.ts`

## Current Refresh Audit - 2026-06-19
- responsibility: Rust-side evidence custody and evidence-reference evaluation only. `crates/ocentra-evidence` is a compact decision crate, not a runtime, event, or proof-chain owner.
- dependencies: `serde` is the only declared dependency. There is no obvious third-party coupling risk; the main risk is contract drift across owners.
- violations: no direct intra-crate boundary violation, but the crate appears to share the same evidence boundary concept as the TS package without a single declared canonical owner.
- duplicated shapes: `EvidenceCustodyScope`, `EvidenceReferenceState`, `EvidenceReferenceInput`, and `EvidenceReferenceDecision` overlap conceptually with `packages/evidence-domain`, while `PrivatePayloadState`, `RetentionState`, `RuntimeBoundaryState`, and `ManualReviewState` encode policy decisions that should likely live in one shared owner.
- barrel/re-export/shim debt: none observed in `crates/ocentra-evidence/src/lib.rs`; the crate exposes concrete enums, structs, and the evaluator directly, with no re-export layer to hide ownership.
- schema drift: the Rust serde literals use `child-device-local`, `parent-device-local`, and `parent-owned-remote`, while the TS evidence package uses `local-only`, `family-shared`, and `exportable`. If these are meant to be the same boundary, the wire contract is already split.
- event/log/proof misuse: none in the crate itself, which is appropriate for a pure decision module. The local evidence/proof directories are not carrying a visible contract proof artifact yet, so the boundary is not pinned by a local proof chain.
- test/proof structure issues: only one unit test file exists, with three path tests. There is no serialization round-trip check, no table-driven state coverage, and no proof harness under `proof/` or `proofs/` to anchor the contract.
- DRY score: 67/100. The crate is small and readable, but the duplicated policy shapes, cross-language naming drift, and absent proof scaffolding keep it from being cleanly DRY.
- fix recommendation: pick one canonical evidence vocabulary, then either align the Rust enum names and serialized literals with `packages/evidence-domain` or introduce a deliberate translation layer that owns the mismatch.
- decouple recommendation: if Rust must remain the owner, isolate conversion in a dedicated adapter module and keep the TS package as a consumer; otherwise move the shared custody contract to one owner and remove the mirrored shape from the other side.
- blockers: cannot close the audit without confirming whether `packages/evidence-domain` is the authoritative evidence contract owner and whether any downstream consumer depends on the Rust serde strings as public wire format.
- exact likely paths: `crates/ocentra-evidence/src/lib.rs`, `crates/ocentra-evidence/tests/unit/evidence_reference.rs`, `packages/evidence-domain/src/custody.ts`, `packages/evidence-domain/src/primitives.ts`, `crates/ocentra-evidence/proof/`, `crates/ocentra-evidence/proofs/ci/`, `crates/ocentra-evidence/proofs/local/`
