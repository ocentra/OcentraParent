# packages/agent-protocol-domain
- target_kind: TypeScript shared contract package.
- owned_paths: packages/agent-protocol-domain/package.json; packages/agent-protocol-domain/tsconfig.json; packages/agent-protocol-domain/src/contracts.ts; packages/agent-protocol-domain/src/primitives.ts; packages/agent-protocol-domain/src/security.ts; packages/agent-protocol-domain/src/defaults.ts; packages/agent-protocol-domain/src/activity-surface-adapter.ts; packages/agent-protocol-domain/src/activity-surface-adapter-manifest.ts; packages/agent-protocol-domain/src/network-runtime-events.ts; packages/agent-protocol-domain/src/network-remote-delivery-status.ts; packages/agent-protocol-domain/src/lan-pairing-browser-runtime.ts; packages/agent-protocol-domain/src/lan-pairing-browser-add-device-state.ts; packages/agent-protocol-domain/src/network-flow-read-model.ts; packages/agent-protocol-domain/src/package-info.ts
- declared_responsibility: Shared agent command and event contracts for localhost and future relay transports.
- observed_responsibility: The broadest shared contract surface in the repo, covering commands/events, primitives, security states, network runtime events, network remote-delivery status, activity surface adapters, LAN add-device flows, and package metadata/defaults.
- should_own: Effect Schema contracts, branded primitives, literals, parser helpers, and shared contract tables for TS/Rust boundary data.
- should_not_own: Runtime transport execution, service orchestration, or duplicated adapter manifests when a single canonical table can derive them.
- allowed_dependencies: @ocentra-parent/activity-domain, ai-domain, app-game-domain, browser-domain, child-runtime-domain, enforcement-domain, event-domain, evidence-domain, family-domain, lan-domain, logging-domain, network-domain, schema-domain, screen-domain.
- suspicious_dependencies: `src/primitives.ts` is a barrel export of `event-domain/primitives`; `src/contracts.ts` is a giant central aggregator; `activity-surface-adapter-manifest.ts` duplicates the operation matrix already embedded in `activity-surface-adapter.ts`; `network-remote-delivery-status.ts` and `network-runtime-events.ts` are very large single-file shape families.
- expected_dependents: Rust protocol/core/service crates, portal/runtime adapters, parity tests, and future relay transports.
- shared_contract_schema_usage: Very high; these files are contract-first schemas with runtime validation and parser helpers.
- duplicate_or_near_duplicate_shapes: `ActivitySurfaceAdapterOperationManifest` duplicates the command/event/payload/read-model matrix in `activity-surface-adapter.ts`; `AgentCommandNameLiteral`/`AgentEventNameLiteral` plus `AgentCommand`/`AgentEvent` duplicate string tables and parsed enums; `AgentLanBrowserRuntime*` aliases duplicate lan-domain runtime requests; `AgentNetworkRemoteDeliveryStatus` repeats a large ref/state/count lattice that overlaps the Rust protocol crate.
- id_name_status_drift: High in `contracts.ts` and `primitives.ts`; both read like generic frontage files even though they own major shared contract tables.
- direct_import_vs_event_boundary: Schema imports are appropriate, but this package is close to an executable adapter layer because parsing/serialization and manifest logic sit beside contracts.
- event_bus_usage: Yes, but only as contract/codec shape, not runtime transport.
- logging_and_proof_chain_expectations: Contract-only files are mostly exempt from runtime logging, but parser/codec helpers and manifest changes must preserve exact field naming and proof semantics for downstream consumers.
- boundary_violations: `src/primitives.ts` violates the no-barrels rule via `export * from '@ocentra-parent/event-domain/primitives'`; `src/contracts.ts` and `src/activity-surface-adapter.ts` are frontage-heavy and partially duplicate routing/operation tables.
- dry_common_core_candidates: Derive adapter manifests from a single source table; collapse repeated `Schema.Literal`/`Schema.Struct` families into shared local builders; move common command/event/default logic into smaller per-domain files.
- dead_frontage_or_shims: `primitives.ts` is a pure barrel; `contracts.ts` and `package-info.ts` are facade modules; `activity-surface-adapter-manifest.ts` is metadata-only frontage over `activity-surface-adapter.ts`.
- proposed_fix_packets: Remove the barrel export in `primitives.ts`, split `contracts.ts` into smaller domain entrypoints, and make manifest files derive from one canonical contract table instead of repeating command/event/payload pairs.
- severity: high.
- confidence: high.
- evidence_paths: packages/agent-protocol-domain/package.json; packages/agent-protocol-domain/tsconfig.json; packages/agent-protocol-domain/src/contracts.ts; packages/agent-protocol-domain/src/primitives.ts; packages/agent-protocol-domain/src/security.ts; packages/agent-protocol-domain/src/defaults.ts; packages/agent-protocol-domain/src/activity-surface-adapter.ts; packages/agent-protocol-domain/src/activity-surface-adapter-manifest.ts; packages/agent-protocol-domain/src/network-runtime-events.ts; packages/agent-protocol-domain/src/network-remote-delivery-status.ts; packages/agent-protocol-domain/src/lan-pairing-browser-runtime.ts; packages/agent-protocol-domain/src/lan-pairing-browser-add-device-state.ts; packages/agent-protocol-domain/src/package-info.ts

## Current Refresh Audit - 2026-06-19
- current_responsibility: shared agent protocol contract package for command/event envelopes, defaults, LAN pairing, activity adapters, browser/runtime bridges, and network runtime/delivery shapes.
- current_dependencies: `@ocentra-parent/activity-domain`, `browser-domain`, `enforcement-domain`, `event-domain`, `family-domain`, `lan-domain`, `logging-domain`, `network-domain`, `schema-domain`, and `screen-domain`, with live bridge imports still concentrated in `contracts.ts`, `defaults.ts`, `security.ts`, and the adapter files.
- boundary_violations: no live `export *` barrels remain in `packages/agent-protocol-domain/src`; the stale `src/primitives.ts` violation from the earlier audit is gone. The live boundary blur is the mega-facade `src/contracts.ts` plus the wide mixed-owner tables in `src/defaults.ts` and `src/activity-surface-adapter.ts`.
- duplicated_shapes: `ActivitySurfaceAdapterOperationManifest` still duplicates the command/event/payload/read-model matrix already encoded in `src/activity-surface-adapter.ts`; `AgentCommand`/`AgentEvent` still mirror large literal tables in `src/contracts.ts`; `src/network-runtime-events.ts` and `src/network-remote-delivery-status.ts` each carry large self-validating shape lattices that their tests also restate; `src/lan-pairing-browser-runtime.ts` remains a thin alias bridge over LAN-domain requests.
- barrel/reexport/shim_debt: there is no source barrel debt left under `src/`, but `packages/agent-protocol-domain/package.json` still fans out many subpath exports while `src/contracts.ts` acts as the central facade. `src/package-info.ts` is only a metadata shim.
- schema/contract_drift: the old audit path `src/primitives.ts` is stale, so the current drift is not a barrel file but an over-concentrated contract surface. Contract names, defaults, and field literals are still centralized enough that package exports and tests can drift from the owning schemas if the facade grows further.
- event_bus/log/proof_misuse: the network/runtime parsing files correctly enforce no-claim and no-content boundaries, and they stay side-effect free. They do not misuse runtime logging, but they are proof-like validators, so any future instrumentation must remain structured and redacted rather than adding ad hoc runtime side effects.
- test/proof_structure_issues: this package is still mostly covered by broad unit tests such as `tests/unit/contracts.test.ts`, `tests/unit/activity-surface-adapter.test.ts`, `tests/unit/network-runtime-events.test.ts`, and `tests/unit/network-remote-delivery-status.test.ts`. The `proofs/local` and `proofs/ci` directories are still only `.gitkeep`, so there is no dedicated proof-artifact layer for the largest contract families.
- current_dry_score: 52/100.
- fix_pass_recommendation: first split `src/contracts.ts` along domain ownership lines and derive adapter manifests from one canonical operation table; then shrink `src/defaults.ts` into smaller domain-owned constant modules.
- decouple_pass_recommendation: after the fix pass, move the LAN/browser alias bridge into the owning LAN package if callers can absorb the dependency shift, and trim the package.json subpath fan-out so each exported entrypoint has a clearer ownership boundary.
- sequencing_blockers: fix-pass is unblocked. Decouple-pass is gated by keeping existing subpath exports stable for current callers and by replacing the current unit-test import surface before any entrypoint reshaping.
- exact_likely_file_paths: `packages/agent-protocol-domain/src/contracts.ts`, `packages/agent-protocol-domain/src/defaults.ts`, `packages/agent-protocol-domain/src/activity-surface-adapter.ts`, `packages/agent-protocol-domain/src/activity-surface-adapter-manifest.ts`, `packages/agent-protocol-domain/src/network-runtime-events.ts`, `packages/agent-protocol-domain/src/network-remote-delivery-status.ts`, `packages/agent-protocol-domain/src/lan-pairing-browser-runtime.ts`, `packages/agent-protocol-domain/package.json`, `packages/agent-protocol-domain/tests/unit/contracts.test.ts`, `packages/agent-protocol-domain/tests/unit/activity-surface-adapter.test.ts`, `packages/agent-protocol-domain/tests/unit/network-runtime-events.test.ts`, `packages/agent-protocol-domain/tests/unit/network-remote-delivery-status.test.ts`.
- blocking_uncertainty: the only live uncertainty is downstream compatibility for the large `package.json` subpath export map; I did not inspect every consumer in this refresh, so the decouple pass should verify caller breakage before reshaping entrypoints.

## Current Refresh Audit - 2026-06-19
- responsibility: `packages/agent-protocol-domain` is the shared agent protocol contract package for command/event envelopes, defaults, LAN pairing, activity adapters, browser/runtime bridges, network runtime/delivery shapes, and package metadata.
- dependencies: live coupling still runs through `@ocentra-parent/activity-domain`, `browser-domain`, `enforcement-domain`, `event-domain`, `family-domain`, `lan-domain`, `logging-domain`, `network-domain`, `schema-domain`, and `screen-domain`; the heaviest bridge imports are in `src/contracts.ts`, `src/defaults.ts`, `src/activity-surface-adapter.ts`, and the network/LAN adapter files.
- violations: there is no live `export *`/re-export barrel debt left in `src/`; the stale `src/primitives.ts` barrel from the prior audit is not present in the current boundary. The current boundary problem is concentration, not a barrel: `src/contracts.ts` is a mega-facade and `src/package-info.ts` is a metadata shim.
- duplicated_shapes: `ActivitySurfaceAdapterOperationManifest` still duplicates the command/event/payload/read-model matrix already expressed in `src/activity-surface-adapter.ts`; `AgentCommand` and `AgentEvent` still mirror large literal tables in `src/contracts.ts`; `src/network-runtime-events.ts` and `src/network-remote-delivery-status.ts` each encode large self-validating shape lattices that the unit tests restate; `src/lan-pairing-browser-runtime.ts` is still a thin alias bridge over LAN-domain request schemas.
- barrel/re-export/shim_debt: `src/` no longer contains a barrel file, but `packages/agent-protocol-domain/package.json` still fans out many subpath exports, and `src/contracts.ts` remains the central facade that downstream consumers are likely to depend on directly.
- schema_drift: the earlier `primitives.ts` drift is stale. The live drift risk is that names, defaults, and payload literals are centralized enough that `package.json` exports and tests can drift from the owning schemas if `src/contracts.ts` continues to absorb new surfaces. The package is still schema-first, but too many contract families meet in one file.
- event_log_proof_misuse: the inspected parser/validator modules stay side-effect free and do not misuse runtime logging. They are proof-like guards, so the main risk is overclaiming in contract fields or tests, not logging noise.
- test_proof_structure_issues: this package is covered by broad unit tests under `packages/agent-protocol-domain/tests/unit/*.test.ts`, with no dedicated `proofs/` subtree visible in this boundary. The biggest contract families are proven by wide tests that restate the same tables instead of having smaller, owning proof artifacts.
- dry_score: 56/100. The score is higher than the previous refresh because the live barrel violation is gone, but the package still has heavy centralization, repeated manifest tables, and a large export map. The score stays below 60 because one facade file still owns too many domains and the tests mirror rather than decouple the largest shapes.
- fix_recommendation: split `src/contracts.ts` along domain lines, then derive `ActivitySurfaceAdapterOperationManifest` from one canonical table instead of maintaining parallel matrices. After that, shrink `src/defaults.ts` into smaller owner-aligned modules.
- decouple_recommendation: move the LAN/browser alias bridge out of this package only after consumers can absorb the dependency shift, and only after the export-map surface is reduced so each subpath has a single obvious owner.
- blockers: no blocker for the audit refresh itself. The next code-change blocker would be downstream compatibility for reshaping the `package.json` export map and consumer imports; that needs caller verification before any entrypoint reshaping.
- exact_likely_paths: `packages/agent-protocol-domain/src/contracts.ts`, `packages/agent-protocol-domain/src/defaults.ts`, `packages/agent-protocol-domain/src/activity-surface-adapter.ts`, `packages/agent-protocol-domain/src/activity-surface-adapter-manifest.ts`, `packages/agent-protocol-domain/src/network-runtime-events.ts`, `packages/agent-protocol-domain/src/network-remote-delivery-status.ts`, `packages/agent-protocol-domain/src/lan-pairing-browser-runtime.ts`, `packages/agent-protocol-domain/src/package-info.ts`, `packages/agent-protocol-domain/package.json`, `packages/agent-protocol-domain/tests/unit/contracts.test.ts`, `packages/agent-protocol-domain/tests/unit/activity-surface-adapter.test.ts`, `packages/agent-protocol-domain/tests/unit/network-runtime-events.test.ts`, `packages/agent-protocol-domain/tests/unit/network-remote-delivery-status.test.ts`.
