# crates/tracking-core
- target_kind: Rust domain core crate with a large public barrel in `src/lib.rs`
- owned_paths: `crates/tracking-core/Cargo.toml`; `crates/tracking-core/src/*.rs`; `crates/tracking-core/tests/*.rs`
- declared_responsibility: tracking evidence, geofence, runtime, read-model, and retention core
- observed_responsibility: core tracking schemas plus geofence, nearby-place, missing-device, retention, read-model, runtime event flow, and SQLite-backed read-model helpers
- should_own: tracking-specific evidence/state evaluation, read-model shaping, retention application, and tracking runtime event assembly
- should_not_own: protocol string inventories, parent-runtime orchestration, or broad persistence concerns that could live behind narrower adapters
- allowed_dependencies: `ocentra-evidence`; `ocentra-eventing`; `ocentra-family-identity-core`; `ocentra-parent-agent-protocol`; `ocentra-policy-control-core`; `rusqlite`; `serde`; `serde_json`
- suspicious_dependencies: `rusqlite` inside a "core" crate is acceptable for the read-model seam but becomes suspicious if more persistence logic accretes; `ocentra-parent-agent-protocol` and `ocentra-policy-control-core` are expected but should stay contract-only
- expected_dependents: parent runtime flows, tracking UI/report consumers, contract tests, replay/ordering tests, and any local read-model or retention harnesses
- shared_contract_schema_usage: heavy reuse of `ocentra-parent-agent-protocol` constants/events and `ocentra-eventing` envelopes; local schemas are built on shared evidence and policy-control contracts
- duplicate_or_near_duplicate_shapes: status/read-model/retention shapes repeat across `status.rs`, `read_model.rs`, `read_model_rows.rs`, `retention_settings.rs`, and `runtime_flow.rs`; the public barrel also duplicates the module boundary in export form
- id_name_status_drift: low-to-moderate; most names are coherent, but the public API surface is flattened in `lib.rs`, which hides the real module ownership behind a single entrypoint
- direct_import_vs_event_boundary: direct imports into agent protocol and eventing are appropriate for schema/event assembly, but runtime helpers should keep the event boundary explicit instead of letting protocol constants leak into more logic
- event_bus_usage: yes; `runtime_flow.rs`, `read_model.rs`, and the tests around replay/ordering/observability all sit on the event/protocol chain
- logging_and_proof_chain_expectations: proof-chain coverage is present in tests (`observability`, `replay`, `contract`, `ordering`, `idempotency`), but the sampled runtime files do not show an explicit logger-ready pattern
- boundary_violations: `src/lib.rs` is a broad `pub use` barrel; the crate boundary is also carrying both domain logic and read-model persistence helpers
- dry_common_core_candidates: `tracking-primitives`, `tracking-event-contracts`, `tracking-evidence`, `tracking-geofence`, `tracking-local-place-store-schemas`, `tracking-read-model`
- dead_frontage_or_shims: `src/lib.rs` is the main frontage concern; the rest of the crate looks like real code rather than dead shims
- proposed_fix_packets: split the barrel into smaller named entry modules, keep protocol/event assembly close to the flows that need it, and isolate read-model/persistence helpers behind a narrower storage seam
- dry_score_0_to_100: 69
- score_breakdown:
  - owner_boundary_0_to_20: 14
  - dependency_direction_0_to_15: 10
  - shared_contract_schema_0_to_15: 13
  - duplicate_shape_control_0_to_15: 9
  - direct_import_vs_event_boundary_0_to_15: 10
  - decoupling_and_common_core_0_to_10: 7
  - proof_logging_alignment_0_to_10: 6
- first_fix_roi: remove the `src/lib.rs` barrel pattern first; that makes the real module ownership visible and reduces accidental coupling immediately
- severity: medium-high
- confidence: high
- evidence_paths:
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\crates\tracking-core\Cargo.toml`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\crates\tracking-core\src\lib.rs`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\crates\tracking-core\src\tracking-primitives.rs`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\crates\tracking-core\src\tracking-runtime.rs`
- `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\crates\tracking-core\src\tracking-read-model.rs`
- `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\crates\tracking-core\tests\observability.rs`

## Current Refresh Audit - 2026-06-19
- scope: `crates/tracking-core` with the smallest boundary needed to judge `src/lib.rs`, `src/runtime_flow.rs`, `src/read_model_guard.rs`, `src/read_model.rs`, `src/read_model_rows.rs`, `src/status.rs`, and the focused test tree under `tests/`
- responsibility: the crate still owns tracking evidence, geofence, nearby-place, missing-device, retention, runtime event assembly, read-model schema/differential checks, and SQLite-backed read-model helpers
- dependencies: `ocentra-evidence`, `ocentra-eventing`, `ocentra-family-identity-core`, `ocentra-parent-agent-protocol`, `ocentra-policy-control-core`, and `rusqlite` are the declared boundary; `rusqlite` is the only dependency that reads as a persistence seam inside a "core" crate
- barrel and shim debt: `src/lib.rs` is still a broad `pub use` frontage that flattens the module boundary and hides which submodule owns which decisions; that is the main structural debt in the crate
- duplicated shapes: `read_model.rs`, `read_model_rows.rs`, `read_model_guard.rs`, `retention_settings.rs`, and `runtime_flow.rs` repeat similar state/decision naming around schema, differential, retention, and runtime assembly
- schema drift risk: low-to-moderate; the protocol constants used by `runtime_flow.rs` and `read_model_guard.rs` look aligned with the declared contract packages, but the persistence seam is still mixed into the same crate boundary
- event/log/proof misuse: no direct misuse surfaced in the sampled files, but `runtime_flow.rs` assembles protocol events without a separate logger-ready or proof-handoff seam, so proof intent is carried mostly by the test tree
- test/proof structure: the test surface is strong and focused (`tests/unit.rs`, `tests/contract.rs`, `tests/replay.rs`, `tests/observability.rs`), but the same public-barrel flattening makes ownership harder to map from test intent back to one module
- dry_score_0_to_100: 69
- fix_recommendation: remove the crate-level frontage first by shrinking `src/lib.rs` into smaller named entry modules or direct module imports from callers; that will expose the real ownership split and reduce accidental coupling
- decouple_recommendation: isolate read-model and persistence helpers behind a narrower storage seam around `read_model.rs`, `read_model_rows.rs`, and `read_model_guard.rs`, while keeping runtime event assembly closer to `runtime_flow.rs`
- blockers: no audit blocker; the only meaningful structural blocker is that the current crate boundary already behaves like one large API surface, so decoupling has to start by shrinking that surface
- exact_likely_paths: `crates/tracking-core/src/lib.rs`; `crates/tracking-core/src/runtime_flow.rs`; `crates/tracking-core/src/read_model_guard.rs`; `crates/tracking-core/src/read_model.rs`; `crates/tracking-core/src/read_model_rows.rs`; `crates/tracking-core/src/retention_settings.rs`; `crates/tracking-core/tests/unit.rs`; `crates/tracking-core/tests/contract.rs`; `crates/tracking-core/tests/replay.rs`; `crates/tracking-core/tests/observability.rs`
