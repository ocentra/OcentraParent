# packages/parent-domain

## Current Refresh Audit - 2026-06-19

- Responsibility: `packages/parent-domain` is functioning as a compatibility/forwarding surface, not a local domain owner. `packages/parent-domain/package.json` exposes many subpaths, while the sampled `src` files are single-line shims and `README.md` still describes the package as a placeholder boundary.
- Dependencies: the package depends directly on `@ocentra-parent/app-game-domain`; no local implementation ownership was found in the sampled source.
- Violations: the repo's no-barrels rule is violated by the package's own `export *` files. The package also hides authority behind a forwarding layer instead of owning the contracts it names.
- Duplicated shapes: repeated `*-data`, `*-schema`, `*-rules`, `*-proof`, `*-handoff`, `*-values`, `*-preflight`, and `*-read-model` module names all follow the same pass-through pattern. `672` of `702` files under `packages/parent-domain/src` are direct re-export shims.
- Barrel/re-export/shim debt: this is effectively a barrel farm. Clear examples are `packages/parent-domain/src/app-control-catalog.ts`, `packages/parent-domain/src/app-control-catalog-data.ts`, `packages/parent-domain/src/app-control-catalog-schema.ts`, `packages/parent-domain/src/app-game-control-authority.ts`, `packages/parent-domain/src/app-game-notification-intent.ts`, and `packages/parent-domain/src/app-game-notification-preference-status-handoff.ts`.
- Schema drift: local filenames advertise ownership of catalog, authority, notification, proof, and handoff contracts, but the implementation is deferred to another package. That creates name-to-owner drift and makes the local package exports misleading as an authority boundary.
- Event/log/proof misuse: proof- and handoff-suffixed modules are not local proofs or events; they are pass-through aliases. Any proof/log expectation here is inherited, not enforced, so this package should not be treated as a validation boundary.
- Test/proof structure issues: the sampled `src` surface has no local proof/test layer. Validation is externalized to the target package, so these exports do not provide an owning test harness.
- DRY score: `8/100`. Reasons: 672/702 source files are one-line forwarding shims, the package repeats naming shapes instead of consolidating ownership, and there is no local implementation layer to remove duplication.
- Fix recommendation: keep only compatibility entrypoints that are still required, and move real ownership into the package that implements the contracts rather than duplicating one-line forwarders here.
- Decouple recommendation: move actual contract/schema/proof ownership into the `@ocentra-parent/app-game-domain` implementation package, then delete the per-module forwarding files from `packages/parent-domain/src`.
- Blockers: consumer blast radius is unknown; the exact downstream import set for these shims was not verified here; and the real implementation paths should be confirmed before deleting compatibility exports.
- Exact likely paths: `packages/parent-domain/package.json`, `packages/parent-domain/README.md`, `packages/parent-domain/src/app-control-catalog.ts`, `packages/parent-domain/src/app-control-catalog-data.ts`, `packages/parent-domain/src/app-control-catalog-schema.ts`, `packages/parent-domain/src/app-game-control-authority.ts`, `packages/parent-domain/src/app-game-notification-intent.ts`, `packages/parent-domain/src/app-game-notification-preference-status-handoff.ts`, and likely `packages/app-game-domain/src/*.ts` for the real implementations.
- target_kind: TypeScript parent-domain package, currently dominated by a re-export facade
- owned_paths: `packages/parent-domain/package.json`; `packages/parent-domain/README.md`; `packages/parent-domain/src/*.ts`; `packages/parent-domain/tests/**/*.ts`
- declared_responsibility: shared product contracts for family safety, policy, enforcement, local AI, LAN, mobile readiness, and control catalogs
- observed_responsibility: mostly a mirror layer over `app-game-domain`, `tracking-domain`, `browser-domain`, `enforcement-domain`, `lan-domain`, `notification-domain`, `production-domain`, and related packages, with a smaller set of genuine parent-owned billing, mobile, release-support, and local-export contracts
- should_own: only parent-specific contract/proof surfaces, parent-owned billing/support/admin rows, parent mobile/runtime helpers, release-support proofs, and any genuinely local parent-facing schemas
- should_not_own: broad alias copies of lower-level domain contracts, especially when the local file name adds no new shape or logic
- allowed_dependencies: the package manifest allows a wide set of domain dependencies, including `@ocentra-parent/ai-domain`; `@ocentra-parent/app-game-domain`; `@ocentra-parent/billing-domain`; `@ocentra-parent/browser-domain`; `@ocentra-parent/capability-domain`; `@ocentra-parent/child-runtime-domain`; `@ocentra-parent/enforcement-domain`; `@ocentra-parent/family-domain`; `@ocentra-parent/lan-domain`; `@ocentra-parent/network-domain`; `@ocentra-parent/notification-domain`; `@ocentra-parent/policy-domain`; `@ocentra-parent/production-domain`; `@ocentra-parent/schema-domain`; `@ocentra-parent/screen-domain`; `@ocentra-parent/tracking-domain`
- suspicious_dependencies: the package is allowed to depend on many domains, but `app-game-domain`, `tracking-domain`, `browser-domain`, `enforcement-domain`, `lan-domain`, `notification-domain`, and `production-domain` are suspicious because the source set mostly forwards their contracts instead of owning new ones
- expected_dependents: any package that imports parent-facing policy/billing/release/support contracts, but in practice many consumers are probably using this package as a convenience alias instead of the canonical owner
- shared_contract_schema_usage: extremely high; most source files are thin pass-throughs to other packages' schemas, types, and decoders
- duplicate_or_near_duplicate_shapes: enormous duplication across the `app-game-*`, `tracking-*`, `social-*`, and `v0-8/v0-9-*` mirror files; `billing-*`, `parent-mobile-*`, `parent-owned-local-export-*`, and `parent-desktop-release-support-*` also repeat many state/value/proof wrappers
- id_name_status_drift: severe; many local file names imply ownership, but the file contents are just aliases or re-exports from other packages, so the package name and the actual code ownership diverge
- direct_import_vs_event_boundary: the problem is not eventing so much as direct aliasing of foreign contracts; this package commonly imports and re-exports other packages' runtime behavior instead of staying on a parent-facing boundary
- event_bus_usage: limited and secondary; the real issue is façade density, not bus ownership
- logging_and_proof_chain_expectations: proof-oriented files exist, but the huge shim surface makes it hard to tell which files actually establish the proof chain versus just forwarding it
- boundary_violations: 672 of 702 source files are shims; `src/lib.rs`-style flattening is replaced here by file-per-shim flattening, which is still a façade problem
- dry_common_core_candidates: keep only the truly local cores such as `billing-entitlement`, `billing-support-admin-boundary`, `parent-mobile-runtime`, `parent-owned-local-export-runtime`, `parent-desktop-release-support`, and `tracking-policy-compiler-runtime-proof`
- dead_frontage_or_shims: `672` shim files in `src`, plus a thin `package-info.ts` forwarding file; this is the biggest dead-frontage concentration in the audit set
- proposed_fix_packets: delete or collapse redundant shim files, route consumers to canonical owning packages, and keep only the genuinely parent-owned contracts/proofs in this package
- dry_score_0_to_100: 11
- score_breakdown:
  - owner_boundary_0_to_20: 3
  - dependency_direction_0_to_15: 2
  - shared_contract_schema_0_to_15: 2
  - duplicate_shape_control_0_to_15: 1
  - direct_import_vs_event_boundary_0_to_15: 1
  - decoupling_and_common_core_0_to_10: 1
  - proof_logging_alignment_0_to_10: 1
- first_fix_roi: remove the shim forest first; it is the highest-leverage way to restore real ownership and stop accidental dependency spread
- severity: high
- confidence: very high
- evidence_paths:
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\package.json`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\README.md`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\src\package-info.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\src\billing-entitlement.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\src\billing-support-admin-boundary.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\src\parent-owned-local-export-runtime.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\src\parent-desktop-release-support.ts`
  - `C:\Users\sujan\.codex\worktrees\ocentra-parent-codex-a\OcentraParent\packages\parent-domain\src\tracking-policy-compiler-runtime-proof.ts`
