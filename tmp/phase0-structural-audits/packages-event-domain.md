# packages/event-domain
- target_kind: TypeScript shared contract package for event and protocol identity schemas
- owned_paths: packages/event-domain/package.json, packages/event-domain/src/**, packages/event-domain/tests/**
- declared_responsibility: shared event, message, peer, and route identity contracts for UI-facing schemas and runtime protocol handoff
- observed_responsibility: the package is really an agent-protocol-style contract layer for peer identity, route selection, envelope metadata, and acknowledgement schemas
- should_own: shared branded IDs, protocol envelope metadata, event delivery mode/route shapes, and acknowledgement contracts
- should_not_own: runtime bus behavior, storage, transport execution, logging contracts, or product-specific payload policy
- allowed_dependencies: `@ocentra-parent/schema-domain` only, plus TypeScript type/runtime tooling
- suspicious_dependencies: the package name suggests generic event domain ownership, but the actual contents skew toward agent protocol handoff and runtime envelope semantics
- expected_dependents: Rust protocol mirrors, runtime consumers, and any UI or worker code that needs the shared agent/event contract
- shared_contract_schema_usage: the package correctly centralizes all runtime validation through Effect Schema helpers from `schema-domain`
- duplicate_or_near_duplicate_shapes: `AgentPeer`, `AgentMessageTarget`, `AgentEventEnvelope`, and `AgentEventAcknowledgement` mirror the Rust event-envelope and request-style structures closely enough that drift would be easy to introduce
- id_name_status_drift: the package name reads as generic event-domain, but the actual file contents are closer to agent protocol and route handoff contracts
- direct_import_vs_event_boundary: there is no illegal runtime import pattern here, but the package should stay contract-only and not absorb behavior from the event bus or logging layers
- event_bus_usage: none
- logging_and_proof_chain_expectations: if these contracts are used in proofs, the package should remain schema-only and not accumulate writer, transport, or logging side effects
- boundary_violations: the package mixes peer, message, route, envelope, and acknowledgement shapes in one contract bundle, so it is broader than the name implies
- dry_common_core_candidates: shared peer identity, route identity, envelope metadata, and acknowledgement schemas
- dead_frontage_or_shims: `package-info.ts` is only package-name frontage and does not contribute a separate domain model
- proposed_fix_packets: rename or split if more protocol surfaces are added, and keep any future runtime behavior out of this package so the schema boundary stays clean
- severity: medium
- confidence: medium
- evidence_paths: packages/event-domain/package.json, packages/event-domain/src/eventing.ts, packages/event-domain/src/primitives.ts, packages/event-domain/src/package-info.ts

## Current Refresh Audit - 2026-06-19
- responsibility: contract-only event and protocol identity package; owns shared eventing taxonomy, envelope metadata, peer/message target shapes, acknowledgement shapes, topology manifest, and request completion report schemas.
- deps: `@ocentra-parent/schema-domain` only; no runtime transport, storage, or logging dependency in the package manifest.
- violations: no live barrel or re-export syntax in `packages/event-domain/src`; the main issue is naming and scope drift, not a direct code-level import violation.
- duplicated shapes: `AgentPeer`, `AgentMessageTarget`, `AgentEventEnvelope`, and `AgentEventAcknowledgement` are mirrored closely by `packages/agent-protocol-domain/src/primitives.ts`; `EventingEventContract` / `EventingStoredEnvelopeHeader` also mirror Rust event-envelope and request metadata shapes.
- barrel/reexport/shim debt: `package-info.ts` is a thin frontage shim; `package.json` exposes per-file entrypoints, but the package still presents as a single contract bundle rather than distinct protocol slices.
- schema drift: `eventing.ts` mixes generic event taxonomy with agent-protocol-style envelope and topology metadata, so future drift can appear between `eventing.ts`, `primitives.ts`, and the Rust mirrors that consume them.
- event/log/proof misuse: no runtime logging or event-bus behavior in this package; the risk is indirect misuse by downstream proof or runtime code that treats these schemas as behavior instead of pure contracts.
- test/proof structure issues: only `tests/unit/*.test.ts` are active; the rest of `tests/**`, `proofs/**`, and several category subfolders are empty `.gitkeep` scaffolds, so the package has thin proof structure relative to its contract surface.
- score: 65/100, medium risk.
- fix recommendation: keep this package strictly schema-only, and if more protocol surfaces are added, split the envelope/acknowledgement family from the generic event taxonomy instead of broadening the current bundle.
- decouple recommendation: move any future agent-protocol handoff or runtime-facing shapes into a dedicated protocol package; leave `event-domain` as the canonical shared identity/schema layer only.
- blockers: none in the live package surface; the remaining limitation is sparse test/proof coverage and the package-name-to-responsibility mismatch.
- exact likely paths: `packages/event-domain/src/eventing.ts`, `packages/event-domain/src/primitives.ts`, `packages/event-domain/src/package-info.ts`, `packages/event-domain/tests/unit/eventing.test.ts`, `packages/event-domain/tests/unit/primitives.test.ts`, `packages/event-domain/tests/unit/package-info.test.ts`, `packages/agent-protocol-domain/src/primitives.ts`, `packages/tracking-domain/src/tracking-event-contracts.ts`, `packages/child-runtime-domain/src/child-domain-runtime-events.ts`, `packages/logging-domain/src/contracts.ts`.

## Current Refresh Audit - 2026-06-19
- responsibility: schema-only shared contract package for event, message, peer, route, envelope, topology, and request-completion identity surfaces; the live code is closer to an agent-protocol and event-envelope boundary than a generic event domain.
- dependencies: only `@ocentra-parent/schema-domain` plus TypeScript/runtime tooling in the package manifest; no transport, storage, or logging dependencies are declared here.
- violations: no barrel/re-export syntax appears in `packages/event-domain/src`, and no direct runtime boundary violation is visible in the package surface; the main issue is boundary breadth and naming drift.
- duplicated shapes: `AgentPeer`, `AgentMessageTarget`, `AgentEventEnvelope`, and `AgentEventAcknowledgement` closely parallel `packages/agent-protocol-domain/src/primitives.ts`; `EventingEventContract`, `EventingEnvelopeMetadata`, and `EventingStoredEnvelopeHeader` also shadow Rust-style event-envelope metadata contracts.
- barrel/re-export/shim debt: `package-info.ts` is a thin frontage shim, and `package.json` exposes per-file exports that still present the package as one broad bundle instead of clearly separated contract slices.
- schema drift: `eventing.ts` mixes generic event taxonomy, topology manifests, envelope metadata, and request-completion reporting, so the drift risk is between this package, `packages/agent-protocol-domain`, and any Rust mirrors that model the same metadata.
- event/log/proof misuse: the package itself stays schema-only, but downstream code can easily misuse these schemas as if they were event-bus, logging, or proof-chain behavior rather than pure contracts.
- test/proof structure issues: only `tests/unit/*.test.ts` are active, while the broader `tests/**`, `proofs/**`, and category subfolders remain scaffold-only, so proof depth is thin for the amount of contract surface exposed here.
- dry score: 63/100. Reasoning: the package is internally consistent and dependency-light, but it bundles several adjacent contract families, mirrors sibling shapes, and has shallow proof scaffolding, so the reuse value is moderate rather than high.
- fix recommendation: keep this package strictly schema-only, and if more protocol surfaces appear, split the envelope and acknowledgement family away from generic event taxonomy before the bundle grows further.
- decouple recommendation: move future agent-protocol handoff shapes into a dedicated protocol package and leave `event-domain` as the canonical shared identity/schema layer only.
- blockers: none in the live package; the only practical blocker to a cleaner DRY assessment is the current scope bundling and the sparse proof/test footprint.
- exact likely paths: `packages/event-domain/package.json`, `packages/event-domain/src/eventing.ts`, `packages/event-domain/src/primitives.ts`, `packages/event-domain/src/package-info.ts`, `packages/event-domain/tests/unit/eventing.test.ts`, `packages/event-domain/tests/unit/primitives.test.ts`, `packages/event-domain/tests/unit/package-info.test.ts`, `packages/agent-protocol-domain/src/primitives.ts`, `packages/tracking-domain/src/tracking-event-contracts.ts`, `packages/child-runtime-domain/src/child-domain-runtime-events.ts`, `packages/logging-domain/src/contracts.ts`.
