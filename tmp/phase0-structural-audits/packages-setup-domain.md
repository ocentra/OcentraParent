# packages/setup-domain
- target_kind: TypeScript package
- owned_paths: packages/setup-domain/package.json; packages/setup-domain/src/package-info.ts; packages/setup-domain/src/*.ts
- declared_responsibility: Setup, install, pairing, onboarding, and provisioning contract boundary.
- observed_responsibility: A full setup lifecycle package that spans readiness evaluation, state-machine routing, pairing intent, registration entry, and family-context projection.
- should_own: Canonical setup lifecycle states, readiness report shapes, pairing intent/projection shapes, and setup-state machine routing.
- should_not_own: Production transport, event delivery, or any low-level runtime action beyond contract/projection derivation.
- allowed_dependencies: @ocentra-parent/family-domain; @ocentra-parent/schema-domain.
- suspicious_dependencies: Heavy family-domain usage is expected, but it pulls the package close to the family lifecycle owner and makes drift against provisioning-core likely.
- expected_dependents: setup UI, provisioning/projection code, pairing flows, and tests or proofs that need setup lifecycle contracts.
- shared_contract_schema_usage: Shares the same readiness/recovery/pairing schema vocabulary as the family domain and strongly mirrors the Rust provisioning core.
- duplicate_or_near_duplicate_shapes: `readiness.ts`, `setup-state-machine.ts`, `pairing-intent.ts`, `registration-entry.ts`, and `family-setup-bridge.ts` repeat the same state taxonomy in different projections.
- id_name_status_drift: The package name says setup-domain, but the actual surface also includes provisioning, registration, recovery, and projection logic.
- direct_import_vs_event_boundary: Direct contract imports only; there is no event bus or transport boundary here.
- event_bus_usage: None.
- logging_and_proof_chain_expectations: Readiness and recovery projections should remain deterministic because downstream proofs likely depend on their exact state strings and checklist order.
- boundary_violations: The package overlaps heavily with crates/provisioning-core on readiness and recovery ownership, so the canonical owner is not obvious from the package split alone.
- dry_common_core_candidates: A shared setup/provisioning lifecycle core with crates/provisioning-core and maybe family-domain setup lifecycle helpers.
- dead_frontage_or_shims: `src/package-info.ts` is only a name shim; the rest of the package is active.
- proposed_fix_packets: Consolidate the repeated setup lifecycle taxonomy into one canonical module set, then make the other side a projection-only layer.
- severity: high
- confidence: high
- evidence_paths: packages/setup-domain/package.json; packages/setup-domain/src/package-info.ts; packages/setup-domain/src/readiness.ts; packages/setup-domain/src/setup-state-machine.ts; packages/setup-domain/src/pairing-intent.ts; packages/setup-domain/src/registration-entry.ts; packages/setup-domain/src/family-setup-bridge.ts

## Current Refresh Audit - 2026-06-19
- responsibility: This package is still acting as the setup/provisioning contract boundary, but the observed surface is broader than the declared setup-only scope because it also carries registration, recovery, projection, and lifecycle routing concerns.
- dependencies: The package leans on `@ocentra-parent/family-domain` and `@ocentra-parent/schema-domain`; that dependency set is plausible for contracts, but it also pulls the package toward family lifecycle ownership and makes it easy to drift into provisioning-core territory.
- boundary_violations: The main boundary risk is duplicated lifecycle ownership with `crates/provisioning-core`, where readiness, recovery, and setup-state taxonomy appear to be represented on both sides without a clean projection-only split.
- duplicated_shapes: `readiness.ts`, `setup-state-machine.ts`, `pairing-intent.ts`, `registration-entry.ts`, and `family-setup-bridge.ts` appear to repeat the same setup-state vocabulary in different wrappers instead of sharing one canonical model.
- barrel_reexport_shim_debt: `src/package-info.ts` reads as a name shim, and the broader package shape suggests it may be masking the real ownership split instead of making it explicit.
- schema_drift: The package name suggests a narrower setup domain, while the actual exported surface includes provisioning, recovery, registration, and family-context projection, which is a schema and ownership drift signal.
- event_log_proof_misuse: No event bus is present here, but the package still participates in downstream proof expectations through deterministic readiness and recovery state strings, so any non-canonical reshaping would mislead proof consumers even without transport code.
- test_proof_structure_issues: The package’s contract surface is fragmented across several near-duplicate modules, which makes it harder to pin one authoritative contract/proof path and increases the chance of tests asserting the wrong projection shape.
- dry_score: 28/100. The score is low because the package contains multiple overlapping lifecycle projections, duplicated state taxonomies, and an unclear canonical boundary relative to provisioning-core. The score is not lower because the package still appears contract-focused rather than runtime-heavy.
- fix_recommendation: Collapse the repeated setup lifecycle taxonomy into one canonical contract module set and make every other file a thin projection or adapter over that shared core.
- decouple_recommendation: Separate setup lifecycle contracts from provisioning implementation ownership so this package owns only the stable setup boundary, while provisioning-core owns runtime lifecycle behavior and state transitions.
- blockers: The main blocker is ownership ambiguity across the TypeScript package and `crates/provisioning-core`; without a canonical owner decision, any cleanup risks just moving the duplication instead of removing it.
- exact_likely_paths: `packages/setup-domain/src/readiness.ts`; `packages/setup-domain/src/setup-state-machine.ts`; `packages/setup-domain/src/pairing-intent.ts`; `packages/setup-domain/src/registration-entry.ts`; `packages/setup-domain/src/family-setup-bridge.ts`; `packages/setup-domain/src/package-info.ts`; `crates/provisioning-core`; `packages/family-domain`.

## Current Refresh Audit - 2026-06-19
- responsibility: `packages/setup-domain` still acts as the setup/install/pairing/onboarding contract boundary, but the actual source surface extends into registration, recovery, projection, and state-routing logic.
- dependencies: The package depends on `@ocentra-parent/family-domain` and `@ocentra-parent/schema-domain`; that is legitimate for contract work, but it also keeps pulling family lifecycle and recovery ownership into this package.
- violations: The main structural violation is overlapping lifecycle ownership with `crates/provisioning-core`, plus package scope drift from "setup domain" into registration and recovery orchestration.
- duplicated_shapes: `readiness.ts`, `setup-state-machine.ts`, `pairing-intent.ts`, `registration-entry.ts`, and `family-setup-bridge.ts` all model the same setup lifecycle in slightly different projections instead of a single shared canonical taxonomy.
- barrel_reexport_shim_debt: There are no obvious TS barrel files in the source boundary, but `package.json` exposes a wildcard export surface and `src/package-info.ts` is a pure name shim, so the package still has alias/shim debt even without re-export syntax.
- schema_drift: The package name implies a narrower setup-only contract, while the live surface includes provisioning, recovery, registration, and family-context projection, which is a clear ownership and schema drift signal.
- event_log_proof_misuse: There is no runtime event bus here, but the readiness and recovery state strings are used as proof-critical contract outputs; if these shapes diverge, downstream proof consumers can be misled even without transport code.
- test_proof_structure_issues: The contract surface is split across several near-duplicate modules, which makes it harder to assign one authoritative test/proof source of truth and increases the chance of validating a projection instead of the canonical shape.
- dry_score: 30/100. The score is still low because the package repeats the same lifecycle taxonomy across multiple modules and does not cleanly separate setup contracts from provisioning ownership. It is not lower only because the package remains mostly contract/projection code rather than runtime transport logic.
- fix_recommendation: Collapse the repeated setup lifecycle taxonomy into one canonical contract module set, then make the remaining files thin adapters or projections over that shared core.
- decouple_recommendation: Split stable setup contracts from provisioning behavior so this package owns only setup boundary types and projections, while provisioning-core owns lifecycle execution and transition logic.
- blockers: The blocking issue is unresolved ownership between the TypeScript package and `crates/provisioning-core`; until one side is declared canonical, cleanup can only reshuffle duplication.
- exact_likely_paths: `packages/setup-domain/package.json`; `packages/setup-domain/src/package-info.ts`; `packages/setup-domain/src/readiness.ts`; `packages/setup-domain/src/setup-state-machine.ts`; `packages/setup-domain/src/pairing-intent.ts`; `packages/setup-domain/src/registration-entry.ts`; `packages/setup-domain/src/family-setup-bridge.ts`; `crates/provisioning-core`; `packages/family-domain`.
