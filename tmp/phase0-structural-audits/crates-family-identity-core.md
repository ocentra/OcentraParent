# crates/family-identity-core
- target_kind: rust crate
- owned_paths: crates/family-identity-core/src/lib.rs; crates/family-identity-core/src/family_identity.rs; crates/family-identity-core/src/household_authority.rs; crates/family-identity-core/src/setup_lifecycle.rs; crates/family-identity-core/src/session_lifecycle.rs; crates/family-identity-core/tests/unit.rs; crates/family-identity-core/tests/unit/device_scope.rs; crates/family-identity-core/tests/unit/household_authority.rs; crates/family-identity-core/tests/unit/setup_lifecycle.rs; crates/family-identity-core/tests/unit/session_lifecycle.rs
- declared_responsibility: household membership, child/profile/device role contracts, local authorization decisions, invite/recovery state, and device-ownership checks shared by parent and child runtimes
- observed_responsibility: a broad Rust common-core for family actor roles, device scope evaluation, household authority, setup invite/recovery flow, session token lifecycle, and event-contract/idempotency helpers
- should_own: canonical Rust-side family/device authorization enums and decision structs; event contract wrappers for device-scope evaluation/decision; setup/recovery/session lifecycle decisions that must be shared by Rust runtimes
- should_not_own: package-export frontage; sibling package shape duplication; unrelated product feature behavior outside family/device/session contracts; direct UI or portal semantics
- allowed_dependencies: ocentra-eventing envelope/error/ids; serde
- suspicious_dependencies: crate-local duplication of TS domain semantics; `pub use` re-export frontage in `src/lib.rs`; stringly typed text-id macro inside the crate rather than a shared id helper; broad action matrix that mirrors package-domain logic
- expected_dependents: Rust service code, protocol adapters, and tests that need the canonical family/device/session authority contracts
- shared_contract_schema_usage: none on TS Effect Schema; Rust uses serde-enums and eventing contracts instead of a shared schema layer
- duplicate_or_near_duplicate_shapes: household authority action matrices mirror `packages/family-domain/src/household-authority.ts`; setup invite/recovery decisions mirror `packages/family-domain/src/setup-lifecycle.ts`; session token and issuance decisions mirror `packages/family-domain/src/session-lifecycle.ts`
- id_name_status_drift: `DeviceScopeDecisionId` and related text IDs are locally macro-built strings; the crate owns the prefixes but not a reusable branded-id layer
- direct_import_vs_event_boundary: direct imports are used for shared Rust contracts, which is fine; cross-responsibility runtime behavior stays in function calls rather than event-driven boundaries, so there is no real event bus seam here
- event_bus_usage: no bus; only `DomainEvent` impls for device-scope evaluation/decision events
- logging_and_proof_chain_expectations: this crate participates in a command/event/request chain and should stay logger-ready if runtime behavior is extended; current file set is mostly pure contract/decision logic, with no visible proof-chain instrumentation
- boundary_violations: `pub use` barrel in `src/lib.rs`; shared household/session/setup semantics duplicated with the TypeScript family-domain package; text-ID macro is a local helper that can hide naming drift across crates
- dry_common_core_candidates: a shared family-authorization core trait/module for action matrices; a shared text-id/newtype helper; a shared setup/recovery/session decision catalog for Rust and TypeScript
- dead_frontage_or_shims: `src/lib.rs` is pure re-export frontage; it does not add ownership and makes the crate look flatter than its actual module layout
- proposed_fix_packets: remove barrel frontage and expose modules explicitly; decide whether the family/domain authority model should be unified across Rust and TypeScript via one canonical contract source; extract the repeated text-id helper into a named shared id utility if the crate keeps owning string IDs
- severity: high
- confidence: high
- evidence_paths: crates/family-identity-core/src/lib.rs; crates/family-identity-core/src/family_identity.rs; crates/family-identity-core/src/household_authority.rs; crates/family-identity-core/src/setup_lifecycle.rs; crates/family-identity-core/src/session_lifecycle.rs; crates/family-identity-core/Cargo.toml

## Current Refresh Audit - 2026-06-19

- responsibility: owns household membership, child/profile/device role contracts, local authorization decisions, invite and recovery state, session lifecycle authorization, and device-scope checks shared by parent and child runtimes.
- deps: `ocentra-eventing` plus `serde`; there is no broader runtime dependency surface in the crate manifest.
- violations: no direct syntax or lint violations were visible in the owned surface, but the crate still carries duplicated family-authorization semantics relative to the TypeScript family-domain package.
- duplicated shapes: `household_authority.rs`, `session_lifecycle.rs`, and `setup_lifecycle.rs` all mirror matching TS contract shapes in `packages/family-domain/src/household-authority.ts`, `packages/family-domain/src/session-lifecycle.ts`, and `packages/family-domain/src/setup-lifecycle.ts`.
- barrel/reexport/shim debt: none observed in the current snapshot. `src/lib.rs` is module frontage only (`pub mod ...`), not a `pub use` barrel; the local `family_identity_text_id!` macro is a helper, not a reexport shim.
- schema drift: the crate uses serde enum/string contracts directly while the TS family-domain side uses its own Effect Schema-backed shapes, so the cross-language contract source is still split and can drift on names, states, or decision wording.
- event/log/proof misuse: `family_identity.rs` correctly implements `DomainEvent` for device-scope request/decision records, but there is no logger/proof-chain instrumentation in this crate; that is acceptable for pure contract logic, but runtime behavior should not be layered here without proof plumbing.
- test/proof structure issues: coverage is unit-only under `tests/unit.rs`; there is no crate-local contract/proof matrix, no integration proof harness, and no explicit cross-language parity test that locks the Rust and TS shapes together.
- score: 76/100. Structurally solid and narrow in dependency surface, but still over-broad as a duplicated canonical family-contract source.
- fix recommendation: keep this crate as the Rust canonical contract owner for family identity and device-scope decisions, but remove any lingering surface flattening, then add a single source-of-truth contract map or generated parity check for the mirrored family-domain shapes.
- decouple recommendation: if Rust and TypeScript must remain independently owned, split the duplicated authority/session/setup decision catalogs into a smaller shared id-and-enum boundary and let each runtime own only its local decision wiring.
- blockers: no blocker for the audit refresh itself; the main unresolved question is whether the TS family-domain package or this Rust crate should be the canonical authority contract source.
- exact likely paths: `crates/family-identity-core/src/family_identity.rs`; `crates/family-identity-core/src/household_authority.rs`; `crates/family-identity-core/src/session_lifecycle.rs`; `crates/family-identity-core/src/setup_lifecycle.rs`; `crates/family-identity-core/src/lib.rs`; `crates/family-identity-core/tests/unit.rs`; `crates/family-identity-core/tests/unit/device_scope.rs`; `crates/family-identity-core/tests/unit/household_authority.rs`; `crates/family-identity-core/tests/unit/session_lifecycle.rs`; `crates/family-identity-core/tests/unit/setup_lifecycle.rs`; `packages/family-domain/src/household-authority.ts`; `packages/family-domain/src/session-lifecycle.ts`; `packages/family-domain/src/setup-lifecycle.ts`.

## Current Refresh Audit - 2026-06-19

- responsibility: this crate owns the Rust-side family identity boundary for household membership, child/profile/device role contracts, authorization decisions, invite/recovery/session lifecycle state, and device-ownership evaluation shared by parent and child runtimes.
- dependencies: manifest scope is narrow, with only `ocentra-eventing` and `serde`; crate boundary is `crates/family-identity-core/Cargo.toml` plus `crates/family-identity-core/src/lib.rs`, and there is no crate-local README or rules file in the module root.
- violations: no direct Rust syntax or dependency violation is visible in the boundary files read, but the crate still duplicates canonical family-contract semantics that also exist in the TypeScript family-domain package.
- duplicated shapes: `household_authority.rs`, `setup_lifecycle.rs`, and `session_lifecycle.rs` mirror TS shapes in `packages/family-domain/src/household-authority.ts`, `packages/family-domain/src/setup-lifecycle.ts`, and `packages/family-domain/src/session-lifecycle.ts`.
- barrel/re-export/shim debt: no `pub use` barrel was present in `src/lib.rs`; current frontage is explicit `pub mod` declarations only, so the remaining debt is semantic duplication rather than export flattening.
- schema drift: Rust uses serde-backed enum/state contracts while TS uses Effect Schema-backed domain shapes, so names, state transitions, and decision wording can drift unless one side becomes canonical or a parity check is added.
- event/log/proof misuse: no misuse is visible in the read boundary; `family_identity.rs` remains a domain-event carrier and the crate does not show ad hoc logging or proof-chain code, but it should not grow runtime behavior without logger/proof coverage.
- test/proof structure issues: validation is unit-only under `tests/unit.rs` and per-topic unit files; there is no local contract matrix, no cross-language parity proof, and no integration-level check binding the Rust shapes to the TS family-domain source.
- score: 75/100. The crate is structurally clean and dependency-light, but it still functions as a duplicated family-contract source rather than a single canonical owner.
- fix recommendation: keep this crate as the Rust canonical contract boundary for family identity and device-scope decisions, then add a parity or generation bridge so the mirrored family-domain shapes cannot silently diverge.
- decouple recommendation: if Rust and TypeScript must stay separately owned, extract a smaller shared enum/id core for family role and lifecycle decisions, and let each runtime own only its local wiring and presentation.
- blockers: none for the audit refresh itself; the only open design blocker is canonical ownership between this crate and `packages/family-domain`.
- exact likely paths: `crates/family-identity-core/Cargo.toml`; `crates/family-identity-core/src/lib.rs`; `crates/family-identity-core/src/family_identity.rs`; `crates/family-identity-core/src/household_authority.rs`; `crates/family-identity-core/src/setup_lifecycle.rs`; `crates/family-identity-core/src/session_lifecycle.rs`; `crates/family-identity-core/tests/unit.rs`; `crates/family-identity-core/tests/unit/device_scope.rs`; `crates/family-identity-core/tests/unit/household_authority.rs`; `crates/family-identity-core/tests/unit/setup_lifecycle.rs`; `crates/family-identity-core/tests/unit/session_lifecycle.rs`; `packages/family-domain/src/household-authority.ts`; `packages/family-domain/src/setup-lifecycle.ts`; `packages/family-domain/src/session-lifecycle.ts`.
