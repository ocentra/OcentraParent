Contract proof:

- Added `packages/parent-domain/src/app-game-ai-classifier-boundary-values.ts`.
- Added `packages/parent-domain/src/app-game-ai-classifier-boundary.ts`.
- Added `packages/parent-domain/src/app-game-ai-classifier-boundary-data.ts`.
- Added `packages/parent-domain/tests/app-game-ai-classifier-boundary.test.ts`.
- Existing activity-domain digest proof remains the source spine and is run by
  the proof harness without editing the locked package.

Assertions covered:

- classifier output must cite stored evidence refs;
- confidence must stay within 0..1;
- direct action authority is rejected;
- forbidden action, duration, and raw scan fields are rejected before policy;
- provider-unavailable output must carry a fallback state;
- low-confidence fallback must stay low confidence.
