N/A for WP21.

This workpack is TypeScript parent-domain and text-domain contract proof only.
No Rust protocol payloads, `crates/agent-protocol` structs, `agent-core`
runtime helpers, or `agent-service` WebSocket/read-model paths were changed.

Rust/service parity remains a later work item before child-facing UX states can
cross the runtime boundary.
