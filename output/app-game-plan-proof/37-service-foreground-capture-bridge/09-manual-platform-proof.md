# Manual Platform Proof

WP37 does not claim product-complete platform foreground capture.

The service bridge preserves the WP36 optional source boundary: if the
active-window adapter is unavailable or unsupported, no app/game foreground
event is appended.

Manual platform proof is still required for product support claims.
