{
  "workpack": "11-cross-platform-authority-matrix",
  "policyExecutionChanged": false,
  "provedGuards": [
    "observe-only hard-control rows cannot execute adapters",
    "manual-required rows cannot execute adapters",
    "not-claimed rows cannot execute adapters",
    "hard-control rows require rollback proof before executable supported claims",
    "bare unsupported copy is rejected in parent-visible limitation fields"
  ],
  "manualRequiredBoundary": "The matrix names proof needed to claim; it does not dispatch policy actions."
}
