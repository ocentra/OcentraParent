# Manual Platform Proof

No manual platform behavior was claimed.

Rows stronger than observe-only remain proof-gated by contract until the
relevant workpack attaches real OS/device version, permission/enrollment setup,
commands or UI steps, screenshots/logs, rollback, cleanup, and audit evidence.

Manual-required platform examples still requiring real proof:

- Windows AppLocker or App Control broad block/allowlist.
- macOS MDM or Endpoint/System Extension hard block.
- Linux root/admin service with cgroup/systemd/AppArmor/SELinux mechanism,
  distro, and session proof.
- Android Device Owner/Profile Owner hide/suspend.
- iOS FamilyControls/ManagedSettings shield and supervised/MDM modes.
