WP06 Rust protocol and adapter proof

Passed:
- cargo test -p ocentra-parent-agent-protocol app_game
  Result: 4 tests passed.
- cargo test -p ocentra-parent-agent-core app_game_windows_inventory
  Result: 5 tests passed.
- cargo check -p ocentra-parent-agent-core
  Result: passed without warnings after the staged parser module was scoped with a dead_code allow until live source readers call it.

Protocol coverage:
- AppGameInventoryEvidenceRow serializes with camelCase field names.
- sourceKind, custodyState, productKind, classificationState, catalogReadyState,
  capabilityStatus, and categoryCandidates preserve WP05 value strings.
- runtimeState and foregroundState remain notClaimed.
- runningDurationMs and foregroundDurationMs remain 0.

Adapter/parser coverage:
- registry-like records produce nativeApp inventory rows;
- Start Menu shortcut records produce nativeApp inventory rows;
- launcher manifest records produce nativeGame inventory rows with game category candidates;
- registry and shortcut rows dedupe only by shared strong identity refs;
- matching display labels alone do not merge;
- installed inventory rows never become runtime or foreground use rows.
