# Authority Tier Proof

The new read-model event reports existing staged authority rows only.

It does not promote any platform authority tier, setup state, proof state, or
adapter capability. Manual-required and not-claimed rows remain non-executable
until separate platform proof exists.
