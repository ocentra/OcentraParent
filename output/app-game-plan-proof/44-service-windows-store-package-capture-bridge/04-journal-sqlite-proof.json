{
  "workpack": "44-service-windows-store-package-capture-bridge",
  "test": "activity_capture_tests::inventory::record_capture_with_store_package_root_writes_inventory_journal_and_sqlite_rows",
  "journal": "encrypted ActivityJournal append path",
  "store": "ActivityStore SQLite ingest path",
  "assertions": [
    "store package manifest emits an inventory journal event",
    "read model returns one inventory row",
    "source kind is storePackage",
    "package id and app user model id survive projection",
    "runtime state remains not claimed",
    "foreground state remains not claimed"
  ]
}
