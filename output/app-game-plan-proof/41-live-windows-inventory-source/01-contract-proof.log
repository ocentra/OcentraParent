Contract proof:

- Reuses existing `WindowsInstalledAppInventoryRecord` and `AppGameInventoryEvidenceRow`.
- Adds protocol-owned constants for source refs, desktop-entry refs, shortcut extension, and Windows Start Menu path segments.
- New rows keep `runtime_state=notClaimed`, `foreground_state=notClaimed`, and zero durations.
- Raw shortcut paths are hashed into source and desktop-entry refs before row projection.

Validation:

- `cargo test -p ocentra-parent-agent-core live_inventory`
