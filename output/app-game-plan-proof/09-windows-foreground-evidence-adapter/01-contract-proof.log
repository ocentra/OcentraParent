Command:
cmd /c npm run test --workspace @ocentra-parent/activity-domain -- app-game-foreground.test.ts

Result:
PASS packages/activity-domain/tests/app-game-foreground.test.ts
Test Files 1 passed
Tests 6 passed

Covered behavior:
- accepts active foreground focus evidence with runtime running state;
- requires background rows to close foreground intervals;
- keeps background processes from gaining foreground duration;
- allows omitted title refs without content claims;
- preserves permission-limited foreground state;
- rejects foreground evidence that claims content knowledge.

Schema-boundary/source-shape:
Final `npm run lint:schema-boundaries` and `npm run precommit` results are
recorded in `10-validation-commands.log`.
