Rust protocol proof for WP10:

- Added `AppGameLauncherEvidenceRow` to `crates/agent-protocol/src/app_game.rs`.
- Added launcher kind, proof-state, and test constants to the Rust protocol
  boundary.
- Added serialization proof in `crates/agent-protocol/src/app_game_tests.rs`.

Focused Rust protocol validation:
- cargo test -p ocentra-parent-agent-protocol app_game_launcher_evidence_row_serializes_launcher_without_game_claim
  Result: PASS, 1 test.

Protocol behaviors proved:
- serde emits the TypeScript-compatible `camelCase` launcher row;
- launcher-only proof carries `knownLauncher`;
- child game evidence claim is null for launcher-only proof;
- launcher process fields remain explicit Rust-crossing fields.
