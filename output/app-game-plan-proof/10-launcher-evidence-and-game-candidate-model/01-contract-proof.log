Contract proof for WP10:

- Added `packages/activity-domain/src/app-game-launcher.ts`.
- Exported launcher evidence contracts through `packages/activity-domain/src/app-game.ts`.
- Added `packages/activity-domain/tests/app-game-launcher.test.ts`.

Focused TypeScript validation:
- cmd /c npm run test --workspace @ocentra-parent/activity-domain -- app-game-launcher
  Result: PASS, 1 test file, 4 tests.

Behaviors proved:
- launcher-only evidence parses as `knownLauncher`;
- launcher-game candidates parse as `launcherGameCandidate`;
- known-game classification is rejected unless a child-game evidence claim is
  present with deterministic or classifier-backed child-game proof state;
- permission-limited launcher evidence stays explicit with zero confidence.

No-claim coverage:
- launcher installed/running/foreground evidence does not become game proof;
- child-process candidate is not known game;
- known game requires child-game evidence.
