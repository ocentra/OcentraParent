Rust protocol proof:

- Added `constants::activity_capture::RECURRING_CAPTURE_INTERVAL_MS`.
- Added test-only activity store suffix constants for the recurring freshness
  proof files.
- No new serialized Rust-crossing payload shape was introduced.

Expected validation:

- `cargo test -p ocentra-parent-agent-protocol app_game`
- `cargo test -p ocentra-parent-agent-service activity_capture`
