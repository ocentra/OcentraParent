No TypeScript contract files changed in WP35.

Contract boundary proof:

- The recurring cadence is owned by `crates/agent-protocol` constants.
- The service uses the existing `ActivityIngestStatus` and
  `AppGameServiceReadModel` structs.
- No new app/runtime naked strings, Zod schemas, manual brands, or portal
  domain contracts were introduced.

Focused validation will include `npm run lint:schema-boundaries`.
