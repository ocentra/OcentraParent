Rust protocol changes:

- Added `APP_GAME_RUNTIME_EVIDENCE_ID_PREFIX`.
- Added `APP_GAME_EXECUTABLE_PATH_REF_PREFIX`.
- Core process-source code consumes those constants instead of inventing local
  runtime strings.

Focused validation:

`cargo test -p ocentra-parent-agent-core app_game_windows_process`

Result: PASS, 9 tests.
