WP36 does not add TypeScript contracts.

Rust protocol constants added for core source refs:

- APP_GAME_FOREGROUND_EVIDENCE_ID_PREFIX
- APP_GAME_WINDOW_REF_PREFIX
- APP_GAME_WINDOW_TITLE_REF_PREFIX
- TEST_LIVE_FOREGROUND_SUFFIX

Contract boundary:

- foreground evidence remains distinct from runtime evidence
- foreground evidence remains distinct from content knowledge
- title refs are opaque evidence refs, not title capture
- unsupported or unavailable active-window adapter state returns no event
- no policy decision, action result, adapter execution, or platform support claim is introduced

Focused protocol validation:

- cargo test -p ocentra-parent-agent-protocol app_game: passed, 20 tests
