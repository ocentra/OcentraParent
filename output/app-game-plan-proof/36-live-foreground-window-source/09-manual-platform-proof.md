# Manual Platform Proof

WP36 does not claim product-complete Windows foreground capture.

The Windows active-window adapter is optional at runtime. If the adapter is
unavailable or the platform is unsupported, the source returns no event instead
of promoting a false support claim.

Product-level proof still requires service-owned capture wiring, manual platform
capture evidence, portal freshness rows, policy consumers, and adapter authority
evidence.
