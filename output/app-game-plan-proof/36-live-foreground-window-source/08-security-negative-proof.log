Security and privacy no-claim checks:

- Active-window title metadata is hashed into an opaque title ref.
- Window identity metadata is hashed into an opaque window ref.
- Raw process paths are not stored by the foreground source.
- Foreground evidence is not content knowledge.
- Unsupported or unavailable active-window adapter state produces no event.
- No service capture loop, portal display, policy decision, adapter execution,
  or broad blocking path is introduced.

Focused validation:

- git diff --check: passed
- cmd /c npm run lint:schema-boundaries: passed with existing source-shape warnings only
