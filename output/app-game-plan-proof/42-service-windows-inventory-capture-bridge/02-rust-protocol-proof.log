Rust protocol proof:

- `APP_GAME_INVENTORY_SNAPSHOT_LIMIT` defines the service capture bound.
- Inventory journal events continue to use the existing app/game inventory row
  shape and journal row-kind fields.
- No new app/game event kind was introduced; inventory remains projected through
  the existing journal row-kind contract.

Validation:

- cargo test -p ocentra-parent-agent-protocol app_game
