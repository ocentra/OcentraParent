Security and no-claim negative proof:

- PASS: inventory evidence-claim rows are rejected if they claim runtime or
  foreground use.
- PASS: observe-only or inactive approval authority rows are rejected if they
  grant approve, deny, extend, or override capability.
- PASS: manual-required action-result rows are rejected if they include an
  enforcement result.
- PASS: manual-required platform authority rows are rejected if they can execute
  adapters.
- PASS: classifier result rows are rejected if they request direct action,
  include raw scan fields, or include content claims.

No content capture, direct AI enforcement, broad blocking, private path leak, or
platform support claim is introduced by WP31.
