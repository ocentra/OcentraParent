Rust protocol proof:

- `cargo test -p ocentra-parent-agent-protocol app_game`
- Result: PASS, 20 tests passed.

Protocol changes covered by the test run:

- `AppGameServiceReadModel` serializes the new returned-count fields.
- `AppGameServiceReadModel` carries evidence claim, identity, approval
  authority, approval action-result, platform authority matrix, and classifier
  result row vectors.
- The WP29 and WP30 protocol serialization tests still pass.
- The new persistence-state constant is exported from the shared protocol crate
  instead of being inlined in `agent-core`.
