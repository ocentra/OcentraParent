Command:
cmd /c npm run --workspace @ocentra-parent/parent-domain test -- native-game-budget-policy

Result:
PASS packages/parent-domain/tests/native-game-budget-policy.test.ts
Test Files: 1 passed
Tests: 4 passed

Covered contract claims:
- Known game sessions count toward native game budgets.
- Launcher-only sessions are excluded by default.
- Launcher-game candidates count only when the parent policy explicitly allows
  parent-approved candidates.
- Rating, UGC, multiplayer, and purchase signals cannot carry direct
  enforcement role.
- Time-limit dry-runs require counted gameplay over budget.
- Decisions remain dry-run only and do not request enforcement handoff.

Negative cases:
- Counting launcher-only rows fails decode.
- Counting launcher-game candidates while candidate policy excludes them fails
  decode.
- Rating signals marked direct-enforcement fail decode.
- Non-dry-run decisions fail decode.
- Signal-only time-limit decisions fail decode.
