WP07 TypeScript contract proof

No TypeScript contracts were changed in WP07. The Rust parser uses the existing
WP05 inventory evidence row contract and WP06 Rust inventory row parity.

Passed:
- cmd /c npm run test --workspace @ocentra-parent/activity-domain -- app-game.test.ts app-game-evidence-claim.test.ts app-game-identity.test.ts app-game-inventory.test.ts
  Result: 4 files passed, 22 tests passed.

Contract coverage reused:
- storePackage source rows require at least packageId, bundleId,
  appUserModelId, or storeId;
- high-confidence rows require deterministic identity refs;
- inventory rows must not claim runtime or foreground use;
- store package rows remain distinct from Win32 display-name matching.
