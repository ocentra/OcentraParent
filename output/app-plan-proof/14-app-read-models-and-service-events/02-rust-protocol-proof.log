# Rust Protocol Proof

See shared app/game proof:

```text
output/app-game-plan-proof/15-read-models-and-service-events/02-rust-protocol-proof.log
```

Native app protocol coverage:

- `AppGameServiceReadModel` carries replayed inventory, runtime, foreground,
  launcher, and daily rollup rows.
- `ActivityAppUseReadModelRow` serializes typed native app state for activity
  surface consumers.
- `crates/agent-service/src/activity_surface_read_models.rs` derives app-use
  rows from app-classified projection rows only.

Commands passed:

```text
cargo test -p ocentra-parent-agent-protocol app_game -- --nocapture
cargo test -p ocentra-parent-agent-protocol activity_surface -- --nocapture
```
