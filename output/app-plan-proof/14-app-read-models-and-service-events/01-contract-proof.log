# Contract Proof

See shared app/game proof:

```text
output/app-game-plan-proof/15-read-models-and-service-events/01-contract-proof.log
```

Native app contract coverage:

- `ActivityAppUseReadModelRow` now carries native app state fields from the
  app-game projection.
- App-use rows keep `inventoryState`, `runtimeState`, and `foregroundState`
  separate.
- `dailyRollupCount`, `inventoryRowCount`, `runningRowCount`, and
  `foregroundRowCount` expose source shape without implying stronger evidence.
- `capabilityStatus` remains explicit for stale, degraded, manual-required, and
  not-claimed style states.

Commands passed:

```text
cmd /c npm run test --workspace @ocentra-parent/activity-domain -- activity-surface
cmd /c npm run build:contracts
cmd /c npm run test --workspace @ocentra-parent/agent-protocol-domain -- activity-surface-adapter
```
