# UI Not Applicable

No app-plan UI files changed. Parent-visible manual-required reason proof is
contract-level in `AppGameBroadBlockingGateMatrix`.

Future native app UI work must render the manual-required, unavailable, and
not-claimed reason strings from service-backed capability state before claiming
portal UI proof.
