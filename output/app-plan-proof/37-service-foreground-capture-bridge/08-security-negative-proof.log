Native app security no-claim checks:

- Optional foreground rows are evidence only.
- Foreground remains separate from content.
- No policy, adapter, notification, or broad blocking path is added.

Focused validation:

- git diff --check: passed
- cmd /c npm run lint:schema-boundaries: passed with existing source-shape warnings only
