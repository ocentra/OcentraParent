# Manual Platform Proof

Native app WP32 uses the real local `sysinfo` process table and asserts the
current process appears exactly once.

No manual platform setup, privilege tier, or adapter execution is claimed.
