# Manual Platform Proof

WP45 is Windows-only native app inventory evidence. On unsupported platforms,
the default source returns an empty event list.

This does not promote product-wide platform support, app blocking, policy
execution, or adapter authority.
