App-plan WP42 contract proof:

- Native app inventory rows can now enter the service journal/store path from
  bounded Windows shortcut inventory events.
- Rows remain inventory-only and do not claim runtime or foreground usage.
- The native app proof is cross-recorded from shared app/game WP42.

Validation:

- cargo test -p ocentra-parent-agent-service activity_capture
