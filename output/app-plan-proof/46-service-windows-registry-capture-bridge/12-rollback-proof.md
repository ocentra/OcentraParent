# WP46 Rollback Proof

Rollback is not applicable to WP46 because no policy or adapter action is
executed.

If this source is disabled, the service still has the existing process,
foreground, shortcut inventory, Store-package inventory, and network capture
paths. Registry rows are additive native app inventory evidence and are not
required for timer rollback or adapter rollback behavior.
