# Source Snapshot

Native app WP39 cross-records the shared app/game read-model count exposure.

The native app plan receives explicit staged boundary counts through the
existing app-use and games service payloads. This does not add native app
classifier execution, policy consumption, portal rows, adapter execution, or
platform support.
