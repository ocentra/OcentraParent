# AI-25 Security Negative Proof

Rejected or guarded states:

- missing AI proof directory;
- missing AI implementation-checklist row;
- checklist row without the expected `[x]` or `[~]` status;
- checklist row without `codex-d` ownership;
- checklist row without its proof-directory reference;
- proof pack without source snapshot, security negative proof, or validation log;
- proof pack without a `01-*` proof artifact;
- proof pack without `ui-not-applicable.md` where applicable;
- URL/video AI plan missing critical no-claim guard text.

Security boundary:

AI-25 proves proof-pack and rollout guard coverage. It does not run AI models,
call remote providers, inspect raw browser content, evaluate policy, deliver UI,
mutate browser runtime, or execute enforcement.
