AI-19 contract proof

Passed:

- cmd /c npm run build:contracts
- cmd /c npm run test --workspace @ocentra-parent/activity-domain -- browser-ai-child-ux.test.ts
- cmd /c npm run test --workspace @ocentra-parent/text-domain -- browser-child-ux.test.ts

Existing contract coverage:

- Accepts checking hold snapshots with calm text tokens and adapter proof.
- Accepts warning snapshots linked to a post-analysis action.
- Rejects raw, shaming, or surveillance copy claims.
- Rejects rendered child pages without adapter proof.
- Rejects state/token mismatches.
- Rejects warning states without a matching post-analysis action.
- Accepts unclassified fallback without a rendered child UI claim.
