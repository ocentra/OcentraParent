# screen-plan Instruction

## Verdict

`partial / false-green`. Real screen code exists, but docs say 100/100 while many workpacks are open; proof roots are missing and Rust tests are inline.

## Assign first

`screen-slice-a-truth-and-proof-contract-repair`:

- repair PLAN_STATE/WORKPACK/proof-contract mismatch;
- establish `docs/proof/screen-plan/*` and `output/screen-plan-proof/<proof-id>/` manifest rules;
- identify stale parent-domain screen/tracking shims;
- do not change runtime behavior yet.

## Then

1. stale shim removal/retargeting;
2. Rust screen test relocation to crate `tests/` categories;
3. Windows/Android/Linux proof regeneration;
4. final closure audit after tracking and sibling proof surfaces are current.

## Coordinate with

- `tracking-plan` first for tracking retention/event dependencies.
- `screen-ai-pipeline-plan` and `ai-plan` for derived AI claims.
- `policy-control-plane-plan` and enforcement only for policy/action rows.

## Do not

- Do not count 100/100 checklist as truth.
- Do not close from missing proof roots.
- Do not claim macOS/iOS local proof from Windows host.
