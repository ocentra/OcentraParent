# screen-ai-pipeline-plan Instruction

## Verdict

`blocked / partial`. Substantial runtime/proof-script surface exists, but architecture debt, missing retained proof root, and upstream screen/AI artifacts block closure.

## Assign first

`screen-ai-proof-test-normalization-and-architecture-cleanup`:

- clear banned re-export surfaces in screen/portal/parent AI paths;
- create retained proof manifest/root contract;
- normalize test placement, especially Rust service `src` tests versus crate `tests/` categories;
- run cheap syntax checks and scoped architecture.

## Then

Only after `screen-plan` and `ai-plan` publish current proof artifacts:

1. Windows core proof regeneration;
2. cross-platform feasible proof;
3. live-operator and final product-path closure.

## Coordinate with

- `screen-plan` for capture/retention/live-view prerequisites.
- `ai-plan` for local AI/runtime/provider proof.
- `policy-control-plane-plan` for policy dry-run claims.

## Do not

- Do not claim closure from proof scripts that only pass `node --check`.
- Do not count missing `output/screen-ai-pipeline-proof/` as proof.
- Do not widen while upstream proof roots are absent.
