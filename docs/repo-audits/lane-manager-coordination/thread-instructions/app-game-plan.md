# app-game-plan Instruction

## Verdict

`partial`. Real TS/Rust/portal surfaces exist, but ownership docs, proof roots, parent-domain shims, and runtime/product proof remain incomplete.

## Assign first

`app-game-truth-ownership`:

- make `packages/app-game-domain` the canonical TS owner in docs/source index;
- remove or replace banned `packages/parent-domain/src/app-game*.ts` façade files;
- update consumers without creating new barrels;
- run scoped architecture and package tests.

## Then

1. `app-game-proof-root-normalization`: create canonical `output/app-game-plan-proof/<workpack>/` materialization from real scripts.
2. `app-game-source-policy-timer`: close source freshness -> policy preview/readiness -> timer/read-model chain.
3. `app-game-notification-child-ux`: notification, child UX, and runtime service handoff.
4. `app-game-platform-proof`: Windows/Android/Linux feasible proof; Apple remains external/manual.

## Coordinate with

- `v0-8-enforcement-control-plan` for app-game readiness/preflight/host capability.
- `policy-control-plane-plan` for policy compiler/readiness.
- `app-plan` for native-app truth alignment.

## Do not

- Do not let parent-domain shims remain as ownership proof.
- Do not claim platform blocking without real host-feasible artifacts.
- Do not widen into enforcement execution until readiness/preflight proof is current.
