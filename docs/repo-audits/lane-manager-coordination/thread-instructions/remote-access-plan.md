# remote-access-plan Instruction

## Verdict

`missing / partial contract only`. Do not schedule broad remote product work yet.

## Assign first

Only after account/device/LAN/screen foundations are coherent, assign `ra-01-contract-parity-and-test-repair`:

- align `packages/remote-access-domain` and `crates/remote-access-core` model parity;
- add missing route/capability/session/revoke/expiry/audit contracts;
- repair failing Rust tests;
- add category tests for contract/property/security where needed.

## Then

- protocol/service remote command family;
- portal remote route surface;
- proof scripts and canonical proof roots;
- relay/standing-access proof with remote control still deferred.

## Coordinate with

- account identity for actor/session/household authority;
- device trust for trusted/removed/revoked device state;
- LAN for route/transport assumptions;
- screen plan for live-view primitives.

## Do not

- Do not claim remote closure from screen live-view proof.
- Do not implement remote control; keep it deferred.
- Do not start service fabric before contract parity is fixed.
