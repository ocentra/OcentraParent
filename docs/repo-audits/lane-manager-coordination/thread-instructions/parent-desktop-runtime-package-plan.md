# parent-desktop-runtime-package-plan Instruction

## Verdict

`partial`. Slice1 build/entrypoint cleanup is green; package/distribution proof remains open.

## Assign first

`pcrd-proof-root-and-parent-web-distribution`:

- create canonical parent-client proof root;
- prove parent web distribution route/boundary;
- remove reliance on old `parent-desktop-runtime-package-plan` proof names or empty scaffolds.

## Then

1. desktop shell/package proof;
2. parent-client identity closure;
3. Linux update/rollback proof;
4. Android parent runtime proof;
5. setup handoff contract after setup plan repairs WP03/WP06;
6. release gate/proof manifest.

## Coordinate with

- `setup-install-provisioning-plan` for setup handoff producer contract.
- `child-agent-runtime-distribution-plan` for package identity separation.

## Do not

- Do not claim parent-client identity from old agent package identity.
- Do not claim Apple proof from Windows host.
- Do not count empty `test-results/parent-desktop-release-support-proof/` as proof.
