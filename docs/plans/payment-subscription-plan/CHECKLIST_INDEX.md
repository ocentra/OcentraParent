# Checklist Index

Status: reset.

This checklist tracks execution only. Use it as the shortest path into the live queue, then jump to the source-surface and harness tables.

- Read `AGENTS.md`, `PLAN_STATE.md`, and the selected workpack.
- Use `NEXT_ACTIONS.md` for the current slice and `PLAN_EXECUTION_BLUEPRINT.md` for the first-touch source surface.
- Use `PROOF_AND_TEST_INVENTORY.md` for harness paths, validation commands, and proof bundle paths.
- Lock the proof path outside the plan folder.
- Write or update the code.
- Write or update the tests.
- Compile and validate the touched boundary.
- Run the tests for the touched boundary.
- Run full crate or package validation for the touched boundary.
- Collect proof artifacts in the designated local artifact path.
- Record the proof location outside the plan folder.
- Prepare PR-ready notes and remaining gaps.
