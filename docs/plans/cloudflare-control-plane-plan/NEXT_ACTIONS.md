# Next Actions

## Current slice

- Current slice: `CFCP-A truth-sync and proof-root canonicalization`
- Current owner: `cloudflare-control-plane-plan`
- Current status: `in_progress`

## Ordered queue

| Order | Slice | Status | First-touch surface | Next action | Exit gate |
| --- | --- | --- | --- | --- | --- |
| 00 | Games infra parity extraction | blocked / proof-present | `GAMES_INFRA_PARITY_MAP.md` | Clear the repo-wide `npm run format:check` drift or carve a narrower accepted docs-validation gate, then rerun the WP00 formatting check without widening into unrelated runtime packets. | Parent parity map exists, the proof root is real, and the required docs-validation gate is no longer blocked outside the packet. |
| 01 | Cloudflare module scaffold | blocked / proof-present | `infra/cloudflare/package.json` | Keep the scaffold aligned with the live generated billing-contract source surface and current output inventory, then rerun scoped module lint and a meaningful architecture gate; the gap is unrun focused validation, not missing package imports. | `infra/cloudflare/` source shape and docs agree on what is real versus placeholder, and the scoped WP01 validation set is no longer blocked outside the packet. |
| 02 | Wrangler env and bindings | blocked / proof-present | `infra/cloudflare/wrangler.toml` | Keep wrangler/env docs aligned with the live generated billing-contract source surface and current output inventory, then rerun the required unit and lint commands; the gap is unrun focused validation and dependency bootstrap, not missing package imports. | `output/cloudflare-control-plane-plan-proof/02-wrangler-env-bindings/` stays aligned with explicit dev/prod bindings, placeholder-only secret custody, and reruns that no longer fail outside the packet. |
| 03 | Worker entrypoint runtime guards | blocked / proof-present | `infra/cloudflare/src/index.ts` | The worker entrypoint already consumes the generated billing-contract surface; rerun the scoped unit and integration families here so the worker can prove live fetch-path guards. The gap is the unrun rerun, not source ownership. | Env validation, CORS, request-size, kill-switch, and safe error behavior are explicit in source and runtime-proven through the booted worker entrypoint. |
| 04 | Route manifest and domain contracts | blocked / proof-present | `infra/cloudflare/src/routes.ts` | Keep the route-manifest packet aligned with the live generated billing-contract source surface, then rerun the scoped unit, property, contract, and integration families so the exact route list and auth exception proofs pair with live contract and dispatch coverage. | `output/cloudflare-control-plane-plan-proof/04-route-manifest-and-domain-contracts/` stays aligned with the manifest and auth model, no raw route strings drift outside the manifest, and contract plus integration reruns reach the live contract surface. |
| 05 | Auth, admin, and support boundary | blocked / proof-present | `infra/cloudflare/src/auth/verifier.ts` | Keep the auth/admin/support packet aligned with the live generated billing-contract source surface, then rerun the unit, security, and integration families so the owned packet can move from blocked proof to booted-worker validation. | Private, admin, support, webhook, and queue trust states are explicit, and the focused WP05 validation set no longer depends on stale package-import wording. |
| 06 | Storage and coordination bindings | blocked / proof-present | `infra/cloudflare/src/env.ts` | Keep the storage and coordination packet aligned with the live generated billing-contract source surface, then rerun the required unit, integration, and property families so WP06 can move from direct owned proof plus carried blocker to booted-worker validation. | `output/cloudflare-control-plane-plan-proof/06-storage-do-d1-kv-r2-queue-bindings/` stays aligned with explicit DO/D1/KV/Queue/R2 ownership, no-child-data boundaries, and the required family reruns reach the live source surface. |
| 07 | Local dev, seeding, and fixtures | blocked / proof-present | `LOCAL_DEV_AND_SEEDING_MODEL.md` | Keep the local-dev and seed packet aligned with the live generated billing-contract source surface, then rerun the owned workflow probe and integration test so local start and seed can move from explicit blockers to runnable paths. | `output/cloudflare-control-plane-plan-proof/07-local-dev-seeding-and-fixtures/` stays aligned with the model, fixture families remain explicit, and the scoped reruns no longer report blocked start or seed states. |
| 08 | Test runner and test pyramid | blocked / proof-present | `infra/cloudflare/scripts/test-runner.ts` | Keep the test runner aligned with the live generated billing-contract source surface, then rerun the narrowed required-family runner and module lint without widening into extra Cloudflare suites. | Test families, exact assertions, blocker states, and proof paths are explicit and command-backed under `output/cloudflare-control-plane-plan-proof/08-testing-runner-and-test-pyramid/`. |
| 09 | Portal-to-worker smoke | blocked / proof-present | `output/cloudflare-control-plane-plan-proof/09-portal-to-worker-e2e-smoke/` | Keep the portal-to-worker smoke aligned with the live generated billing-contract source surface, then rerun the owned e2e family so the smoke can reach `/auth/billing/status` through a booted worker. | Parent portal consumer can point at a real smoke artifact that proves redacted billing-status handoff without secret leakage or raw worker failures. |
| 10 | Security, property, fuzz, observability | blocked / proof-present | `TESTING_STRATEGY.md` | Keep the security, property, fuzz, and observability packet aligned with the live generated billing-contract source surface, then rerun the required families without widening into excluded Cloudflare tests. | Security/property/fuzz/observability expectations are explicit and scoped, and execution produces real command output or exact blockers under `output/cloudflare-control-plane-plan-proof/10-security-fuzz-property-observability/`. |
| 11 | Deployment and promotion | blocked / proof-present | `DEPLOYMENT_MODEL.md` | Keep the deployment and promotion packet aligned with the live generated billing-contract source surface, provision real resource identifiers and out-of-repo secrets, then rerun deploy plus post-deploy smoke so promotion and rollback can move from manual-required notes to real artifacts. | `output/cloudflare-control-plane-plan-proof/11-deployment-and-environment-promotion/` records dev/prod dry-run truth, placeholder/manual-required blockers, and the exact missing conditions for publish, smoke, and rollback proof. |
| 12 | Payment handoff gate | blocked / proof-present | `output/cloudflare-control-plane-plan-proof/12-payment-plan-handoff-gate/` | Point `payment-subscription-plan` WP00 at the refreshed handoff artifact, then keep historical WP01-WP11 references distinct from the current checkout output inventory; the current gap is unrun focused gates, WP02 lint debt, placeholder deployment custody, and missing downstream payment acknowledgment. | Payment WP00 can point at a real Cloudflare handoff artifact that separates current checkout output inventory from historical plan references and keeps the remaining blockers explicit without turning them into readiness claims. |

## Working rules

- Move exactly one row to `in_progress` when implementation starts.
- Do not start payment runtime slices while row 12 still lacks proof.
- Keep scaffold placeholders honest: `exists` is not the same as `validated`.
- Do not invent, shrink, or merge away test scope outside
  `REQUIRED_TEST_ASSERTION_MATRIX.md`.
- Keep `PLAN_EXECUTION_SCORECARD.md` aligned with real module and proof state.
