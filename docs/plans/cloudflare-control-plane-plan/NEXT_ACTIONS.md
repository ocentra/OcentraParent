# Next Actions

## Current slice

- Current slice: `CFCP-A truth-sync and proof-root canonicalization`; WP01 evidence refresh complete
- Current owner: `cloudflare-control-plane-plan`
- Current status: `in_progress`

## Ordered queue

| Order | Slice | Status | First-touch surface | Next action | Exit gate |
| --- | --- | --- | --- | --- | --- |
| 00 | Games infra parity extraction | blocked / proof-present | `GAMES_INFRA_PARITY_MAP.md` | Clear the repo-wide `npm run format:check` drift or carve a narrower accepted docs-validation gate, then rerun the WP00 formatting check without widening into unrelated runtime packets. | Parent parity map exists, the proof root is real, and the required docs-validation gate is no longer blocked outside the packet. |
| 01 | Cloudflare module scaffold | scoped acceptance complete / proof-present | `infra/cloudflare/package.json` | Maintain the retained proof and source-surface inventory when the module tree or script contract changes; do not reopen the stale missing-billing-domain import blocker without a fresh failing command. | Module source shape, no-claim boundaries, retained proof, module lint, unit family, and architecture gate agree. This does not close later runtime, binding, deployment, or payment workpacks. |
| 02 | Wrangler env and bindings | blocked / proof-present | `infra/cloudflare/wrangler.toml` | Restore the missing billing-domain boundary modules and broader Cloudflare lint health, then rerun the required unit and lint commands so WP02 can move from direct wrangler/dev-vars proof to fully green scoped validation. | `output/cloudflare-control-plane-plan-proof/02-wrangler-env-bindings/` stays aligned with explicit dev/prod bindings, placeholder-only secret custody, and reruns that no longer fail outside the packet. |
| 03 | Worker entrypoint runtime guards | blocked / proof-present | `infra/cloudflare/src/index.ts` | Restore `packages/billing-domain/src/billing-checkout-portal-boundary.js`, then rerun the scoped unit and integration families so the worker entrypoint can prove live fetch-path guards instead of blocking at module resolution. | Env validation, CORS, request-size, kill-switch, and safe error behavior are explicit in source and runtime-proven through the booted worker entrypoint. |
| 04 | Route manifest and domain contracts | blocked / proof-present | `infra/cloudflare/src/routes.ts` | Restore `packages/billing-domain/src/billing-checkout-portal-boundary.js`, then rerun the scoped unit, property, contract, and integration families so the exact route list and auth exception proofs can pair with live contract and dispatch coverage. | `output/cloudflare-control-plane-plan-proof/04-route-manifest-and-domain-contracts/` stays aligned with the manifest and auth model, no raw route strings drift outside the manifest, and contract plus integration reruns no longer block on the missing billing boundary import. |
| 05 | Auth, admin, and support boundary | blocked / proof-present | `infra/cloudflare/src/auth/verifier.ts` | Restore `packages/billing-domain/src/billing-checkout-portal-boundary.js`, then rerun the unit, security, and integration families so the owned auth/admin/support packet can move from exact blocked proof to booted-worker validation. | Private, admin, support, webhook, and queue trust states are explicit, and the focused WP05 validation set no longer fails on the external billing import. |
| 06 | Storage and coordination bindings | blocked / proof-present | `infra/cloudflare/src/env.ts` | Restore `packages/billing-domain/src/billing-checkout-portal-boundary.js`, then rerun the required unit, integration, and property families so WP06 can move from direct owned proof plus carried blocker to booted-worker validation. | `output/cloudflare-control-plane-plan-proof/06-storage-do-d1-kv-r2-queue-bindings/` stays aligned with explicit DO/D1/KV/Queue/R2 ownership, no-child-data boundaries, and the required family reruns no longer fail on worker module resolution. |
| 07 | Local dev, seeding, and fixtures | blocked / proof-present | `LOCAL_DEV_AND_SEEDING_MODEL.md` | Restore the missing billing-domain runtime boundary modules, then rerun the owned workflow probe and integration test so local start and seed can move from explicit blockers to runnable paths. | `output/cloudflare-control-plane-plan-proof/07-local-dev-seeding-and-fixtures/` stays aligned with the model, fixture families remain explicit, and the scoped reruns no longer report blocked start or seed states. |
| 08 | Test runner and test pyramid | blocked / proof-present | `infra/cloudflare/scripts/test-runner.ts` | Restore the missing billing-domain boundary modules, then rerun the narrowed required-family runner and module lint without widening into extra Cloudflare suites. | Test families, exact assertions, blocker states, and proof paths are explicit and command-backed under `output/cloudflare-control-plane-plan-proof/08-testing-runner-and-test-pyramid/`. |
| 09 | Portal-to-worker smoke | blocked / proof-present | `output/cloudflare-control-plane-plan-proof/09-portal-to-worker-e2e-smoke/` | Restore `packages/billing-domain/src/billing-checkout-portal-boundary.js`, then rerun the owned e2e family so the portal-to-worker smoke can reach `/auth/billing/status` through a booted worker without widening into checkout, admin, or provider flows. | Parent portal consumer can point at a real smoke artifact that proves redacted billing-status handoff without secret leakage or raw worker failures. |
| 10 | Security, property, fuzz, observability | blocked / proof-present | `TESTING_STRATEGY.md` | Restore the missing billing-domain boundary import, then rerun the required security, property, fuzz, and carried observability integration families without widening into excluded Cloudflare tests. | Security/property/fuzz/observability expectations are explicit and scoped, and execution produces real command output or exact blockers under `output/cloudflare-control-plane-plan-proof/10-security-fuzz-property-observability/`. |
| 11 | Deployment and promotion | blocked / proof-present | `DEPLOYMENT_MODEL.md` | Restore the missing billing-domain runtime boundary modules, provision real resource identifiers and out-of-repo secrets, then rerun deploy plus post-deploy smoke so promotion and rollback can move from manual-required notes to real artifacts. | `output/cloudflare-control-plane-plan-proof/11-deployment-and-environment-promotion/` records dev/prod dry-run truth, placeholder/manual-required blockers, and the exact missing conditions for publish, smoke, and rollback proof. |
| 12 | Payment handoff gate | blocked / proof-present | `output/cloudflare-control-plane-plan-proof/12-payment-plan-handoff-gate/` | Point `payment-subscription-plan` WP00 at the refreshed handoff artifact, then keep the accepted WP01-WP11 blocked truths explicit while upstream Cloudflare validation remains blocked on missing billing-domain runtime boundary modules, WP02 lint debt, placeholder deployment custody, and missing downstream payment acknowledgment. | Payment WP00 can point at a real Cloudflare handoff artifact that lists the current accepted proof roots, no missing Cloudflare proof roots, exact carried blockers, and the still-missing downstream acknowledgment without turning blocked roots into readiness claims. |

## Working rules

- Move exactly one row to `in_progress` when implementation starts.
- Do not start payment runtime slices while row 12 still lacks proof.
- Keep scaffold placeholders honest: `exists` is not the same as `validated`.
- Do not invent, shrink, or merge away test scope outside
  `REQUIRED_TEST_ASSERTION_MATRIX.md`.
- Keep `PLAN_EXECUTION_SCORECARD.md` aligned with real module and proof state.
