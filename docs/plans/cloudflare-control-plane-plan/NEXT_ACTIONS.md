# Next Actions

## Current slice

- Current slice: `CFCP-A truth-sync and proof-root canonicalization`
- Current owner: `cloudflare-control-plane-plan`
- Current status: `in_progress`

## Ordered queue

| Order | Slice | Status | First-touch surface | Next action | Exit gate |
| --- | --- | --- | --- | --- | --- |
| 00 | Games infra parity extraction | blocked / proof-required | `GAMES_INFRA_PARITY_MAP.md` | Clear the repo-wide `npm run format:check` drift or carve a narrower accepted docs-validation gate, then rerun the WP00 formatting check; the current checkout carries no proof artifact. | Parent parity map exists in docs, but the proof artifact is absent from the current checkout. |
| 01 | Cloudflare module scaffold | blocked / proof-required | `infra/cloudflare/package.json` | Keep the scaffold aligned with the live generated billing-contract source surface and current output inventory, then rerun scoped module lint and a meaningful architecture gate; the current checkout carries no proof artifact. | `infra/cloudflare/` source shape and docs agree on what is real versus placeholder, but the proof artifact is absent from the current checkout. |
| 02 | Wrangler env and bindings | blocked / proof-required | `infra/cloudflare/wrangler.toml` | Keep wrangler/env docs aligned with the live generated billing-contract source surface and current output inventory, then rerun the required unit and lint commands; the current checkout carries no proof artifact. | Explicit dev/prod bindings and placeholder-only secret custody remain documented, but the proof artifact is absent from the current checkout. |
| 03 | Worker entrypoint runtime guards | blocked / proof-required | `infra/cloudflare/src/index.ts` | The worker entrypoint already consumes the generated billing-contract surface; rerun the scoped unit and integration families here so the worker can prove live fetch-path guards. The current checkout carries no proof artifact. | Env validation, CORS, request-size, kill-switch, and safe error behavior are explicit in source, but the proof artifact is absent from the current checkout. |
| 04 | Route manifest and domain contracts | blocked / proof-required | `infra/cloudflare/src/routes.ts` | Keep the route-manifest packet aligned with the live generated billing-contract source surface, then rerun the scoped unit, property, contract, and integration families so the exact route list and auth exception proofs pair with live contract and dispatch coverage; the current checkout carries no proof artifact. | The manifest and auth model remain documented, but the proof artifact is absent from the current checkout. |
| 05 | Auth, admin, and support boundary | blocked / proof-required | `infra/cloudflare/src/auth/verifier.ts` | Keep the auth/admin/support packet aligned with the live generated billing-contract source surface, then rerun the unit, security, and integration families so the owned packet can move from blocked proof to booted-worker validation; the current checkout carries no proof artifact. | Private, admin, support, webhook, and queue trust states are explicit, but the proof artifact is absent from the current checkout. |
| 06 | Storage and coordination bindings | blocked / proof-required | `infra/cloudflare/src/env.ts` | Keep the storage and coordination packet aligned with the live generated billing-contract source surface, then rerun the required unit, integration, and property families so WP06 can move from direct owned proof plus carried blocker to booted-worker validation; the current checkout carries no proof artifact. | DO/D1/KV/Queue/R2 ownership stays explicit, but the proof artifact is absent from the current checkout. |
| 07 | Local dev, seeding, and fixtures | blocked / proof-required | `LOCAL_DEV_AND_SEEDING_MODEL.md` | Keep the local-dev and seed packet aligned with the live generated billing-contract source surface, then rerun the owned workflow probe and integration test so local start and seed can move from explicit blockers to runnable paths; the current checkout carries no proof artifact. | The local dev and seed model remains explicit, but the proof artifact is absent from the current checkout. |
| 08 | Test runner and test pyramid | blocked / proof-required | `infra/cloudflare/scripts/test-runner.ts` | Keep the test runner aligned with the live generated billing-contract source surface, then rerun the narrowed required-family runner and module lint without widening into extra Cloudflare suites; the current checkout carries no proof artifact. | Test families, exact assertions, blocker states, and proof paths are explicit, but the proof artifact is absent from the current checkout. |
| 09 | Portal-to-worker smoke | blocked / proof-required | `output/cloudflare-control-plane-plan-proof/09-portal-to-worker-e2e-smoke/` | Keep the portal-to-worker smoke aligned with the live generated billing-contract source surface, then rerun the owned e2e family so the smoke can reach `/auth/billing/status` through a booted worker; the current checkout carries no proof artifact. | Parent portal consumer expectations remain documented, but the proof artifact is absent from the current checkout. |
| 10 | Security, property, fuzz, observability | blocked / proof-required | `TESTING_STRATEGY.md` | Keep the security, property, fuzz, and observability packet aligned with the live generated billing-contract source surface, then rerun the required families without widening into excluded Cloudflare tests; the current checkout carries no proof artifact. | Security/property/fuzz/observability expectations stay explicit, but the proof artifact is absent from the current checkout. |
| 11 | Deployment and promotion | blocked / proof-required | `DEPLOYMENT_MODEL.md` | Keep the deployment and promotion packet aligned with the live generated billing-contract source surface, provision real resource identifiers and out-of-repo secrets, then rerun deploy plus post-deploy smoke so promotion and rollback can move from manual-required notes to real artifacts; the current checkout carries no proof artifact. | Deployment model notes remain explicit, but the proof artifact is absent from the current checkout. |
| 12 | Payment handoff gate | blocked / proof-required | `output/cloudflare-control-plane-plan-proof/12-payment-plan-handoff-gate/` | Keep historical WP01-WP11 references distinct from the current checkout output inventory; the current gap is unrun focused gates, WP02 lint debt, placeholder deployment custody, and missing downstream payment acknowledgment. The defined expected output path is present in docs, but the generated proof is absent/not produced in this checkout. | The handoff proof is generated, validated, consumed downstream, and the carried blockers are resolved or explicitly accepted. |

## Working rules

- Move exactly one row to `in_progress` when implementation starts.
- Do not start payment runtime slices while row 12 still lacks proof.
- Keep scaffold placeholders honest: `exists` is not the same as `validated`.
- Do not invent, shrink, or merge away test scope outside
  `REQUIRED_TEST_ASSERTION_MATRIX.md`.
- Keep `PLAN_EXECUTION_SCORECARD.md` aligned with real module and proof state.
