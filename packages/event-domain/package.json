{
  "name": "@ocentra-parent/event-domain",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "Shared event, message, peer, and route identity contracts for UI-facing schemas and runtime protocol handoff.",
  "exports": {
    "./eventing": {
      "import": "./dist/eventing.js",
      "types": "./dist/eventing.d.ts"
    },
    "./primitives": {
      "import": "./dist/primitives.js",
      "types": "./dist/primitives.d.ts"
    },
    "./package-info": {
      "import": "./dist/package-info.js",
      "types": "./dist/package-info.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../../scripts/fix-esm-imports.mjs dist",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "npx turbo run lint:exec --filter=@ocentra-parent/event-domain",
    "lint:exec": "eslint --cache src tests && tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ocentra-parent/schema-domain": "file:../schema-domain"
  }
}
