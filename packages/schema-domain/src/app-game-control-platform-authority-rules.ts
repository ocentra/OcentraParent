import { platformAuthorityRowIsConsistent as platformAuthorityRowIsConsistentImpl } from './app-game-control-platform-authority-rules-checks';

export const platformAuthorityRowIsConsistent = platformAuthorityRowIsConsistentImpl;
