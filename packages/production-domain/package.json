{
  "name": "@ocentra-parent/production-domain",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "Production release, support, public status, incident, and publication contracts.",
  "exports": {
    "./*": {
      "import": "./dist/src/*.js",
      "types": "./dist/src/*.d.ts"
    },
    "./package-info": {
      "import": "./dist/src/package-info.js",
      "types": "./dist/src/package-info.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../../scripts/fix-esm-imports.mjs dist",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "npx turbo run lint:exec --filter=@ocentra-parent/production-domain",
    "lint:exec": "eslint --cache src tests && tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ocentra-parent/family-domain": "file:../family-domain",
    "@ocentra-parent/schema-domain": "file:../schema-domain"
  }
}
