name: CI Tooling

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tooling:
    name: Repo Tooling and Script Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup-ci
        with:
          install-rust: 'false'

      - name: Tooling tests
        run: npm run test:tooling
