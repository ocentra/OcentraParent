# Handlers

This directory is reserved for request handlers that consume route-manifest entries from `src/routes.ts`.
Do not copy game-only handlers into Parent. Each future handler must point back to the owning plan workpack and proof path.
