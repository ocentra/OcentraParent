# Local Dev

Intended local commands:

- root: `npm run dev:cloudflare`
- module: `npm run dev`
- seeding: `npm run seed:local`

Current blocker:

- the worker shell exists, but local dev and seeding remain `manual-required`
- placeholder scripts intentionally fail with exact blockers instead of pretending the module is ready
