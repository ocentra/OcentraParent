# Routes

Route truth lives in:

- `src/routes.ts`
- `docs/plans/cloudflare-control-plane-plan/ROUTE_MANIFEST_MODEL.md`

Billing consumers must reuse the shared route groups instead of creating a second Cloudflare API surface.
